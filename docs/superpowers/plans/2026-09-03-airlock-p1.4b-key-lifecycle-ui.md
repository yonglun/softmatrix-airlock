# P1.4b 密钥生命周期界面 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 P1.3a/b/c 已经做完、但在控制台上完全够不着的密钥能力（轮换、按子树批量吊销、全局紧急吊销）接出来，并补上「我的密钥」这一页。

**Architecture:** 后端补四处小增量（子树查询、责任人自助吊销、`keyView` 轮换字段、`GET /api/keys/mine`），前端新增两个页面并抽出三个共享组件。子树查询与子树吊销使用逐字相同的节点选择子句——预览与开火必须命中同一批，这是整个设计的支点。

**Tech Stack:** Go 1.25（`net/http` + pgx + goose）、Next.js 15 静态导出 + Ant Design 5、Vitest + React Testing Library、Postgres。

**设计文档:** `docs/superpowers/specs/2026-09-03-airlock-p1.4b-key-lifecycle-ui-design.md`

---

## 文件结构

**后端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `internal/control/model.go` | `KeyStore` 接口 | 改：加 `ListByOrgSubtree`、`ListByUser` |
| `internal/control/keystore.go` | Postgres 实现 | 改：实现上面两个 |
| `internal/control/keystore_test.go` | store 测试 | 改：加两组测试 |
| `internal/control/keyapi.go` | 密钥处理器 | 改：`keyView` 加两字段、`HandleList` 支持子树、`HandleRevoke` 加 owner 分支、新增 `HandleMine` |
| `internal/control/keyapi_test.go` | 处理器测试 | 改：加四组测试 |
| `internal/control/routes.go` | 路由表 | 改：加 `GET /api/keys/mine` |
| `internal/control/routes_test.go` | 路由机械检查 | 改：覆盖清单加一条 |

**前端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `web/src/lib/types.ts` | wire 类型 | 改：加 `ApiKey`/`IssuedKey`/`RevokeResult`，删 `ClaimedKey` |
| `web/src/lib/keyDisplay.ts` | 状态标签与轮换状态文案（纯函数） | 新建 |
| `web/src/lib/keyDisplay.test.ts` | 上面的单测 | 新建 |
| `web/src/components/OrgTree.tsx` | 组织树（从组织页搬出） | 新建 |
| `web/src/components/OrgTree.test.tsx` | `toTree` 单测 | 新建 |
| `web/src/components/PlaintextKeyModal.tsx` | 一次性明文弹窗（从申请页搬出） | 新建 |
| `web/src/components/KeyRotateModal.tsx` | 轮换弹窗（两页共用） | 新建 |
| `web/src/components/SubtreeRevokeModal.tsx` | 子树吊销的预览与确认 | 新建 |
| `web/src/components/DangerZone.tsx` | 全局紧急吊销 | 新建 |
| `web/src/components/DangerZone.test.tsx` | 上面的单测 | 新建 |
| `web/src/components/workbenches.ts` | 导航元数据 | 改：加两条 |
| `web/src/app/platform/orgs/page.tsx` | 组织与成员页 | 改：改用 `OrgTree` |
| `web/src/app/my/requests/page.tsx` | 我的申请页 | 改：改用 `PlaintextKeyModal` |
| `web/src/app/platform/keys/page.tsx` | 虚拟密钥页 | 新建 |
| `web/src/app/my/keys/page.tsx` | 我的密钥页 | 新建 |

**为什么这么切**：`KeyRotateModal` 与 `SubtreeRevokeModal` 各自封装一段完整的交互（含自己的请求与错误处理），放进页面会让页面文件迅速失控；`keyDisplay.ts` 是纯函数，独立出来才能单测。密钥表格**故意不抽**——两页的列不同（平台侧要「责任人」，我的密钥要「所属节点」），参数化的代价高于重复。

---

## Task 1: `KeyStore.ListByOrgSubtree`

**Files:**
- Modify: `internal/control/model.go`（`KeyStore` 接口）
- Modify: `internal/control/keystore.go`
- Test: `internal/control/keystore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加：

```go
func TestListByOrgSubtreeCoversDescendants(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "root", "/root")
	seedOrg(t, db, "rd", "/root/rd")
	seedOrg(t, db, "gw2", "/root/rd/gw2")
	seedKey(t, db, "k-rd", "h-rd", "rd", uid, "active")
	seedKey(t, db, "k-deep", "h-deep", "gw2", uid, "active")

	got, err := keys.ListByOrgSubtree(ctx, "/root/rd")
	require.NoError(t, err)
	ids := []string{}
	for _, k := range got {
		ids = append(ids, k.ID)
	}
	require.ElementsMatch(t, []string{"k-rd", "k-deep"}, ids, "节点自身与后代都要在")
}

func TestListByOrgSubtreeSpareSiblingWithSamePrefix(t *testing.T) {
	// 第五次遇到这个陷阱（P1.2b 权限判定、P1.3b 审批人查找、
	// P1.3c 子树吊销、P1.4a 待审列表各踩过一次）：
	// /root/rd 是 /root/rd2 的字符串前缀，但 rd2 并不在 rd 的子树里。
	//
	// 这条尤其要紧：这个查询是子树吊销的**预览**。多算一个兄弟节点，
	// 管理员会以为吊销会打到那些密钥；少算则会漏报影响范围。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "root", "/root")
	seedOrg(t, db, "rd", "/root/rd")
	seedOrg(t, db, "rd2", "/root/rd2")
	seedKey(t, db, "k-rd", "h-rd", "rd", uid, "active")
	seedKey(t, db, "k-rd2", "h-rd2", "rd2", uid, "active")

	got, err := keys.ListByOrgSubtree(ctx, "/root/rd")
	require.NoError(t, err)
	require.Len(t, got, 1)
	require.Equal(t, "k-rd", got[0].ID, "同前缀兄弟节点不能出现在预览里")
}

func TestListByOrgSubtreeIncludesAllStatuses(t *testing.T) {
	// 浏览要看得见历史，因此不限状态——与 RevokeByOrgSubtree 的
	// status IN ('active','pending') 不同。「将吊销 N 把」的 N
	// 由调用方按状态自己数。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "rd", "/rd")
	seedKey(t, db, "k-a", "h-a", "rd", uid, "active")
	seedKey(t, db, "k-p", "h-p", "rd", uid, "pending")
	seedKey(t, db, "k-r", "h-r", "rd", uid, "revoked")

	got, err := keys.ListByOrgSubtree(ctx, "/rd")
	require.NoError(t, err)
	require.Len(t, got, 3)
}

func TestListByOrgSubtreeEmptyReturnsEmptySlice(t *testing.T) {
	_, _, _, keys, db, _ := issuerFixture(t)
	seedOrg(t, db, "rd", "/rd")

	got, err := keys.ListByOrgSubtree(context.Background(), "/rd")
	require.NoError(t, err)
	require.NotNil(t, got, "空结果要是空切片，否则 JSON 会序列化成 null")
	require.Empty(t, got)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListByOrgSubtree
```

Expected: 编译失败 —— `keys.ListByOrgSubtree undefined (type KeyStore has no field or method ListByOrgSubtree)`

- [ ] **Step 3: 加接口方法**

在 `internal/control/model.go` 的 `KeyStore` 接口里，紧跟 `RevokeByOrgSubtree` 那一条之后加：

```go
	// ListByOrgSubtree 返回该路径子树下的全部密钥，不限状态
	// （管理员要看得见历史）。节点选择子句与 RevokeByOrgSubtree
	// 逐字相同——它是子树吊销的预览，两处一旦分叉，预览就在撒谎。
	ListByOrgSubtree(ctx context.Context, orgPath string) ([]*APIKey, error)
```

- [ ] **Step 4: 写实现**

在 `internal/control/keystore.go` 里 `RevokeByOrgSubtree` 函数**之后**加：

```go
// ListByOrgSubtree 返回子树下的全部密钥，不限状态。
//
// 节点选择子句与 RevokeByOrgSubtree 逐字相同，这就是它存在的理由：
// 子树批量吊销不可逆，预览的唯一价值是让人看见即将发生什么。
// 两处匹配规则一旦分叉，预览比没有预览更危险。
func (s *postgresKeyStore) ListByOrgSubtree(
	ctx context.Context, orgPath string,
) ([]*APIKey, error) {
	rows, err := s.db.QueryContext(ctx, `
		SELECT `+keyColumns+` FROM api_keys
		WHERE org_id IN (
		    SELECT id FROM organizations
		    WHERE path = $1 OR path LIKE $1 || '/%'
		)
		ORDER BY created_at DESC`, orgPath)
	if err != nil {
		return nil, fmt.Errorf("查询子树密钥失败: %w", err)
	}
	defer func() { _ = rows.Close() }()
	return collectKeys(rows)
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListByOrgSubtree -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 6: 提交**

```bash
git add internal/control/model.go internal/control/keystore.go internal/control/keystore_test.go
```

```bash
git commit -m "feat: 按子树查询密钥，与子树吊销共用同一套节点选择子句"
```

---

## Task 2: `KeyStore.ListByUser`

**Files:**
- Modify: `internal/control/model.go`
- Modify: `internal/control/keystore.go`
- Test: `internal/control/keystore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加：

```go
func TestListByUserReturnsOnlyOwnKeysAcrossOrgs(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	other := seedUserID(t, db, "list-by-user-other")
	seedOrg(t, db, "a", "/a")
	seedOrg(t, db, "b", "/b")
	seedKey(t, db, "k-mine-a", "h1", "a", uid, "active")
	seedKey(t, db, "k-mine-b", "h2", "b", uid, "active")
	seedKey(t, db, "k-theirs", "h3", "a", other, "active")

	got, err := keys.ListByUser(ctx, uid)
	require.NoError(t, err)
	ids := []string{}
	for _, k := range got {
		ids = append(ids, k.ID)
	}
	require.ElementsMatch(t, []string{"k-mine-a", "k-mine-b"}, ids,
		"跨节点的本人密钥都要在，别人的一把都不能有")
}

func TestListByUserEmptyReturnsEmptySlice(t *testing.T) {
	_, _, _, keys, db, _ := issuerFixture(t)
	nobody := seedUserID(t, db, "list-by-user-nobody")

	got, err := keys.ListByUser(context.Background(), nobody)
	require.NoError(t, err)
	require.NotNil(t, got, "空结果要是空切片，否则 JSON 会序列化成 null")
	require.Empty(t, got)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListByUser
```

Expected: 编译失败 —— `keys.ListByUser undefined`

- [ ] **Step 3: 加接口方法**

在 `internal/control/model.go` 的 `KeyStore` 接口里，紧跟 Task 1 加的那条之后：

```go
	// ListByUser 返回该用户作为责任人的全部密钥，跨节点。
	ListByUser(ctx context.Context, userID string) ([]*APIKey, error)
```

- [ ] **Step 4: 写实现**

在 `internal/control/keystore.go` 里 `ListByOrgSubtree` **之后**加：

```go
// ListByUser 返回该用户作为责任人的全部密钥，跨节点。
func (s *postgresKeyStore) ListByUser(ctx context.Context, userID string) ([]*APIKey, error) {
	rows, err := s.db.QueryContext(ctx,
		`SELECT `+keyColumns+` FROM api_keys WHERE user_id = $1 ORDER BY created_at DESC`,
		userID)
	if err != nil {
		return nil, fmt.Errorf("查询本人密钥失败: %w", err)
	}
	defer func() { _ = rows.Close() }()
	return collectKeys(rows)
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListByUser -v
```

Expected: 2 个测试 PASS

- [ ] **Step 6: 提交**

```bash
git add internal/control/model.go internal/control/keystore.go internal/control/keystore_test.go
```

```bash
git commit -m "feat: 按责任人查询密钥"
```

---

## Task 3: `keyView` 暴露轮换状态

**Files:**
- Modify: `internal/control/keyapi.go:29-53`
- Test: `internal/control/keyapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyapi_test.go` 末尾追加：

```go
func TestKeyViewCarriesRotationState(t *testing.T) {
	// 轮换完界面要说得出「旧凭据还能用多久」——共存窗口的存在意义
	// 就是给客户端替换凭据的时间，而这段时间还剩多少是唯一需要被看见的信息。
	api, _, _, _, _, uid := keyAPIFixture(t)
	rec := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"测试","models":[]}`)
	require.Equal(t, http.StatusCreated, rec.Code)

	var created struct {
		ID               string  `json:"id"`
		RotatedAt        *string `json:"rotated_at"`
		PrevKeyExpiresAt *string `json:"prev_key_expires_at"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &created))
	require.Nil(t, created.RotatedAt, "没轮换过的密钥这两个字段是空的")
	require.Nil(t, created.PrevKeyExpiresAt)

	rot := rotateReq(t, api, uid, created.ID, `{"window_seconds":3600}`)
	require.Equal(t, http.StatusOK, rot.Code)

	var rotated struct {
		RotatedAt        *string `json:"rotated_at"`
		PrevKeyExpiresAt *string `json:"prev_key_expires_at"`
	}
	require.NoError(t, json.Unmarshal(rot.Body.Bytes(), &rotated))
	require.NotNil(t, rotated.RotatedAt, "轮换后必须能看到轮换时间")
	require.NotNil(t, rotated.PrevKeyExpiresAt, "以及旧凭据的到期时间")
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestKeyViewCarriesRotationState -v
```

Expected: FAIL —— `Expected value not to be nil`（字段还没上 wire，反序列化拿到 nil）

- [ ] **Step 3: 改 `keyView` 与 `viewOf`**

`internal/control/keyapi.go`，把 `keyView` 结构体改成（只在 `ExpiresAt` 与 `CreatedAt` 之间插两行）：

```go
type keyView struct {
	ID             string     `json:"id"`
	KeyPrefix      string     `json:"key_prefix"`
	OrgID          string     `json:"org_id"`
	UserID         string     `json:"user_id"`
	Name           string     `json:"name"`
	Status         string     `json:"status"`
	Models         []string   `json:"models"`
	MaxBudget      *float64   `json:"max_budget"`
	BudgetDuration *string    `json:"budget_duration"`
	RPMLimit       *int       `json:"rpm_limit"`
	TPMLimit       *int       `json:"tpm_limit"`
	ExpiresAt      *time.Time `json:"expires_at"`
	// 轮换状态。共存窗口还剩多久，是轮换这个功能唯一需要被看见的信息；
	// 没有这两个字段，界面只能说「轮换成功」，说不出旧凭据还能用多久。
	RotatedAt        *time.Time `json:"rotated_at"`
	PrevKeyExpiresAt *time.Time `json:"prev_key_expires_at"`
	CreatedAt        time.Time  `json:"created_at"`
}
```

`viewOf` 相应加两个字段：

```go
func viewOf(k *APIKey) keyView {
	return keyView{
		ID: k.ID, KeyPrefix: k.KeyPrefix, OrgID: k.OrgID, UserID: k.UserID,
		Name: k.Name, Status: k.Status, Models: orEmptyStrings(k.Models),
		MaxBudget: k.MaxBudget, BudgetDuration: k.BudgetDuration,
		RPMLimit: k.RPMLimit, TPMLimit: k.TPMLimit,
		ExpiresAt: k.ExpiresAt,
		RotatedAt: k.RotatedAt, PrevKeyExpiresAt: k.PrevKeyExpiresAt,
		CreatedAt: k.CreatedAt,
	}
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestKeyView -v
```

Expected: PASS

- [ ] **Step 5: 跑整包，确认没有测试依赖旧的 JSON 形状**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -count=1
```

Expected: `ok`（新增字段是纯增量，既有断言都是按字段取值而非整体比对）

- [ ] **Step 6: 提交**

```bash
git add internal/control/keyapi.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: keyView 暴露轮换状态"
```

---

## Task 4: `HandleList` 支持 `?subtree=true`

**Files:**
- Modify: `internal/control/keyapi.go:97-110`
- Test: `internal/control/keyapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyapi_test.go` 末尾追加。注意这里**不用** `keyAPIFixture`（它不返回 `db`，而子树匹配走的是真实 `organizations` 表），直接用 `issuerFixture` 自己组装：

```go
// listReq 发一次列表请求；subtree 为 true 时带上 ?subtree=true。
func listReq(t *testing.T, api *KeyAPI, orgID string, subtree bool) *httptest.ResponseRecorder {
	t.Helper()
	url := "/api/orgs/" + orgID + "/keys"
	if subtree {
		url += "?subtree=true"
	}
	req := httptest.NewRequest(http.MethodGet, url, nil)
	req.SetPathValue("id", orgID)
	rec := httptest.NewRecorder()
	api.HandleList(rec, req)
	return rec
}

func TestListAPIDefaultIsSingleNodeAndSubtreeIsOptIn(t *testing.T) {
	iss, orgs, _, keys, db, uid := issuerFixture(t)
	rbac := newFakeRBACStore()
	rbac.setPath("gw", "/gw")
	api := NewKeyAPI(iss, keys, orgs, authz.NewResolver(rbac))

	// issuerFixture 已经在真实库与 fakeOrgStore 里都建好了 gw（path=/gw）。
	// 子节点只需要进真实库——子树匹配查的是 organizations 表。
	seedOrg(t, db, "sub", "/gw/sub")
	seedKey(t, db, "k-gw", "h-gw", "gw", uid, "active")
	seedKey(t, db, "k-sub", "h-sub", "sub", uid, "active")

	rec := listReq(t, api, "gw", false)
	require.Equal(t, http.StatusOK, rec.Code)
	var single []struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &single))
	require.Len(t, single, 1, "缺省行为必须不变：只列本节点")
	require.Equal(t, "k-gw", single[0].ID)

	rec2 := listReq(t, api, "gw", true)
	require.Equal(t, http.StatusOK, rec2.Code)
	var sub []struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(rec2.Body.Bytes(), &sub))
	require.Len(t, sub, 2, "带 subtree=true 时子节点的密钥也要在")
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListAPIDefault -v
```

Expected: FAIL —— `Len(sub) == 1`，期望 2（`?subtree=true` 还没被识别）

- [ ] **Step 3: 写实现**

`internal/control/keyapi.go`，把 `HandleList` 整个替换为：

```go
// HandleList 列出某节点下的密钥。
//
// 带 ?subtree=true 时改为列出整个子树，供子树批量吊销的预览用——
// 预览与开火用的是同一套节点选择子句（见 KeyStore.ListByOrgSubtree）。
// 权限不必额外判定：中间件已按 key:read 校验过这个节点，而节点上的
// 授予本就覆盖其子树。
func (a *KeyAPI) HandleList(w http.ResponseWriter, r *http.Request) {
	id := r.PathValue("id")

	var list []*APIKey
	var err error
	if r.URL.Query().Get("subtree") == "true" {
		org, gerr := a.orgs.Get(r.Context(), id)
		if gerr != nil {
			writeKeyError(w, gerr, "查询密钥失败")
			return
		}
		list, err = a.keys.ListByOrgSubtree(r.Context(), org.Path)
	} else {
		list, err = a.keys.ListByOrg(r.Context(), id)
	}
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "查询密钥失败")
		return
	}

	out := make([]keyView, 0, len(list))
	for _, k := range list {
		out = append(out, viewOf(k))
	}
	writeJSON(w, http.StatusOK, out)
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListAPI -v
```

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add internal/control/keyapi.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: 密钥列表支持按子树查询"
```

---

## Task 5: `HandleRevoke` 加责任人分支

**Files:**
- Modify: `internal/control/keyapi.go:112-170`
- Test: `internal/control/keyapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyapi_test.go` 末尾追加：

```go
func TestRevokeAPIOwnerCanRevokeOwnKey(t *testing.T) {
	// 密钥泄露时唯一的自助止血路径：轮换救不了场（共存窗口内旧凭据
	// 仍然有效，且 API 无法表达零窗口），去找管理员的延迟又恰恰是
	// 泄露事件中最不该有的。所以责任人本人不需要任何授予就能吊销自己的密钥。
	api, _, _, keys, _, uid := keyAPIFixture(t)
	rec := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"测试","models":[]}`)
	require.Equal(t, http.StatusCreated, rec.Code)

	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &created))

	// 不给任何授予，只带上「我就是责任人」的身份。
	req := httptest.NewRequest(http.MethodDelete, "/api/keys/"+created.ID, nil)
	req.SetPathValue("id", created.ID)
	req = req.WithContext(context.WithValue(req.Context(), userCtxKey,
		&User{ID: uid, Status: UserStatusActive}))
	rec2 := httptest.NewRecorder()
	api.HandleRevoke(rec2, req)

	require.Equal(t, http.StatusNoContent, rec2.Code)
	stored, err := keys.Get(context.Background(), created.ID)
	require.NoError(t, err)
	require.Equal(t, "revoked", stored.Status)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestRevokeAPIOwner -v
```

Expected: FAIL —— 拿到 403 `permission_denied`（当前实现只认 `key:write`）

- [ ] **Step 3: 写实现**

`internal/control/keyapi.go`，把 `HandleRevoke` 里取到用户之后的那段权限判定替换为：

```go
	// 判定与 HandleRotate 对齐：责任人本人，或在该密钥所属节点上
	// 持有 key:write 的管理员。
	//
	// 责任人本人能吊销自己的密钥，是密钥泄露时唯一的自助止血路径：
	// 轮换救不了场（共存窗口内旧凭据仍然有效，且 API 无法表达零窗口）。
	// 代价是责任人手滑能断掉自己在跑的生产——那靠界面的二次确认拦，
	// 而不是靠从他手里收走止血能力。
	if u.ID != k.UserID {
		allowed, err := a.resolver.Can(r.Context(), subjectOf(u), authz.PermKeyWrite, &k.OrgID)
		if err != nil {
			writeError(w, http.StatusInternalServerError, "internal_error", "权限判定失败")
			return
		}
		if !allowed {
			writeError(w, http.StatusForbidden, "permission_denied", "没有吊销该密钥的权限")
			return
		}
	}
```

- [ ] **Step 4: 运行测试确认通过，并确认无关用户仍被拦住**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestRevokeAPI -v
```

Expected: 全部 PASS。特别确认既有的 `TestRevokeAPIWithoutPermissionIs403` 仍然 PASS —— 它的调用者 ID 是 `nobody`，不等于密钥的责任人，因此新分支不会放行它。

- [ ] **Step 5: 提交**

```bash
git add internal/control/keyapi.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: 责任人可以吊销自己的密钥"
```

---

## Task 6: `GET /api/keys/mine`

**Files:**
- Modify: `internal/control/keyapi.go`
- Modify: `internal/control/routes.go:307-313`
- Modify: `internal/control/routes_test.go:255-257`
- Test: `internal/control/keyapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyapi_test.go` 末尾追加：

```go
func TestMineReturnsOnlyCallersKeysAcrossOrgs(t *testing.T) {
	iss, orgs, _, keys, db, uid := issuerFixture(t)
	rbac := newFakeRBACStore()
	rbac.setPath("gw", "/gw")
	api := NewKeyAPI(iss, keys, orgs, authz.NewResolver(rbac))

	other := seedUserID(t, db, "mine-other")
	seedOrg(t, db, "sub", "/gw/sub")
	seedKey(t, db, "k-mine-gw", "h1", "gw", uid, "active")
	seedKey(t, db, "k-mine-sub", "h2", "sub", uid, "active")
	seedKey(t, db, "k-theirs", "h3", "gw", other, "active")

	req := httptest.NewRequest(http.MethodGet, "/api/keys/mine", nil)
	req = req.WithContext(context.WithValue(req.Context(), userCtxKey,
		&User{ID: uid, Status: UserStatusActive}))
	rec := httptest.NewRecorder()
	api.HandleMine(rec, req)

	require.Equal(t, http.StatusOK, rec.Code)
	var got []struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &got))
	ids := []string{}
	for _, k := range got {
		ids = append(ids, k.ID)
	}
	require.ElementsMatch(t, []string{"k-mine-gw", "k-mine-sub"}, ids,
		"跨节点的本人密钥都要在，别人的一把都不能有")
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestMineReturns
```

Expected: 编译失败 —— `api.HandleMine undefined`

- [ ] **Step 3: 写处理器**

在 `internal/control/keyapi.go` 里 `HandleList` **之后**加：

```go
// HandleMine 列出调用者作为责任人的全部密钥，跨节点。
//
// 按调用者本人过滤，因此不需要节点级判定——与 GET /api/requests
// 同一先例。普通开发者既不知道自己的密钥挂在哪个节点，多半也没有
// 那个节点上的 key:read，按节点查的接口对他没用。
func (a *KeyAPI) HandleMine(w http.ResponseWriter, r *http.Request) {
	u, ok := UserFromContext(r.Context())
	if !ok {
		writeError(w, http.StatusInternalServerError, "internal_error", "上下文缺少用户")
		return
	}

	list, err := a.keys.ListByUser(r.Context(), u.ID)
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "查询密钥失败")
		return
	}

	out := make([]keyView, 0, len(list))
	for _, k := range list {
		out = append(out, viewOf(k))
	}
	writeJSON(w, http.StatusOK, out)
}
```

- [ ] **Step 4: 注册路由**

`internal/control/routes.go`，在 `DELETE /api/keys/{id}` 那一条**之后**插入：

```go
		{
			// 只返回「我是责任人」的密钥，按调用者本人过滤，
			// 因此不需要节点级判定——与 GET /api/requests 同一先例。
			Pattern: "GET /api/keys/mine", Access: AccessAuthenticated,
			Handler: keyH(func(k *KeyAPI) http.HandlerFunc { return k.HandleMine }),
		},
```

`internal/control/routes_test.go`，在覆盖清单里 `"DELETE /api/keys/{id}",` 之后加一行：

```go
		"GET /api/keys/mine",
```

- [ ] **Step 5: 运行测试确认通过，并确认路由注册不 panic**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run 'TestMineReturns|TestRoute|TestHandler' -v
```

Expected: 全部 PASS。`TestHandlerDoesNotPanicWithConsoleRoutes` 尤其要看 —— P1.4a 踩过 `ServeMux` 在注册期直接 panic 的坑（`GET /` 与 `/api/` 冲突），新路由必须过这一关。

- [ ] **Step 6: 提交**

```bash
git add internal/control/keyapi.go internal/control/routes.go internal/control/routes_test.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: 我的密钥接口"
```

---

## Task 7: 前端类型与展示辅助

**Files:**
- Modify: `web/src/lib/types.ts`
- Create: `web/src/lib/keyDisplay.ts`, `web/src/lib/keyDisplay.test.ts`

- [ ] **Step 1: 加类型**

`web/src/lib/types.ts`，在文件末尾（`ClaimedKey` 之后）追加。**保留** `ClaimedKey` 不动，Task 9 才会把它换掉：

```ts
/** GET /api/orgs/{id}/keys、GET /api/keys/mine —— snake_case（keyView 有 json tag） */
export type ApiKey = {
  id: string
  key_prefix: string
  org_id: string
  user_id: string
  name: string
  status: string
  models: string[]
  max_budget: number | null
  budget_duration: string | null
  rpm_limit: number | null
  tpm_limit: number | null
  expires_at: string | null
  /** 轮换状态：共存窗口内旧凭据仍可用，到 prev_key_expires_at 为止 */
  rotated_at: string | null
  prev_key_expires_at: string | null
  created_at: string
}

/** 签发、轮换、领取的响应：keyView 加一个只出现这一次的明文 */
export type IssuedKey = ApiKey & { key: string }

/** 两个批量吊销端点的响应 */
export type RevokeResult = { revoked: number }
```

- [ ] **Step 2: 写失败测试**

创建 `web/src/lib/keyDisplay.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { rotationCell } from './keyDisplay'
import type { ApiKey } from './types'

function key(over: Partial<ApiKey>): ApiKey {
  return {
    id: 'k1',
    key_prefix: 'ak-abc',
    org_id: 'gw',
    user_id: 'u1',
    name: '测试',
    status: 'active',
    models: [],
    max_budget: null,
    budget_duration: null,
    rpm_limit: null,
    tpm_limit: null,
    expires_at: null,
    rotated_at: null,
    prev_key_expires_at: null,
    created_at: '2026-09-03T00:00:00Z',
    ...over,
  }
}

describe('rotationCell', () => {
  it('没轮换过就什么都不说', () => {
    expect(rotationCell(key({}))).toBe('—')
  })

  it('共存窗口还没过时，说清旧凭据还能用到什么时候', () => {
    const future = new Date(Date.now() + 3600_000).toISOString()
    expect(rotationCell(key({ rotated_at: future, prev_key_expires_at: future })))
      .toContain('旧凭据')
  })

  it('窗口已过就不再说旧凭据可用——那会是危险的错误信息', () => {
    const past = new Date(Date.now() - 3600_000).toISOString()
    const text = rotationCell(key({ rotated_at: past, prev_key_expires_at: past }))
    expect(text).not.toContain('旧凭据')
    expect(text).toContain('已轮换')
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

```bash
cd web && npm test -- keyDisplay
```

Expected: FAIL —— 找不到模块 `./keyDisplay`

- [ ] **Step 4: 写实现**

创建 `web/src/lib/keyDisplay.ts`：

```ts
import type { ApiKey } from './types'

/** 密钥状态的中文标签与配色。 */
export const KEY_STATUS: Record<string, { text: string; color: string }> = {
  active: { text: '可用', color: 'green' },
  pending: { text: '签发中', color: 'gold' },
  revoked: { text: '已吊销', color: 'red' },
}

/**
 * 轮换状态的一句话。
 *
 * 窗口没过就报旧凭据的到期时间——那是轮换后唯一需要被看见的信息。
 * 窗口已过就绝不能再说「旧凭据可用」：那会让人以为还有替换时间。
 */
export function rotationCell(k: ApiKey): string {
  if (k.prev_key_expires_at && new Date(k.prev_key_expires_at) > new Date()) {
    return `旧凭据 ${new Date(k.prev_key_expires_at).toLocaleString('zh-CN')} 前仍可用`
  }
  if (k.rotated_at) {
    return `已轮换于 ${new Date(k.rotated_at).toLocaleString('zh-CN')}`
  }
  return '—'
}
```

- [ ] **Step 5: 运行测试与类型检查**

```bash
cd web && npm test -- keyDisplay && npm run typecheck
```

Expected: 3 个测试 PASS，类型检查无输出

- [ ] **Step 6: 提交**

```bash
git add web/src/lib/types.ts web/src/lib/keyDisplay.ts web/src/lib/keyDisplay.test.ts
```

```bash
git commit -m "feat: 密钥的前端类型与展示辅助"
```

---

## Task 8: 抽出 `OrgTree` 共享组件

**Files:**
- Create: `web/src/components/OrgTree.tsx`, `web/src/components/OrgTree.test.tsx`
- Modify: `web/src/app/platform/orgs/page.tsx`

- [ ] **Step 1: 写失败测试**

创建 `web/src/components/OrgTree.test.tsx`：

```tsx
import { describe, expect, it } from 'vitest'
import { toTree } from './OrgTree'
import type { Org } from '@/lib/types'

function org(id: string, parent: string | null, name = id, keyHolder = false): Org {
  return {
    ID: id,
    ParentID: parent,
    Name: name,
    Path: parent === null ? `/${id}` : `/${parent}/${id}`,
    ExternalSource: null,
    ExternalID: null,
    IsKeyHolder: keyHolder,
  }
}

describe('toTree', () => {
  it('按 ParentID 组装层级', () => {
    const tree = toTree([org('root', null), org('child', 'root')])
    expect(tree).toHaveLength(1)
    expect(tree[0].key).toBe('root')
    expect(tree[0].children?.[0].key).toBe('child')
  })

  it('给密钥边界节点加标注', () => {
    const tree = toTree([org('gw', null, '网关组', true)])
    expect(tree[0].title).toBe('网关组（密钥边界）')
  })

  it('可见范围不含根节点时，以最浅的那些作为根——否则整棵树会渲染成空', () => {
    // 只能看到 /root/rd 及其下级、看不到 /root 本身：
    // rd 的 ParentID 指向一个不在列表里的 ID。
    const tree = toTree([org('rd', 'root'), org('gw', 'rd')])
    expect(tree).toHaveLength(1)
    expect(tree[0].key).toBe('rd')
    expect(tree[0].children?.[0].key).toBe('gw')
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd web && npm test -- OrgTree
```

Expected: FAIL —— 找不到模块 `./OrgTree`

- [ ] **Step 3: 写组件**

创建 `web/src/components/OrgTree.tsx`：

```tsx
'use client'

import { Tree } from 'antd'
import type { DataNode } from 'antd/es/tree'
import type { Org } from '@/lib/types'

/**
 * 把扁平的组织列表按 ParentID 组装成 antd Tree 要的结构。
 *
 * 单独导出是为了能单测：「可见范围不含根节点」那条回退分支在界面上
 * 很难构造（要一个恰好只授到中层节点的账号），在单测里却是一行的事。
 */
export function toTree(orgs: Org[]): DataNode[] {
  const byParent = new Map<string | null, Org[]>()
  for (const o of orgs) {
    const list = byParent.get(o.ParentID) ?? []
    list.push(o)
    byParent.set(o.ParentID, list)
  }
  const build = (parent: string | null): DataNode[] =>
    (byParent.get(parent) ?? []).map((o) => ({
      key: o.ID,
      title: o.IsKeyHolder ? `${o.Name}（密钥边界）` : o.Name,
      children: build(o.ID),
    }))
  // 可见范围可能不含根节点，此时以列表里最浅的那些作为根。
  const roots = build(null)
  if (roots.length > 0) return roots
  const ids = new Set(orgs.map((o) => o.ID))
  return orgs
    .filter((o) => o.ParentID === null || !ids.has(o.ParentID))
    .map((o) => ({ key: o.ID, title: o.Name, children: build(o.ID) }))
}

/**
 * 组织树。选中项由调用方持有——两个页面对「选中一个节点」的后续动作
 * 完全不同（组织页要增删，密钥页要拉这个节点的密钥）。
 */
export function OrgTree({
  orgs,
  selected,
  onSelect,
}: {
  orgs: Org[]
  selected: string | null
  onSelect: (id: string | null) => void
}) {
  return (
    <Tree
      treeData={toTree(orgs)}
      defaultExpandAll
      selectedKeys={selected ? [selected] : []}
      onSelect={(keys) => onSelect(keys.length > 0 ? String(keys[0]) : null)}
    />
  )
}
```

- [ ] **Step 4: 组织页改用它**

`web/src/app/platform/orgs/page.tsx`：

1. 删掉文件顶部的 `toTree` 函数整段，以及 `import type { DataNode } from 'antd/es/tree'`
2. 从 antd 的 import 里去掉 `Tree`（其余保留），即改为：

```tsx
import { App, Button, Form, Input, Modal, Popconfirm, Space } from 'antd'
```

3. 加一行 import：

```tsx
import { OrgTree } from '@/components/OrgTree'
```

4. 把 JSX 里的 `<Tree ... />` 整段替换为：

```tsx
        <OrgTree orgs={orgs} selected={selected} onSelect={setSelected} />
```

- [ ] **Step 5: 运行测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部测试 PASS（含新增的 3 个 `toTree` 测试），类型检查无输出，构建成功

- [ ] **Step 6: 提交**

```bash
git add web/src/components/OrgTree.tsx web/src/components/OrgTree.test.tsx web/src/app/platform/orgs/page.tsx
```

```bash
git commit -m "refactor: 抽出组织树共享组件并补上单测"
```

---

## Task 9: 抽出 `PlaintextKeyModal`

**Files:**
- Create: `web/src/components/PlaintextKeyModal.tsx`
- Modify: `web/src/app/my/requests/page.tsx`, `web/src/lib/types.ts`

- [ ] **Step 1: 写组件**

创建 `web/src/components/PlaintextKeyModal.tsx`：

```tsx
'use client'

import { Alert, Button, Modal, Typography } from 'antd'

/**
 * 一次性明文弹窗。
 *
 * 签发、轮换、领取都只在响应里给一次明文，关掉就再也拿不到。
 * 这句话必须写在用户眼前，而不是只写在设计文档里。
 */
export function PlaintextKeyModal({
  plaintext,
  title,
  onClose,
}: {
  plaintext: string | null
  title: string
  onClose: () => void
}) {
  return (
    <Modal
      title={title}
      open={plaintext !== null}
      onCancel={onClose}
      footer={<Button onClick={onClose}>我已保存</Button>}
    >
      <Alert
        type="warning"
        showIcon
        message="这串明文只显示这一次"
        description="关闭后无法再次查看。请立刻保存到你的密钥管理器或 CI 变量里。"
        style={{ marginBottom: 12 }}
      />
      <Typography.Paragraph copyable code>
        {plaintext}
      </Typography.Paragraph>
    </Modal>
  )
}
```

- [ ] **Step 2: 我的申请页改用它**

`web/src/app/my/requests/page.tsx`：

1. antd 的 import 去掉 `Alert` 与 `Typography`：

```tsx
import { App, Button, Form, Input, Modal, Space, Table, Tag } from 'antd'
```

2. 加一行 import：

```tsx
import { PlaintextKeyModal } from '@/components/PlaintextKeyModal'
```

3. 类型 import 把 `ClaimedKey` 换成 `IssuedKey`：

```tsx
import type { ApiRequest, IssuedKey, Org } from '@/lib/types'
```

4. state 与 claim 的类型跟着换：

```tsx
  const [claimed, setClaimed] = useState<IssuedKey | null>(null)
```

```tsx
      const key = await apiSend<IssuedKey>('POST', `/api/requests/${id}/claim`)
```

5. 把「密钥已签发」那个 `<Modal>` 整段替换为：

```tsx
      <PlaintextKeyModal
        plaintext={claimed?.key ?? null}
        title="密钥已签发"
        onClose={() => setClaimed(null)}
      />
```

- [ ] **Step 3: 删掉 `ClaimedKey`**

`web/src/lib/types.ts`，删除 `ClaimedKey` 整段（含其上方的注释）。它与 `IssuedKey` 是同一个 wire 形状（`keyView` + `key`），留两份只会漂移。

- [ ] **Step 4: 运行测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部 PASS，类型检查无输出（若还有文件引用 `ClaimedKey`，这一步会明确报出来），构建成功

- [ ] **Step 5: 提交**

```bash
git add web/src/components/PlaintextKeyModal.tsx web/src/app/my/requests/page.tsx web/src/lib/types.ts
```

```bash
git commit -m "refactor: 抽出一次性明文弹窗，合并重复的 wire 类型"
```

---

## Task 10: `KeyRotateModal`

**Files:**
- Create: `web/src/components/KeyRotateModal.tsx`

> 设计文档 §5 的文件清单里没有这个组件——它是按 D6 同一条规则（第二个消费者出现才抽）在执行期加的：虚拟密钥页与我的密钥页都要轮换，且各自带一段完整的请求与错误处理。

- [ ] **Step 1: 写组件**

创建 `web/src/components/KeyRotateModal.tsx`：

```tsx
'use client'

import { useState } from 'react'
import { App, InputNumber, Modal, Typography } from 'antd'
import { apiSend, ApiError } from '@/lib/api'
import type { ApiKey, IssuedKey } from '@/lib/types'

/** 与后端 maxRotationWindow 一致（30 天）。 */
const MAX_WINDOW_HOURS = 30 * 24

/**
 * 轮换弹窗。平台侧（替人处置）与我的密钥（自助）共用。
 *
 * 文案里要说清共存窗口不是止血手段：窗口内旧凭据仍然有效，
 * 泄露了该走吊销。后端的 API 也表达不了零窗口，所以这不是可以
 * 靠「把窗口填 0」绕过去的事。
 */
export function KeyRotateModal({
  target,
  onClose,
  onRotated,
}: {
  target: ApiKey | null
  onClose: () => void
  onRotated: (plaintext: string) => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [hours, setHours] = useState(24)
  const [busy, setBusy] = useState(false)

  const run = async () => {
    if (!target) return
    setBusy(true)
    try {
      const rotated = await apiSend<IssuedKey>('POST', `/api/keys/${target.id}/rotate`, {
        window_seconds: hours * 3600,
      })
      await onRotated(rotated.key)
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('轮换失败')
    } finally {
      setBusy(false)
    }
  }

  return (
    <Modal
      title={target ? `轮换密钥：${target.name}` : '轮换密钥'}
      open={target !== null}
      onCancel={onClose}
      onOk={run}
      confirmLoading={busy}
    >
      <Typography.Paragraph>
        轮换会换发新的客户端凭据。旧凭据在共存窗口内仍然可用，供你替换配置；窗口一过立刻失效。
      </Typography.Paragraph>
      <Typography.Paragraph type="warning">
        如果这把密钥已经泄露，轮换救不了场——窗口内旧凭据仍然有效。那种情况该用吊销。
      </Typography.Paragraph>
      共存窗口（小时）：
      <InputNumber
        min={1}
        max={MAX_WINDOW_HOURS}
        value={hours}
        onChange={(v) => setHours(v ?? 24)}
        style={{ marginLeft: 8 }}
      />
    </Modal>
  )
}
```

- [ ] **Step 2: 类型检查**

```bash
cd web && npm run typecheck
```

Expected: 无输出

- [ ] **Step 3: 提交**

```bash
git add web/src/components/KeyRotateModal.tsx
```

```bash
git commit -m "feat: 轮换弹窗组件"
```

---

## Task 11: 导航加两条

**Files:**
- Modify: `web/src/components/workbenches.ts`

- [ ] **Step 1: 改导航**

`web/src/components/workbenches.ts`，把 `WORKBENCHES` 里 `my-space` 与 `platform` 两项的 `nav` 改为：

```ts
  {
    id: 'my-space',
    label: '我的空间',
    home: '/my/requests',
    nav: [
      { label: '我的申请', href: '/my/requests' },
      { label: '我的密钥', href: '/my/keys' },
    ],
  },
  {
    id: 'platform',
    label: '平台管理',
    home: '/platform/orgs',
    nav: [
      { label: '组织与成员', href: '/platform/orgs' },
      { label: '虚拟密钥', href: '/platform/keys' },
    ],
  },
```

`finops` 一项不动。

- [ ] **Step 2: 运行测试**

```bash
cd web && npm test && npm run typecheck
```

Expected: 全部 PASS（`AppShell.test.tsx` 只断言工作台标签，不受 nav 变化影响）

- [ ] **Step 3: 提交**

```bash
git add web/src/components/workbenches.ts
```

```bash
git commit -m "feat: 导航加入虚拟密钥与我的密钥"
```

---

## Task 12: 虚拟密钥页

**Files:**
- Create: `web/src/app/platform/keys/page.tsx`

- [ ] **Step 1: 建目录**

```bash
mkdir -p web/src/app/platform/keys
```

- [ ] **Step 2: 写页面**

创建 `web/src/app/platform/keys/page.tsx`：

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { App, Button, Form, Input, Modal, Popconfirm, Space, Switch, Table, Tag } from 'antd'
import { AppShell } from '@/components/AppShell'
import { OrgTree } from '@/components/OrgTree'
import { PlaintextKeyModal } from '@/components/PlaintextKeyModal'
import { KeyRotateModal } from '@/components/KeyRotateModal'
import { useSession } from '@/lib/session'
import { apiGet, apiSend, ApiError } from '@/lib/api'
import { KEY_STATUS, rotationCell } from '@/lib/keyDisplay'
import type { ApiKey, IssuedKey, Org } from '@/lib/types'

export default function PlatformKeysPage() {
  const { session, loading } = useSession()
  const { message } = App.useApp()
  const [orgs, setOrgs] = useState<Org[]>([])
  const [selected, setSelected] = useState<string | null>(null)
  const [subtree, setSubtree] = useState(false)
  const [keys, setKeys] = useState<ApiKey[]>([])
  const [issuing, setIssuing] = useState(false)
  const [plaintext, setPlaintext] = useState<string | null>(null)
  const [rotating, setRotating] = useState<ApiKey | null>(null)
  const [form] = Form.useForm<{ user_id: string; name: string; models: string }>()

  useEffect(() => {
    apiGet<Org[]>('/api/orgs')
      .then(setOrgs)
      .catch(() => setOrgs([]))
  }, [])

  const reload = useCallback(async () => {
    if (!selected) {
      setKeys([])
      return
    }
    try {
      // subtree=true 时服务端用的是与子树吊销逐字相同的节点选择子句，
      // 因此这份列表同时也是那个操作的影响范围。
      const q = subtree ? '?subtree=true' : ''
      setKeys(await apiGet<ApiKey[]>(`/api/orgs/${selected}/keys${q}`))
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      setKeys([])
    }
  }, [selected, subtree, message])

  useEffect(() => {
    void reload()
  }, [reload])

  const issue = async () => {
    const v = await form.validateFields()
    try {
      // models 必须显式传：后端把「没传」当作放行全部模型，
      // 这个 fail-open 只能在调用方堵住（与 P1.4a 的申请页同一处理）。
      const models = v.models
        .split(',')
        .map((s) => s.trim())
        .filter(Boolean)
      const issued = await apiSend<IssuedKey>('POST', '/api/keys', {
        org_id: selected,
        user_id: v.user_id,
        name: v.name,
        models,
      })
      setIssuing(false)
      form.resetFields()
      setPlaintext(issued.key)
      await reload()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
    }
  }

  const revoke = async (id: string) => {
    try {
      await apiSend('DELETE', `/api/keys/${id}`)
      message.success('已吊销')
      await reload()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
    }
  }

  if (loading || !session) return null

  return (
    <AppShell workbenches={session.workbenches}>
      <div style={{ display: 'flex', gap: 24, alignItems: 'flex-start' }}>
        <div style={{ width: 260, flexShrink: 0 }}>
          <OrgTree orgs={orgs} selected={selected} onSelect={setSelected} />
        </div>

        <div style={{ flex: 1, minWidth: 0 }}>
          <Space direction="vertical" style={{ width: '100%' }} size="middle">
            <Space>
              <Button type="primary" disabled={!selected} onClick={() => setIssuing(true)}>
                签发密钥
              </Button>
              <span>
                包含子树{' '}
                <Switch checked={subtree} onChange={setSubtree} disabled={!selected} />
              </span>
            </Space>

            <Table<ApiKey>
              rowKey="id"
              dataSource={keys}
              pagination={false}
              locale={{ emptyText: selected ? '该范围下没有密钥' : '请先在左侧选择一个节点' }}
              columns={[
                { title: '名称', dataIndex: 'name' },
                { title: '前缀', dataIndex: 'key_prefix' },
                { title: '责任人', dataIndex: 'user_id' },
                { title: '所属节点', dataIndex: 'org_id' },
                {
                  title: '状态',
                  dataIndex: 'status',
                  render: (s: string) => {
                    const meta = KEY_STATUS[s] ?? { text: s, color: 'default' }
                    return <Tag color={meta.color}>{meta.text}</Tag>
                  },
                },
                {
                  title: '模型',
                  dataIndex: 'models',
                  render: (m: string[]) => (m.length > 0 ? m.join(', ') : '—'),
                },
                { title: '轮换状态', render: (_, k) => rotationCell(k) },
                {
                  title: '操作',
                  render: (_, k) =>
                    k.status === 'active' ? (
                      <Space>
                        <Button type="link" onClick={() => setRotating(k)}>
                          轮换
                        </Button>
                        <Popconfirm
                          title="确认吊销该密钥？"
                          description="吊销不可逆，使用它的客户端会立刻失效。"
                          onConfirm={() => revoke(k.id)}
                        >
                          <Button type="link" danger>
                            吊销
                          </Button>
                        </Popconfirm>
                      </Space>
                    ) : null,
                },
              ]}
            />
          </Space>
        </div>
      </div>

      <Modal title="签发密钥" open={issuing} onCancel={() => setIssuing(false)} onOk={issue}>
        <Form form={form} layout="vertical">
          <Form.Item
            name="user_id"
            label="责任人用户 ID"
            rules={[{ required: true, message: '请填写责任人用户 ID' }]}
          >
            <Input />
          </Form.Item>
          <Form.Item
            name="name"
            label="密钥名称"
            rules={[{ required: true, message: '请输入密钥名称' }]}
          >
            <Input />
          </Form.Item>
          <Form.Item
            name="models"
            label="可用模型（逗号分隔）"
            rules={[{ required: true, message: '必须显式指定模型' }]}
          >
            <Input placeholder="qwen-plus" />
          </Form.Item>
        </Form>
      </Modal>

      <KeyRotateModal
        target={rotating}
        onClose={() => setRotating(null)}
        onRotated={async (plain) => {
          setRotating(null)
          setPlaintext(plain)
          await reload()
        }}
      />

      <PlaintextKeyModal
        plaintext={plaintext}
        title="密钥明文"
        onClose={() => setPlaintext(null)}
      />
    </AppShell>
  )
}
```

- [ ] **Step 3: 类型检查与构建**

```bash
cd web && npm run typecheck && npm run build
```

Expected: 均成功，构建产物里出现 `/platform/keys`

- [ ] **Step 4: 提交**

```bash
git add web/src/app/platform/keys
```

```bash
git commit -m "feat: 虚拟密钥页"
```

---

## Task 13: 子树批量吊销（预览与确认）

**Files:**
- Create: `web/src/components/SubtreeRevokeModal.tsx`
- Modify: `web/src/app/platform/keys/page.tsx`

- [ ] **Step 1: 写组件**

创建 `web/src/components/SubtreeRevokeModal.tsx`：

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { App, Modal, Table, Tag, Typography } from 'antd'
import { apiGet, apiSend, ApiError } from '@/lib/api'
import { KEY_STATUS } from '@/lib/keyDisplay'
import type { ApiKey, Org, RevokeResult } from '@/lib/types'

/** 会被子树吊销打到的状态。与后端 RevokeByOrgSubtree 的 WHERE 条件对齐。 */
const WILL_REVOKE = ['active', 'pending']

/**
 * 子树批量吊销的预览与确认。
 *
 * 打开时用 ?subtree=true 拉列表——服务端那条查询与吊销用的是逐字相同的
 * 节点选择子句，所以这里列出的就是即将被打到的那一批。
 *
 * 预览是快照，不是锁：确认之间行仍可能变（别的管理员刚签发了一把、
 * 审批 worker 刚落一把）。因此成功提示用响应里的实际 revoked 条数，
 * 而不是回显这里算出的 N。
 */
export function SubtreeRevokeModal({
  org,
  onClose,
  onDone,
}: {
  org: Org | null
  onClose: () => void
  onDone: () => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [rows, setRows] = useState<ApiKey[]>([])
  const [busy, setBusy] = useState(false)

  const load = useCallback(async () => {
    if (!org) return
    try {
      setRows(await apiGet<ApiKey[]>(`/api/orgs/${org.ID}/keys?subtree=true`))
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      setRows([])
    }
  }, [org, message])

  useEffect(() => {
    void load()
  }, [load])

  const willRevoke = rows.filter((k) => WILL_REVOKE.includes(k.status))

  const run = async () => {
    if (!org) return
    setBusy(true)
    try {
      const res = await apiSend<RevokeResult>('POST', `/api/orgs/${org.ID}/keys/revoke`)
      message.success(`已吊销 ${res.revoked} 把密钥`)
      await onDone()
      onClose()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('批量吊销失败')
    } finally {
      setBusy(false)
    }
  }

  return (
    <Modal
      title={org ? `吊销「${org.Name}」子树下的全部密钥` : '批量吊销'}
      open={org !== null}
      onCancel={onClose}
      onOk={run}
      okText="确认吊销"
      okButtonProps={{ danger: true, disabled: willRevoke.length === 0 }}
      confirmLoading={busy}
      width={720}
    >
      <Typography.Paragraph>
        将吊销其中 <strong>{willRevoke.length}</strong> 把（可用与签发中的）。不可逆，
        使用它们的客户端会立刻失效。
      </Typography.Paragraph>
      <Typography.Paragraph type="secondary">
        下面是这一刻的快照。确认之间若有新密钥签发，实际条数可能不同——以吊销后的提示为准。
      </Typography.Paragraph>
      <Table<ApiKey>
        rowKey="id"
        dataSource={rows}
        size="small"
        pagination={false}
        scroll={{ y: 280 }}
        locale={{ emptyText: '该子树下没有密钥' }}
        columns={[
          { title: '名称', dataIndex: 'name' },
          { title: '前缀', dataIndex: 'key_prefix' },
          { title: '所属节点', dataIndex: 'org_id' },
          {
            title: '状态',
            dataIndex: 'status',
            render: (s: string) => {
              const meta = KEY_STATUS[s] ?? { text: s, color: 'default' }
              return <Tag color={meta.color}>{meta.text}</Tag>
            },
          },
        ]}
      />
    </Modal>
  )
}
```

- [ ] **Step 2: 接进虚拟密钥页**

`web/src/app/platform/keys/page.tsx`：

1. 加 import：

```tsx
import { SubtreeRevokeModal } from '@/components/SubtreeRevokeModal'
```

2. 加一个 state（放在 `rotating` 那行之后）：

```tsx
  const [revokingSubtree, setRevokingSubtree] = useState(false)
```

3. 在工具条的 `<Space>` 里、`包含子树` 那个 `<span>` **之后**加一个按钮：

```tsx
              <Popconfirm
                title="先看一眼影响范围"
                description="下一步会列出这个子树下的全部密钥。"
                onConfirm={() => setRevokingSubtree(true)}
              >
                <Button danger disabled={!selected}>
                  吊销该子树的全部密钥
                </Button>
              </Popconfirm>
```

4. 在 `<KeyRotateModal ... />` **之前**加：

```tsx
      <SubtreeRevokeModal
        org={revokingSubtree ? (orgs.find((o) => o.ID === selected) ?? null) : null}
        onClose={() => setRevokingSubtree(false)}
        onDone={reload}
      />
```

- [ ] **Step 3: 类型检查与构建**

```bash
cd web && npm run typecheck && npm run build
```

Expected: 均成功

- [ ] **Step 4: 提交**

```bash
git add web/src/components/SubtreeRevokeModal.tsx web/src/app/platform/keys/page.tsx
```

```bash
git commit -m "feat: 子树批量吊销的预览与确认"
```

---

## Task 14: 危险操作区（全局紧急吊销）

**Files:**
- Create: `web/src/components/DangerZone.tsx`, `web/src/components/DangerZone.test.tsx`
- Modify: `web/src/app/platform/keys/page.tsx`

- [ ] **Step 1: 写失败测试**

创建 `web/src/components/DangerZone.test.tsx`：

```tsx
import { describe, expect, it, vi } from 'vitest'
import { fireEvent, render, screen } from '@testing-library/react'
import { App } from 'antd'
import { DangerZone } from './DangerZone'

function renderZone(canRevokeAll: boolean) {
  return render(
    <App>
      <DangerZone canRevokeAll={canRevokeAll} onDone={vi.fn()} />
    </App>,
  )
}

describe('DangerZone', () => {
  it('没有全局 key:revoke_all 时整个区块不出现', () => {
    renderZone(false)
    expect(screen.queryByText('危险操作')).not.toBeInTheDocument()
  })

  it('确认串没有原样打出来时按钮保持禁用——前端绝不代填这个串', () => {
    renderZone(true)
    expect(screen.getByRole('button', { name: /紧急吊销全系统密钥/ })).toBeDisabled()

    fireEvent.change(screen.getByLabelText('确认串'), { target: { value: 'revoke all keys' } })
    expect(screen.getByRole('button', { name: /紧急吊销全系统密钥/ })).toBeDisabled()
  })

  it('原样打出确认串后按钮才可用', () => {
    renderZone(true)
    fireEvent.change(screen.getByLabelText('确认串'), { target: { value: 'REVOKE ALL KEYS' } })
    expect(screen.getByRole('button', { name: /紧急吊销全系统密钥/ })).toBeEnabled()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd web && npm test -- DangerZone
```

Expected: FAIL —— 找不到模块 `./DangerZone`

- [ ] **Step 3: 写组件**

创建 `web/src/components/DangerZone.tsx`：

```tsx
'use client'

import { useState } from 'react'
import { App, Button, Card, Input, Space, Typography } from 'antd'
import { apiSend, ApiError } from '@/lib/api'
import type { RevokeResult } from '@/lib/types'

/** 后端 HandleRevokeAll 要求原样带上的确认串。前端**不代填**。 */
const CONFIRMATION = 'REVOKE ALL KEYS'

/**
 * 全局紧急吊销。break glass，不可逆。
 *
 * 两道闸与后端对齐：全局 key:revoke_all 管住「谁能按」，确认串管住
 * 「不会手滑按到」。确认串必须由用户手打——前端若从常量里自动填上，
 * 就等于把后端刻意加的第二道闸拆了。
 *
 * 隐藏这个区块只是界面便利，不是授权：真正的闸在路由中间件上
 * （Permission: key:revoke_all, Target: 全局）。这里用
 * global_permissions 判定是准确的——key:revoke_all 是 ScopeGlobal
 * 权限，只可能全局持有，不存在会被漏掉的节点级授予。
 */
export function DangerZone({
  canRevokeAll,
  onDone,
}: {
  canRevokeAll: boolean
  onDone: () => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [typed, setTyped] = useState('')
  const [busy, setBusy] = useState(false)

  if (!canRevokeAll) return null

  const run = async () => {
    setBusy(true)
    try {
      const res = await apiSend<RevokeResult>('POST', '/api/keys/revoke-all', {
        confirm: CONFIRMATION,
      })
      message.success(`已紧急吊销 ${res.revoked} 把密钥`)
      setTyped('')
      await onDone()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('全局吊销失败')
    } finally {
      setBusy(false)
    }
  }

  return (
    <Card
      title="危险操作"
      style={{ borderColor: '#ffa39e', marginTop: 24 }}
      styles={{ header: { color: '#cf1322' } }}
    >
      <Space direction="vertical" style={{ width: '100%' }}>
        <Typography.Text>
          紧急吊销<strong>全系统</strong>密钥。不可逆，全公司所有客户端会立刻失效。
          确认请原样输入 <Typography.Text code>{CONFIRMATION}</Typography.Text>。
        </Typography.Text>
        <Input
          aria-label="确认串"
          value={typed}
          onChange={(e) => setTyped(e.target.value)}
          placeholder={CONFIRMATION}
        />
        <Button
          danger
          type="primary"
          disabled={typed !== CONFIRMATION}
          loading={busy}
          onClick={run}
        >
          紧急吊销全系统密钥
        </Button>
      </Space>
    </Card>
  )
}
```

- [ ] **Step 4: 接进虚拟密钥页**

`web/src/app/platform/keys/page.tsx`：

1. 加 import：

```tsx
import { DangerZone } from '@/components/DangerZone'
```

2. 在右侧那个 `<Space direction="vertical">` 的 `</Space>` **之后**、`</div>` 之前加：

```tsx
            <DangerZone
              canRevokeAll={session.global_permissions.includes('key:revoke_all')}
              onDone={reload}
            />
```

- [ ] **Step 5: 运行测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部 PASS（含 3 个 `DangerZone` 测试），构建成功

- [ ] **Step 6: 提交**

```bash
git add web/src/components/DangerZone.tsx web/src/components/DangerZone.test.tsx web/src/app/platform/keys/page.tsx
```

```bash
git commit -m "feat: 全局紧急吊销的危险操作区"
```

---

## Task 15: 我的密钥页

**Files:**
- Create: `web/src/app/my/keys/page.tsx`

- [ ] **Step 1: 建目录**

```bash
mkdir -p web/src/app/my/keys
```

- [ ] **Step 2: 写页面**

创建 `web/src/app/my/keys/page.tsx`：

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { App, Button, Popconfirm, Space, Table, Tag } from 'antd'
import { AppShell } from '@/components/AppShell'
import { PlaintextKeyModal } from '@/components/PlaintextKeyModal'
import { KeyRotateModal } from '@/components/KeyRotateModal'
import { useSession } from '@/lib/session'
import { apiGet, apiSend, ApiError } from '@/lib/api'
import { KEY_STATUS, rotationCell } from '@/lib/keyDisplay'
import type { ApiKey, Org } from '@/lib/types'

export default function MyKeysPage() {
  const { session, loading } = useSession()
  const { message } = App.useApp()
  const [keys, setKeys] = useState<ApiKey[]>([])
  const [orgNames, setOrgNames] = useState<Record<string, string>>({})
  const [rotating, setRotating] = useState<ApiKey | null>(null)
  const [plaintext, setPlaintext] = useState<string | null>(null)

  const reload = useCallback(async () => {
    try {
      setKeys(await apiGet<ApiKey[]>('/api/keys/mine'))
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
    }
  }, [message])

  useEffect(() => {
    void reload()
    // 节点名只是显示用。普通开发者不一定看得到所有节点，
    // 取不到就退回显示原始 ID（与 P1.4a 同样的降级方式）。
    apiGet<Org[]>('/api/orgs')
      .then((list) => setOrgNames(Object.fromEntries(list.map((o) => [o.ID, o.Name]))))
      .catch(() => setOrgNames({}))
  }, [reload])

  const revoke = async (id: string) => {
    try {
      await apiSend('DELETE', `/api/keys/${id}`)
      message.success('已吊销')
      await reload()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
    }
  }

  if (loading || !session) return null

  return (
    <AppShell workbenches={session.workbenches}>
      <Table<ApiKey>
        rowKey="id"
        dataSource={keys}
        pagination={false}
        locale={{ emptyText: '你名下还没有密钥' }}
        columns={[
          { title: '名称', dataIndex: 'name' },
          { title: '前缀', dataIndex: 'key_prefix' },
          {
            title: '所属节点',
            dataIndex: 'org_id',
            render: (id: string) => orgNames[id] ?? id,
          },
          {
            title: '状态',
            dataIndex: 'status',
            render: (s: string) => {
              const meta = KEY_STATUS[s] ?? { text: s, color: 'default' }
              return <Tag color={meta.color}>{meta.text}</Tag>
            },
          },
          {
            title: '模型',
            dataIndex: 'models',
            render: (m: string[]) => (m.length > 0 ? m.join(', ') : '—'),
          },
          { title: '轮换状态', render: (_, k) => rotationCell(k) },
          {
            title: '操作',
            render: (_, k) =>
              k.status === 'active' ? (
                <Space>
                  <Button type="link" onClick={() => setRotating(k)}>
                    轮换
                  </Button>
                  <Popconfirm
                    title="确认吊销？"
                    description="吊销不可逆，使用这把密钥的客户端会立刻失效。密钥泄露时这是正确的止血手段。"
                    onConfirm={() => revoke(k.id)}
                  >
                    <Button type="link" danger>
                      吊销
                    </Button>
                  </Popconfirm>
                </Space>
              ) : null,
          },
        ]}
      />

      <KeyRotateModal
        target={rotating}
        onClose={() => setRotating(null)}
        onRotated={async (plain) => {
          setRotating(null)
          setPlaintext(plain)
          await reload()
        }}
      />

      <PlaintextKeyModal
        plaintext={plaintext}
        title="密钥已轮换"
        onClose={() => setPlaintext(null)}
      />
    </AppShell>
  )
}
```

- [ ] **Step 3: 全量测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部 PASS，构建产物里出现 `/my/keys` 与 `/platform/keys`

- [ ] **Step 4: 提交**

```bash
git add web/src/app/my/keys
```

```bash
git commit -m "feat: 我的密钥页"
```

---

## Task 16: 端到端验收

**Files:**
- Create: `docs/superpowers/findings/2026-09-03-p1.4b-acceptance.md`

对着真实运行的 Postgres + Casdoor + control 跑完设计文档 §7 的 10 项验收标准，**每一项都记录真实输出，不留占位符**。

> **操作纪律一**（P1.3b 踩过）：手工 E2E 与自动化测试**不能共用同一个数据库**。一旦启动 control 开始手工验收，就不要再跑 `go test` / `make test`，直到全部手工步骤记录完毕。
>
> **操作纪律二**（本期特有，设计文档 §7 已写明）：**验收项 6（全局紧急吊销）必须排在最后**。它会吊销全系统密钥，包括前几期留在开发库里的夹具；先做它，后面所有项的夹具就都没了。

- [ ] **Step 1: 起栈并构建**

```bash
docker ps --format '{{.Names}}\t{{.Status}}'
```

Expected: `deploy-postgres-1`、`deploy-casdoor-1` 在跑（不在则 `docker compose --env-file .env -f deploy/docker-compose.yml up -d postgres casdoor`）

```bash
make console && make build
```

Expected: `web/dist/index.html` 存在，`bin/airlock` 构建成功

- [ ] **Step 2: 启动 control**

```bash
env POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" CONTROL_LISTEN_ADDR=:8081 OIDC_ISSUER=http://localhost:8000 OIDC_CLIENT_ID=airlock OIDC_CLIENT_SECRET=airlock-dev-secret OIDC_REDIRECT_URL=http://localhost:8081/auth/callback AIRLOCK_BOOTSTRAP_ADMIN=admin@example.com AIRLOCK_ENCRYPTION_KEY=YWlybG9jay1kZXYtb25seS0zMmJ5dGUta2V5ISEhISE= LITELLM_BASE_URL=http://localhost:4000 LITELLM_MASTER_KEY=sk-airlock-master-dev-only nohup ./bin/airlock control > /tmp/airlock-p14b.log 2>&1 &
```

```bash
sleep 3 && tail -5 /tmp/airlock-p14b.log
```

Expected: `Control 启动`

- [ ] **Step 3: 造夹具**

沿用 P1.4a 的手法：`python3` 算 sha256 会话哈希写进 `sessions` 表，`role_grants` 直接授予。**会话哈希必须由脚本生成，不要手抄**——64 位十六进制串肉眼核对不可靠。

把下面这段存成 `/tmp/p14b_fixtures.py` 并运行（它会写出 SQL 并打印 token 与哈希的对应关系）：

```python
import hashlib

tokens = {
    'u-p14b-admin': 'p14b-admin-token-aaa111',
    'u-p14b-orgadmin': 'p14b-orgadmin-token-ccc333',
    'u-p14b-dev': 'p14b-dev-token-bbb222',
}

sql = """-- 组织树：p14b-rd 与 p14b-rd2 是同前缀兄弟节点，两者都是密钥边界
INSERT INTO organizations (id, parent_id, name, path, is_key_holder) VALUES
  ('p14b-root', NULL, 'P14b根', '/p14b-root', false),
  ('p14b-rd', 'p14b-root', 'P14b-RD', '/p14b-root/p14b-rd', true),
  ('p14b-rd2', 'p14b-root', 'P14b-RD2（同前缀兄弟节点）', '/p14b-root/p14b-rd2', true)
ON CONFLICT (id) DO NOTHING;

INSERT INTO users (id, external_id, email, display_name, status, primary_org_id) VALUES
  ('u-p14b-admin', 'ext-p14b-admin', 'admin@p14b.test', 'P14b管理员', 'active', 'p14b-root'),
  ('u-p14b-orgadmin', 'ext-p14b-orgadmin', 'orgadmin@p14b.test', 'P14b子树管理员', 'active', 'p14b-root'),
  ('u-p14b-dev', 'ext-p14b-dev', 'dev@p14b.test', 'P14b开发者', 'active', 'p14b-rd')
ON CONFLICT (id) DO NOTHING;

-- admin 是全局 platform_admin（唯一持有 key:revoke_all 的人）。
-- orgadmin 只在 p14b-root 上持 org_admin：他能正常使用平台管理与虚拟密钥页
-- （有 org:write / key:write / key:read），但**没有** key:revoke_all
-- ——验收项 7 要的正是这种「合法但不该看到危险区」的账号。
-- dev 故意一条授予都没有（验收项 9 要靠这一点）。
INSERT INTO role_grants (id, user_id, role_id, org_id) VALUES
  ('rg-p14b-admin', 'u-p14b-admin', 'platform_admin', NULL),
  ('rg-p14b-orgadmin', 'u-p14b-orgadmin', 'org_admin', 'p14b-root')
ON CONFLICT DO NOTHING;

INSERT INTO sessions (id, user_id, expires_at) VALUES
"""
lines = []
for uid, tok in tokens.items():
    h = hashlib.sha256(tok.encode()).hexdigest()
    assert len(h) == 64
    lines.append("  ('%s', '%s', now() + interval '4 hours')" % (h, uid))
sql += ",\n".join(lines) + "\nON CONFLICT (id) DO NOTHING;\n"

with open('/tmp/p14b_fixtures.sql', 'w') as f:
    f.write(sql)

for uid, tok in tokens.items():
    print(uid, '->', tok)
```

```bash
python3 /tmp/p14b_fixtures.py && docker exec -i deploy-postgres-1 psql -U airlock -d airlock < /tmp/p14b_fixtures.sql
```

Expected: 四条 `INSERT` 均成功

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT user_id, length(id)=64 AS hash_ok FROM sessions WHERE user_id LIKE 'u-p14b-%';"
```

Expected: 两行，`hash_ok` 都是 `t`

后续步骤里的 `<admin token>` = `p14b-admin-token-aaa111`，`<orgadmin token>` = `p14b-orgadmin-token-ccc333`，`<dev token>` = `p14b-dev-token-bbb222`。

顺手确认三个账号的权限面确实不同（这决定了后面几项验收是否有意义）：

```bash
for t in p14b-admin-token-aaa111 p14b-orgadmin-token-ccc333 p14b-dev-token-bbb222; do curl -sS http://localhost:8081/api/whoami -H "Cookie: airlock_session=$t" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d["user"]["ID"], "| workbenches =", d["workbenches"], "| revoke_all =", "key:revoke_all" in d["global_permissions"])'; done
```

Expected: admin 的 `revoke_all = True`；orgadmin 的 `workbenches` 含 `platform` 但 `revoke_all = False`；dev 只有 `my-space`、`revoke_all = False`

- [ ] **Step 4: 验收项 1 —— 节点与子树列表**

在 `p14b-rd` 与 `p14b-rd2` 下各签一把密钥，然后：

```bash
curl -sS "http://localhost:8081/api/orgs/p14b-rd/keys" -H "Cookie: airlock_session=<admin token>" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))'
```

Expected: `1`（只有本节点）

```bash
curl -sS "http://localhost:8081/api/orgs/p14b-root/keys?subtree=true" -H "Cookie: airlock_session=<admin token>" | python3 -c 'import json,sys; print(len(json.load(sys.stdin)))'
```

Expected: `2`（子树两个节点的都在）。**在虚拟密钥页上用「包含子树」开关复现同一结果并记录截图或 DOM 文本。**

- [ ] **Step 5: 验收项 2 —— 直接签发的一次性明文**

在虚拟密钥页点「签发密钥」，填责任人 ID、名称、模型 `qwen-plus`。

Expected: 弹窗显示 `ak-` 开头的明文；关闭并刷新后**不再出现**。记录明文前缀与刷新后的列表。

- [ ] **Step 6: 验收项 3 —— 轮换**

对上一步签发的密钥点「轮换」，共存窗口填 1 小时。

Expected: 拿到**新**明文；`id` 与轮换前**相同**、`key_prefix` **不同**；列表的「轮换状态」列显示「旧凭据 … 前仍可用」。三项都记录。

- [ ] **Step 7: 验收项 4 —— 单把吊销**

对某把密钥点「吊销」并确认。

```bash
curl -sS "http://localhost:8081/api/orgs/p14b-rd/keys" -H "Cookie: airlock_session=<admin token>" | python3 -m json.tool
```

Expected: 该密钥 `status` 为 `revoked`

- [ ] **Step 8: 验收项 5 —— 子树吊销的预览与实际命中一致（第五次陷阱）**

先确保 `p14b-rd` 与 `p14b-rd2` 下各有至少一把 `active` 密钥。在虚拟密钥页选中 `p14b-rd`，点「吊销该子树的全部密钥」。

Expected（**逐条记录**）：
1. 预览弹窗列出的**只有** `p14b-rd` 的密钥，`p14b-rd2` 的**一把都不在**
2. 「将吊销其中 N 把」的 N 等于预览里 `active`+`pending` 的条数
3. 确认后提示的实际吊销条数与 N 一致
4. 查库确认 `p14b-rd2` 的密钥仍是 `active`：

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT org_id, status FROM api_keys WHERE org_id IN ('p14b-rd','p14b-rd2') ORDER BY org_id;"
```

- [ ] **Step 9: 验收项 7 —— 无 `key:revoke_all` 的账号看不到危险区，且直接打接口 403**

用 `u-p14b-orgadmin` 的登录态访问虚拟密钥页。**这个账号是故意挑的**：他在 `p14b-root` 上持 `org_admin`，能正常看树、看密钥、签发、吊销——是个完全合法的平台管理用户，只是没有全局 `key:revoke_all`。用「一条授予都没有的 dev」测这一条没有意义（那种账号连平台管理工作台都进不去，看不到危险区证明不了什么）。

Expected: 页面正常可用（能看到组织树与密钥列表），但**没有**「危险操作」区块。

随后用同一个账号直接打接口，且**带上正确的确认串**：

```bash
curl -sS -X POST http://localhost:8081/api/keys/revoke-all -H "Cookie: airlock_session=<orgadmin token>" -H "Content-Type: application/json" -d '{"confirm":"REVOKE ALL KEYS"}' -w "\nHTTP=%{http_code}\n"
```

Expected: `403`。**这一条证明隐藏入口不是授权——绕开界面、连确认串都带对了，仍然过不去。**

- [ ] **Step 10: 验收项 8 —— 我的密钥只见本人**

给 `u-p14b-dev` 在 `p14b-rd` 与 `p14b-rd2` 下各签一把密钥（用 admin 身份签，责任人填 dev）。

```bash
curl -sS http://localhost:8081/api/keys/mine -H "Cookie: airlock_session=<dev token>" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(len(d), sorted(k["org_id"] for k in d))'
```

Expected: `2 ['p14b-rd', 'p14b-rd2']`（跨节点都在）

```bash
curl -sS http://localhost:8081/api/keys/mine -H "Cookie: airlock_session=<admin token>" | python3 -c 'import json,sys; d=json.load(sys.stdin); print([k["user_id"] for k in d])'
```

Expected: 只有 admin 自己的 `user_id`，**没有** dev 的密钥。在「我的密钥」页上复现并记录。

- [ ] **Step 11: 验收项 9 —— 责任人自助吊销**

以 `u-p14b-dev`（**无任何授予**）在「我的密钥」页吊销自己的一把密钥。

Expected: 成功，状态变 `revoked`。随后用 dev 的会话去吊销 **admin 名下**的一把密钥：

```bash
curl -sS -X DELETE http://localhost:8081/api/keys/<admin 的某把密钥 id> -H "Cookie: airlock_session=<dev token>" -w "\nHTTP=%{http_code}\n"
```

Expected: `403 permission_denied`。**两条都要记录——它们一起证明 owner 分支放开的恰好只有「自己的」。**

- [ ] **Step 12: 验收项 6 —— 全局紧急吊销（必须最后做）**

先确认前面所有项都已记录完毕。以 admin 身份在危险区：

1. 输入 `revoke all keys`（小写）

Expected: 按钮**保持禁用**

2. 直接打接口带错的确认串：

```bash
curl -sS -X POST http://localhost:8081/api/keys/revoke-all -H "Cookie: airlock_session=<admin token>" -H "Content-Type: application/json" -d '{"confirm":"nope"}' -w "\nHTTP=%{http_code}\n"
```

Expected: `400 confirm_required`

3. 在页面上原样输入 `REVOKE ALL KEYS` 并点按钮

Expected: 成功，提示里的条数与查库一致：

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT status, count(*) FROM api_keys GROUP BY status;"
```

Expected: 没有任何 `active` 或 `pending` 行

- [ ] **Step 13: 收尾**

```bash
pkill -f "bin/airlock control"
```

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" make test
```

Expected: Go 全部 `ok`，前端 Vitest 全部通过

```bash
make lint && gofmt -l . && git status --short
```

Expected: 全部通过，工作区干净（**不要** `docker compose down`）

- [ ] **Step 14: 验收项 10 —— Edge 零回归**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- PASS'
```

Expected: `33`

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- FAIL'
```

Expected: `0`

- [ ] **Step 15: 写验收记录**

创建 `docs/superpowers/findings/2026-09-03-p1.4b-acceptance.md`，结构照 `2026-09-02-p1.4a-acceptance.md`：表头、10 项结果表、每项的真实输出、过程中发现的问题及处置。**每个单元格填真实数据，不留占位符。**

- [ ] **Step 16: 提交**

```bash
git add docs/superpowers/findings/2026-09-03-p1.4b-acceptance.md
```

```bash
git commit -m "docs: P1.4b 端到端验收记录"
```

---

## 完成标准

- [ ] `make test` 全绿（Go `-race` + 前端 Vitest）
- [ ] `make lint` 通过（含前端 `tsc --noEmit`），`gofmt -l .` 无输出
- [ ] 路由机械检查覆盖 `GET /api/keys/mine`；`Handler()` 不 panic 的测试仍在位
- [ ] 验收记录 10 项全部通过，且无占位符
- [ ] **子树吊销的预览命中与实际命中一致，同前缀兄弟节点毫发无伤**（第五次踩这个陷阱的证据）
- [ ] **无 `key:revoke_all` 的账号即使带对确认串也拿到 403**（隐藏入口不是授权的证据）
- [ ] **责任人能吊销自己的密钥、不能吊销别人的**（D2 放开的范围恰好是「自己的」）
- [ ] 签发与轮换的 `ak-` 明文都只显示一次，刷新后不再出现
- [ ] `internal/edge` 33 PASS / 0 FAIL
- [ ] 所有代码已提交，工作区干净

---

## 自查记录

对照设计文档的覆盖情况：

| 设计文档章节 | 对应任务 |
|---|---|
| §1 做什么（虚拟密钥页、我的密钥页、四处后端、抽共享树） | Task 1–15 |
| §1 范围外（额度编辑、用量、模型池、角色页、组织页补齐） | 全程无对应代码 |
| §2 C1（单节点 vs 子树） | Task 1、4 |
| §2 C2/C3（无自助止血路径） | Task 5 |
| §2 C4（无按用户查询、`keyView` 缺轮换字段） | Task 2、3、6 |
| §3 D1 预览与开火同一套子句 | Task 1（逐字相同的 SQL）、Task 13（预览弹窗与 TOCTOU 说明） |
| §3 D2 责任人自助吊销 | Task 5、Task 16 Step 11 |
| §3 D3 `keyView` 轮换字段 | Task 3、Task 7（`rotationCell`） |
| §3 D4 与两道闸对齐 | Task 13（预览）、Task 14（手打确认串） |
| §3 D5 危险区用 `global_permissions` 判定 | Task 14（含注释说明为何与 P1.4a D2 不矛盾） |
| §3 D6 抽两个组件、不抽表格 | Task 8、9（另按同一规则加了 Task 10 的轮换弹窗与 Task 13 的预览弹窗） |
| §4 B1–B4 四处后端 | Task 1、2、3、4、5、6 |
| §5 前端结构与页面 | Task 7–15 |
| §6 测试策略 | 各任务的测试步骤 |
| §7 验收标准 10 项 + 操作纪律 | Task 16 |

**规模**：16 个任务，与 P1.4a（15 个）相当。

**计划撰写期间查证、并写进计划的两件事**：

1. **`keyAPIFixture` 不返回 `db`**，而子树与跨节点的测试需要往真实 `organizations` 表插子节点。因此 Task 4、6 的测试直接用 `issuerFixture` 自己组装 `KeyAPI`，而不是改动 `keyAPIFixture` 的签名（它的注释明写着「一次定死签名」，改它要动所有既有调用点）。
2. **既有的 `TestRevokeAPIWithoutPermissionIs403` 不会被 Task 5 弄假**——它的调用者 ID 是 `nobody`，不等于密钥的责任人，新的 owner 分支不会放行它。Task 5 Step 4 明确要求确认这一条仍然 PASS。

**设计文档未覆盖、在此定死的事项**：

3. **`ClaimedKey` 与 `IssuedKey` 合并**。P1.4a 的 `ClaimedKey` 与本期要加的「签发/轮换响应」是同一个 wire 形状（`keyView` + `key`），留两份只会漂移。Task 9 顺手合掉——那一步本来就在改我的申请页。
4. **签发表单里的责任人填「用户 ID」而不是选择器**。今天没有「列出可选用户」的接口（`GET /api/orgs/{id}/grants` 只给授予、不给节点成员名单），做选择器要先补后端，超出本期范围。与 P1.4a 申请页填节点 ID 是同一种降级，记为后续候选项。
