# P1.3c 密钥轮换与批量/紧急吊销 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让密钥可以按需轮换（旧凭据在共存窗口内继续可用、窗口一过自动失效），并支持按组织子树与全局紧急两种批量吊销，同时把所有吊销路径的上游封禁变成可自愈的最终一致。

**Architecture:** 轮换用「一行两哈希」——行内加 `prev_key_hash` + `prev_key_expires_at`，一次 UPDATE 完成，零上游调用；Edge 的查询把到期判断写进 SQL，因此正确性不依赖任何后台任务。批量吊销只做本地 UPDATE 就返回，上游封禁交给周期扫描（上游没有批量封禁接口，探针 P3 已证）。

**Tech Stack:** Go 1.27、Postgres（goose 迁移）、LiteLLM `/key/block`、testify。

**设计文档：** `docs/superpowers/specs/2026-09-02-airlock-p1.3c-rotation-revocation-design.md`

---

## 文件结构

**新建**

| 文件 | 职责 |
|---|---|
| `migrations/20260902000001_key_rotation.sql` | 五列 + 形状约束 + 两个索引 |

**修改**

| 文件 | 改动 |
|---|---|
| `internal/authz/permission.go` | 新增 `key:revoke_all`（ScopeGlobal） |
| `internal/authz/permission_test.go` | 权限总数 13 → 14 |
| `internal/control/model.go` | `APIKey` 加五个字段；`KeyStore` 加七个方法；两个哨兵错误 |
| `internal/control/keystore.go` | 上述七个方法的 Postgres 实现 |
| `internal/control/keyissue.go` | `Rotate`、`BlockPendingUpstream`；`Revoke` 成功后盖戳 |
| `internal/control/reconciler.go` | `RevokeByUsers` 封禁成功后盖戳 |
| `internal/control/keyapi.go` | `HandleRotate`、`HandleRevokeOrg`、`HandleRevokeAll`；`KeyAPI` 加 `orgs` |
| `internal/control/routes.go` / `server.go` / `routes_test.go` | 三条新路由 |
| `internal/apikey/store.go` | `Key` 加 `ViaPrevKey`、`PrevKeyExpiresAt` |
| `internal/apikey/postgres.go` | `ByHash` 兼容 `prev_key_hash`，到期判断写在 SQL 里 |
| `internal/edge/auth.go` | 命中旧凭据时打一条结构化日志 |
| `internal/config/config.go` | `KEY_MAINTENANCE_INTERVAL` |
| `internal/app/control.go` | 维护循环接线；`NewKeyAPI` 多传一个 `orgs` |

## 全局签名约定（先定死，后续任务一律照此使用）

P1.3a 的教训是签名中途改动导致十几处调用点返工，因此这里一次定完：

```go
// internal/control/model.go —— APIKey 新增字段
PrevKeyHash           *string
PrevKeyExpiresAt      *time.Time
RotatedAt             *time.Time
UpstreamBlockedAt     *time.Time
UpstreamBlockAttempts int

// internal/control/model.go —— KeyStore 新增方法
Rotate(ctx context.Context, id, newHash, newPrefix string, prevExpiresAt time.Time) error
RetireExpiredPrevKeys(ctx context.Context, now time.Time) (int64, error)
RevokeByOrgSubtree(ctx context.Context, orgPath string) (int64, error)
RevokeAll(ctx context.Context) (int64, error)
ListRevokedUnblocked(ctx context.Context, maxAttempts, limit int) ([]*APIKey, error)
MarkUpstreamBlocked(ctx context.Context, id string) error
RecordBlockAttempt(ctx context.Context, id string) error

// internal/control/keyissue.go
func (i *KeyIssuer) Rotate(ctx context.Context, id string, window time.Duration) (string, *APIKey, error)
func (i *KeyIssuer) BlockPendingUpstream(ctx context.Context) (int, error)

// internal/control/keyapi.go —— 构造函数多一个 orgs 参数
func NewKeyAPI(issuer *KeyIssuer, keys KeyStore, orgs OrgStore, resolver *authz.Resolver) *KeyAPI

// internal/apikey/store.go —— Key 新增字段
ViaPrevKey       bool
PrevKeyExpiresAt *time.Time
```

哨兵错误与常量：

```go
// internal/control/model.go
ErrKeyNotActive  = errors.New("密钥不处于可用状态")
ErrWindowTooLong = errors.New("共存窗口超过上限")

// internal/control/keyissue.go
const (
	defaultRotationWindow = 24 * time.Hour
	maxRotationWindow     = 30 * 24 * time.Hour
	maxBlockAttempts      = 5
	blockScanBatchSize    = 100
)
```

行数统计一律用 `int64`（与既有的 `RevokeByUsers` 一致），只有「处理了几条」的业务计数用 `int`。

## 三处必须先知道的既有事实

1. **`NewPostgresKeyStore` 返回的是接口 `KeyStore`，不是具体类型**（`keystore.go:16`）。给 `KeyStore` 加方法就必须**同一个任务里**同步补实现，否则整个包编译不过。因此任务按「一组方法 + 它们的测试」切分——Task 3 加轮换的两个、Task 6 加批量吊销的两个、Task 7 加封禁记账的三个——而不是先把七个接口方法一次加完再逐个实现。这样每个任务边界上包都是可编译、可测试的。

2. **`api_keys.key_hash` 有 UNIQUE 约束**。轮换是把新哈希写进 `key_hash`、旧值挪进 `prev_key_hash`，两列各自唯一，不会互相冲突。

3. **`internal/apikey` 的测试 `seed()` 会无条件 `DELETE FROM api_keys`**（`postgres_test.go:30`），并已按 P1.3b 的教训先清 `notifications`/`requests`。本阶段不改这套，但新增的 Edge 测试要沿用同一个 `seed()`。

---

## Task 1: 新增 key:revoke_all 权限

**Files:**
- Modify: `internal/authz/permission.go`
- Test: `internal/authz/permission_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/authz/permission_test.go` 末尾追加：

```go
func TestKeyRevokeAllIsGlobalScoped(t *testing.T) {
	// 紧急全局吊销不绑定任何节点，必须是全局权限——
	// 若做成 ScopeOrg，在任意一个叶子节点上拿到它就等于能清空全公司。
	def, ok := Lookup(PermKeyRevokeAll)
	require.True(t, ok, "%s 未注册", PermKeyRevokeAll)
	require.Equal(t, ScopeGlobal, def.Scope)
}

func TestKeyRevokeAllIsNotInOrgAdmin(t *testing.T) {
	// 组织管理员管的是自己那棵子树，不该顺带获得清空全系统的能力。
	require.NotContains(t, roleByID(t, RoleOrgAdmin).Permissions, PermKeyRevokeAll)
}
```

第二条测试要用 `roleByID`，它定义在 `internal/authz/role_test.go:9`，同包可直接调用。

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./internal/authz/ -run "TestKeyRevokeAll" -count=1
```

Expected: FAIL —— `undefined: PermKeyRevokeAll`

- [ ] **Step 3: 写最小实现**

`internal/authz/permission.go` 的常量块里，`PermKeyRequest` 之后加一行：

```go
	PermKeyRequest   = "key:request"
	PermKeyRevokeAll = "key:revoke_all"
```

`registry` 里 `PermKeyRequest` 那行之后加：

```go
	PermKeyRevokeAll: {Key: PermKeyRevokeAll, Scope: ScopeGlobal, Desc: "紧急吊销全系统密钥"},
```

**不要**把它加进 `org_admin`：这是 D7 的核心，它只应由 `platform_admin` 经 `allPermissionKeys()` 自动获得。

- [ ] **Step 4: 修既有的总数断言**

`internal/authz/permission_test.go` 的 `TestAllIsSortedAndComplete`：

```go
	require.Len(t, all, 14, "权限总数与设计文档 §4 的清单一致（P1.3c 新增 key:revoke_all）")
```

- [ ] **Step 5: 运行全量测试**

```bash
go test ./internal/authz/ -count=1
```

Expected: ok

> 注意：本阶段**不会**出现 P1.3b 那次 `CanGrant` 子集不变式的连带失败。那次是因为给**隐式基线角色 developer** 加了权限，而 `org_admin` 必须是 developer 的超集才能授予它。`key:revoke_all` 只进 `platform_admin`，不进任何被别人授予的角色，因此不触及那条不变式。若 `TestCanGrantAllowsSubsetOfOwnPermissions` 仍然失败，说明有人误把它加进了 `org_admin` 或 `developer`。

- [ ] **Step 6: 提交**

```bash
git add internal/authz/
```

```bash
git commit -m "feat: 新增全局权限 key:revoke_all"
```

---

## Task 2: 迁移——五列、形状约束与两个索引

**Files:**
- Create: `migrations/20260902000001_key_rotation.sql`
- Test: `internal/control/keystore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加（该文件已存在，沿用其中既有的 `issuerFixture`）：

```go
func TestPrevShapeCheckRejectsHalfFilledPair(t *testing.T) {
	// 这条 CHECK 是共存窗口的唯一保障。少了它，半填的一对会退化成
	// 两种糟糕状态之一：expires_at 为空 = 旧凭据永久有效（安全事故），
	// 或者只有到期时间的孤儿行。
	_, _, _, _, db, uid := issuerFixture(t)

	_, err := db.Exec(`
		INSERT INTO api_keys (id, key_hash, key_prefix, org_id, user_id,
		                      upstream_key_enc, status, models, prev_key_hash)
		VALUES ('k-half','h-half','ak-x','gw',$1,'enc','active','[]'::jsonb,'oldhash')`, uid)
	require.Error(t, err, "只填 prev_key_hash、不填到期时间必须被拒")
	require.Contains(t, err.Error(), "api_keys_prev_shape_check")
}

func TestPrevShapeCheckRejectsOrphanExpiry(t *testing.T) {
	_, _, _, _, db, uid := issuerFixture(t)

	_, err := db.Exec(`
		INSERT INTO api_keys (id, key_hash, key_prefix, org_id, user_id,
		                      upstream_key_enc, status, models, prev_key_expires_at)
		VALUES ('k-orphan','h-orphan','ak-x','gw',$1,'enc','active','[]'::jsonb, now())`, uid)
	require.Error(t, err, "只填到期时间、不填 prev_key_hash 必须被拒")
	require.Contains(t, err.Error(), "api_keys_prev_shape_check")
}

func TestPrevKeyHashIsUnique(t *testing.T) {
	// Edge 要按 prev_key_hash 查行，两行声称同一个哈希会让查询返回多行。
	_, _, _, _, db, uid := issuerFixture(t)

	for _, id := range []string{"k-a", "k-b"} {
		_, err := db.Exec(`
			INSERT INTO api_keys (id, key_hash, key_prefix, org_id, user_id,
			                      upstream_key_enc, status, models,
			                      prev_key_hash, prev_key_expires_at)
			VALUES ($1,$2,'ak-x','gw',$3,'enc','active','[]'::jsonb,
			        'shared-prev-hash', now() + interval '1 hour')`,
			id, "h-"+id, uid)
		if id == "k-a" {
			require.NoError(t, err)
			continue
		}
		require.Error(t, err, "第二行用同一个 prev_key_hash 必须被唯一索引拒绝")
	}
}

func TestUpstreamBlockAttemptsDefaultsToZero(t *testing.T) {
	_, _, _, _, db, uid := issuerFixture(t)

	_, err := db.Exec(`
		INSERT INTO api_keys (id, key_hash, key_prefix, org_id, user_id,
		                      upstream_key_enc, status, models)
		VALUES ('k-def','h-def','ak-x','gw',$1,'enc','active','[]'::jsonb)`, uid)
	require.NoError(t, err)

	var attempts int
	var blockedAt *time.Time
	require.NoError(t, db.QueryRow(
		`SELECT upstream_block_attempts, upstream_blocked_at FROM api_keys WHERE id='k-def'`).
		Scan(&attempts, &blockedAt))
	require.Zero(t, attempts)
	require.Nil(t, blockedAt, "新签发的密钥还没被吊销，谈不上封禁")
}
```

若 `internal/control/keystore_test.go` 的 import 块尚无 `"time"`，补上。

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestPrevShapeCheck|TestPrevKeyHashIsUnique|TestUpstreamBlockAttempts" -count=1
```

Expected: FAIL —— `column "prev_key_hash" of relation "api_keys" does not exist`

- [ ] **Step 3: 写迁移**

创建 `migrations/20260902000001_key_rotation.sql`：

```sql
-- +goose Up
-- 轮换用「一行两哈希」：换发客户端凭据时，新哈希写进 key_hash，
-- 旧哈希挪到 prev_key_hash 并带一个到期时间。
--
-- 为什么不是两行：两行共享同一把上游密钥的话，退休旧行时的 /key/block
-- 会连带封死新凭据；两行各持一把上游密钥的话，预算会在窗口期裂成两个桶、
-- 有效配额近乎翻倍——那直接破坏了「密钥与配额映射」这件事本身。
-- 一行两哈希同时保住：一个预算桶、不动吊销逻辑、密钥 ID 跨轮换稳定。
ALTER TABLE api_keys
    ADD COLUMN prev_key_hash           TEXT,
    ADD COLUMN prev_key_expires_at     TIMESTAMPTZ,
    ADD COLUMN rotated_at              TIMESTAMPTZ,
    ADD COLUMN upstream_blocked_at     TIMESTAMPTZ,
    ADD COLUMN upstream_block_attempts INTEGER NOT NULL DEFAULT 0;

-- 两列必须同生同死。少了这条约束，半填的一对会退化成两种糟糕状态之一：
-- expires_at 为空 = 旧凭据永久有效（安全事故），或孤儿到期时间。
ALTER TABLE api_keys ADD CONSTRAINT api_keys_prev_shape_check CHECK (
    (prev_key_hash IS NULL AND prev_key_expires_at IS NULL)
 OR (prev_key_hash IS NOT NULL AND prev_key_expires_at IS NOT NULL)
);

-- Edge 要按 prev_key_hash 查行，两行绝不能声称同一个哈希。
CREATE UNIQUE INDEX api_keys_prev_key_hash_idx
    ON api_keys(prev_key_hash) WHERE prev_key_hash IS NOT NULL;

-- 上游封禁兜底扫描的驱动索引。
CREATE INDEX api_keys_pending_block_idx
    ON api_keys(status) WHERE upstream_blocked_at IS NULL;

-- +goose Down
DROP INDEX api_keys_pending_block_idx;
DROP INDEX api_keys_prev_key_hash_idx;
ALTER TABLE api_keys DROP CONSTRAINT api_keys_prev_shape_check;
ALTER TABLE api_keys
    DROP COLUMN prev_key_hash,
    DROP COLUMN prev_key_expires_at,
    DROP COLUMN rotated_at,
    DROP COLUMN upstream_blocked_at,
    DROP COLUMN upstream_block_attempts;
```

> **既有行的处理**：迁移前已 `revoked` 的密钥，`upstream_blocked_at` 为 NULL，会被 Task 8 的扫描捞起来重新封禁一遍。这是期望行为——它们当初可能正是封禁失败的那些。探针 P2 已证重复封禁幂等，P1 已证打不存在的 key 返回 404 被当作成功，两种情况都能安全收敛。

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestPrevShapeCheck|TestPrevKeyHashIsUnique|TestUpstreamBlockAttempts" -count=1 -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 5: 运行全量测试**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ ./internal/apikey/ -count=1
```

Expected: 两个包都 ok（加列不影响既有的 `SELECT` 明确列名的查询）

- [ ] **Step 6: 提交**

```bash
git add migrations/20260902000001_key_rotation.sql internal/control/keystore_test.go
```

```bash
git commit -m "feat: api_keys 增加轮换与上游封禁记账的五列"
```

---

## Task 3: KeyStore.Rotate 与 RetireExpiredPrevKeys

**Files:**
- Modify: `internal/control/model.go`, `internal/control/keystore.go`
- Test: `internal/control/keystore_test.go`

`NewPostgresKeyStore` 返回的是接口 `KeyStore`，所以每个任务都必须**同时**加接口方法与实现，包才编译得过。本任务只加轮换相关的两个，批量吊销与封禁记账分别在 Task 6、7。

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加：

```go
// seedKey 造一把密钥，供本文件的存储层测试使用。
func seedKey(t *testing.T, db *sql.DB, id, hash, orgID, uid, status string) {
	t.Helper()
	_, err := db.Exec(`
		INSERT INTO api_keys (id, key_hash, key_prefix, org_id, user_id,
		                      upstream_key_enc, status, models)
		VALUES ($1,$2,'ak-xxxxxxxxx',$3,$4,'enc',$5,'[]'::jsonb)`,
		id, hash, orgID, uid, status)
	require.NoError(t, err)
}

func TestKeyStoreRotateMovesHashToPrev(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedKey(t, db, "k1", "oldhash", "gw", uid, "active")
	exp := time.Now().Add(2 * time.Hour)

	require.NoError(t, keys.Rotate(ctx, "k1", "newhash", "ak-newpref", exp))

	got, err := keys.Get(ctx, "k1")
	require.NoError(t, err)
	require.Equal(t, "newhash", got.KeyHash)
	require.Equal(t, "ak-newpref", got.KeyPrefix)
	require.Equal(t, "oldhash", *got.PrevKeyHash, "旧哈希必须挪进 prev")
	require.NotNil(t, got.PrevKeyExpiresAt)
	require.NotNil(t, got.RotatedAt)
}

func TestKeyStoreRotateOnlyFromActive(t *testing.T) {
	// 已吊销的密钥不能被轮换活过来。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedKey(t, db, "k1", "oldhash", "gw", uid, "revoked")

	err := keys.Rotate(ctx, "k1", "newhash", "ak-newpref", time.Now().Add(time.Hour))
	require.ErrorIs(t, err, ErrKeyNotActive)
}

func TestKeyStoreRotateUnknownKey(t *testing.T) {
	_, _, _, keys, _, _ := issuerFixture(t)

	err := keys.Rotate(context.Background(), "nope", "h", "p", time.Now().Add(time.Hour))
	require.ErrorIs(t, err, ErrAPIKeyNotFound, "不存在与状态不对必须能分辨")
}

func TestKeyStoreRetireExpiredPrevKeys(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	now := time.Now()

	seedKey(t, db, "k-expired", "h1", "gw", uid, "active")
	seedKey(t, db, "k-live", "h2", "gw", uid, "active")
	require.NoError(t, keys.Rotate(ctx, "k-expired", "h1b", "ak-p1", now.Add(-time.Hour)))
	require.NoError(t, keys.Rotate(ctx, "k-live", "h2b", "ak-p2", now.Add(time.Hour)))

	n, err := keys.RetireExpiredPrevKeys(ctx, now)
	require.NoError(t, err)
	require.Equal(t, int64(1), n)

	expired, err := keys.Get(ctx, "k-expired")
	require.NoError(t, err)
	require.Nil(t, expired.PrevKeyHash, "过期的应被置空")
	require.Nil(t, expired.PrevKeyExpiresAt)

	live, err := keys.Get(ctx, "k-live")
	require.NoError(t, err)
	require.NotNil(t, live.PrevKeyHash, "没到期的不该动")
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestKeyStoreRotate|TestKeyStoreRetire" -count=1
```

Expected: FAIL —— `keys.Rotate undefined (type KeyStore has no field or method Rotate)`

- [ ] **Step 3: 扩展模型**

`internal/control/model.go` 的 `APIKey` 结构体，把 `ExpiresAt` 与 `CreatedAt` 之间改成：

```go
	ExpiresAt *time.Time
	// 轮换：共存窗口内旧凭据仍然可用。两列同生同死，
	// 由 api_keys_prev_shape_check 强制。
	PrevKeyHash      *string
	PrevKeyExpiresAt *time.Time
	RotatedAt        *time.Time
	// 上游封禁的记账：吊销本地立即生效，上游封禁由扫描兜底（Task 7、8）。
	UpstreamBlockedAt     *time.Time
	UpstreamBlockAttempts int
	CreatedAt             time.Time
```

`KeyStore` 接口在 `ListStalePending` 之后加两个方法：

```go
	// Rotate 换发客户端凭据：新哈希写进 key_hash，旧哈希挪到 prev_key_hash。
	// 只在 active 时生效，否则返回 ErrKeyNotActive。
	Rotate(ctx context.Context, id, newHash, newPrefix string, prevExpiresAt time.Time) error
	// RetireExpiredPrevKeys 把过期的 prev_* 两列置空，返回处理行数。纯卫生——
	// 到期判断在 Edge 的 SQL 里，正确性不依赖这个方法跑过。
	RetireExpiredPrevKeys(ctx context.Context, now time.Time) (int64, error)
```

哨兵错误块里加两条：

```go
	ErrKeyNotActive  = errors.New("密钥不处于可用状态")
	ErrWindowTooLong = errors.New("共存窗口超过上限")
```

- [ ] **Step 4: 写实现**

`internal/control/keystore.go` 的 `keyColumns` 常量改为（**顺序必须与 `scanKey` 逐一对应**）：

```go
const keyColumns = `id, key_hash, key_prefix, org_id, user_id, name,
	upstream_key_enc, status, models, max_budget, budget_duration,
	rpm_limit, tpm_limit, expires_at,
	prev_key_hash, prev_key_expires_at, rotated_at,
	upstream_blocked_at, upstream_block_attempts, created_at`
```

`scanKey` 的 `row.Scan(...)` 相应改为：

```go
	if err := row.Scan(&k.ID, &k.KeyHash, &k.KeyPrefix, &k.OrgID, &k.UserID, &k.Name,
		&k.UpstreamKeyEnc, &k.Status, &models, &k.MaxBudget, &k.BudgetDuration,
		&k.RPMLimit, &k.TPMLimit, &k.ExpiresAt,
		&k.PrevKeyHash, &k.PrevKeyExpiresAt, &k.RotatedAt,
		&k.UpstreamBlockedAt, &k.UpstreamBlockAttempts, &k.CreatedAt); err != nil {
		return nil, err
	}
```

`internal/control/keystore.go` 末尾追加：

```go
// Rotate 换发客户端凭据。
//
// WHERE status = 'active' 是乐观并发守卫，与 P1.3b 的 Decide/MarkExecuted
// 同一套：已吊销或仍 pending 的密钥不能轮换。
//
// 行里只有一个 prev_key_hash 的位置，因此窗口内再次轮换会让上一次轮出来的
// 成为新的 prev、最初那把当场失效——「最多只保留一代宽限」。这是刻意的：
// 会连着轮两次通常意味着第一次轮出来的也泄漏了。
func (s *postgresKeyStore) Rotate(
	ctx context.Context, id, newHash, newPrefix string, prevExpiresAt time.Time,
) error {
	res, err := s.db.ExecContext(ctx, `
		UPDATE api_keys SET
			key_hash = $1, key_prefix = $2,
			prev_key_hash = key_hash, prev_key_expires_at = $3,
			rotated_at = $4
		WHERE id = $5 AND status = 'active'`,
		newHash, newPrefix, prevExpiresAt.UTC(), time.Now().UTC(), id)
	if err != nil {
		return fmt.Errorf("轮换密钥失败: %w", err)
	}
	n, err := res.RowsAffected()
	if err != nil {
		return err
	}
	if n == 0 {
		// 分辨「不存在」与「状态不对」，让调用方能返回正确的状态码。
		if _, gerr := s.Get(ctx, id); gerr != nil {
			return gerr
		}
		return ErrKeyNotActive
	}
	return nil
}

func (s *postgresKeyStore) RetireExpiredPrevKeys(
	ctx context.Context, now time.Time,
) (int64, error) {
	res, err := s.db.ExecContext(ctx, `
		UPDATE api_keys SET prev_key_hash = NULL, prev_key_expires_at = NULL
		WHERE prev_key_hash IS NOT NULL AND prev_key_expires_at <= $1`, now.UTC())
	if err != nil {
		return 0, fmt.Errorf("清理过期旧凭据失败: %w", err)
	}
	return res.RowsAffected()
}
```

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestKeyStoreRotate|TestKeyStoreRetire" -count=1 -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 6: 运行全量测试**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -count=1
```

Expected: ok

- [ ] **Step 7: 提交**

```bash
git add internal/control/model.go internal/control/keystore.go internal/control/keystore_test.go
```

```bash
git commit -m "feat: KeyStore.Rotate 与过期旧凭据清理"
```

---

## Task 4: KeyIssuer.Rotate

**Files:**
- Modify: `internal/control/keyissue.go`
- Test: `internal/control/keyissue_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyissue_test.go` 末尾追加：

```go
func TestRotateReturnsNewPlaintextAndKeepsUpstreamKey(t *testing.T) {
	// D1/D2 的核心：轮换只换客户端凭据，上游密钥原封不动——
	// 预算桶因此连续，不会在窗口期裂成两份。
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	oldPlain, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "轮换测试", Models: []string{},
	})
	require.NoError(t, err)
	before, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	admin.resetCalls()

	newPlain, rotated, err := iss.Rotate(ctx, k.ID, time.Hour)
	require.NoError(t, err)

	require.NotEqual(t, oldPlain, newPlain, "必须换出一把新的客户端凭据")
	require.True(t, strings.HasPrefix(newPlain, "ak-"))
	require.Equal(t, k.ID, rotated.ID, "密钥身份跨轮换稳定")
	require.Equal(t, before.UpstreamKeyEnc, rotated.UpstreamKeyEnc,
		"上游密钥必须原封不动——预算桶靠它保持连续")
	require.Empty(t, admin.callsSnapshot(), "轮换不调用上游")
}

func TestRotateStoresOnlyHashOfNewPlaintext(t *testing.T) {
	iss, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)

	newPlain, _, err := iss.Rotate(ctx, k.ID, time.Hour)
	require.NoError(t, err)

	var stored string
	require.NoError(t, db.QueryRow(
		`SELECT key_hash FROM api_keys WHERE id=$1`, k.ID).Scan(&stored))
	require.NotEqual(t, newPlain, stored, "库里绝不能出现明文")
	require.Equal(t, apikey.Hash(newPlain), stored)

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.NotNil(t, got.PrevKeyHash)
}

func TestRotateRejectsRevokedKey(t *testing.T) {
	iss, _, _, _, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	require.NoError(t, iss.Revoke(ctx, k.ID))

	_, _, err = iss.Rotate(ctx, k.ID, time.Hour)
	require.ErrorIs(t, err, ErrKeyNotActive)
}

func TestRotateDefaultsAndCapsWindow(t *testing.T) {
	iss, _, _, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)

	// 0 表示用默认值。
	_, _, err = iss.Rotate(ctx, k.ID, 0)
	require.NoError(t, err)
	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.WithinDuration(t, time.Now().Add(defaultRotationWindow),
		*got.PrevKeyExpiresAt, time.Minute)

	// 超过上限直接拒绝——否则填个 10 年就把轮换变成了摆设。
	_, _, err = iss.Rotate(ctx, k.ID, maxRotationWindow+time.Hour)
	require.ErrorIs(t, err, ErrWindowTooLong)
}

func TestRotateTwiceKeepsOnlyOneGenerationOfGrace(t *testing.T) {
	// 行里只有一个 prev 位置：窗口内二次轮换会让最初那把当场失效。
	// 这是刻意的——连轮两次通常意味着第一次轮出来的也泄漏了。
	iss, _, _, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	firstPlain, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)

	secondPlain, _, err := iss.Rotate(ctx, k.ID, time.Hour)
	require.NoError(t, err)
	_, _, err = iss.Rotate(ctx, k.ID, time.Hour)
	require.NoError(t, err)

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.Equal(t, apikey.Hash(secondPlain), *got.PrevKeyHash,
		"prev 位置上应是上一次轮出来的那把")
	require.NotEqual(t, apikey.Hash(firstPlain), *got.PrevKeyHash,
		"最初那把必须当场失效")
}
```

该文件 import 补 `"strings"` 与 `"github.com/softmatrix/airlock/internal/apikey"`（若尚未引入）。

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run TestRotate -count=1
```

Expected: FAIL —— `iss.Rotate undefined`

- [ ] **Step 3: 写实现**

`internal/control/keyissue.go` 的 `upstreamKeyRandomBytes` 常量之后加：

```go
const (
	// defaultRotationWindow 是共存窗口的默认时长。
	defaultRotationWindow = 24 * time.Hour
	// maxRotationWindow 是上限。不设上限的话，填个 10 年就等于永不轮换。
	maxRotationWindow = 30 * 24 * time.Hour
)
```

文件末尾追加：

```go
// Rotate 换发客户端凭据，返回新的 ak- 明文（仅此一次）与轮换后的记录。
//
// 不调用上游：ak- 到 sk- 的映射完全由控制面持有，换掉客户端那一端
// 根本不需要惊动 LiteLLM。因此轮换不可能因为上游宕机而失败——这是
// P1.3a「本地记录优先」推到尽头的形态。
//
// 上游密钥保持不变是刻意的（设计文档 D2）：它加密存放、从不离开控制面，
// 而 ak- 散落在客户端的 .env、CI secrets 与几十份配置里，需要被限定
// 泄漏后可用时长的是后者。保持同一把上游密钥也让预算桶保持连续。
func (i *KeyIssuer) Rotate(
	ctx context.Context, id string, window time.Duration,
) (string, *APIKey, error) {
	if window == 0 {
		window = defaultRotationWindow
	}
	if window < 0 || window > maxRotationWindow {
		return "", nil, ErrWindowTooLong
	}

	plaintext, hash, prefix, err := apikey.Generate()
	if err != nil {
		return "", nil, err
	}
	if err := i.deps.Keys.Rotate(ctx, id, hash, prefix, time.Now().Add(window)); err != nil {
		return "", nil, err
	}

	k, err := i.deps.Keys.Get(ctx, id)
	if err != nil {
		return "", nil, err
	}
	return plaintext, k, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run TestRotate -count=1 -v
```

Expected: 5 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
git add internal/control/keyissue.go internal/control/keyissue_test.go
```

```bash
git commit -m "feat: KeyIssuer.Rotate，零上游调用的客户端凭据轮换"
```

---

## Task 5: Edge 识别共存窗口内的旧凭据

**Files:**
- Modify: `internal/apikey/store.go`, `internal/apikey/postgres.go`, `internal/edge/auth.go`
- Test: `internal/apikey/postgres_test.go`

这是设计文档 D5 落地的地方：**到期判断写在 SQL 里**，因此窗口一过旧凭据当场失效，不依赖任何 worker 跑过。

- [ ] **Step 1: 写失败测试**

在 `internal/apikey/postgres_test.go` 末尾追加：

```go
// seedRotated 在 seed() 造好的那把密钥上模拟一次轮换。
func seedRotated(t *testing.T, db *sql.DB, newHash, prevHash string, prevExpires time.Time) {
	t.Helper()
	_, err := db.Exec(`
		UPDATE api_keys SET key_hash = $1, prev_key_hash = $2, prev_key_expires_at = $3
		WHERE id = 'k1'`, newHash, prevHash, prevExpires)
	require.NoError(t, err)
}

func TestPostgresStoreAcceptsPrevKeyInsideWindow(t *testing.T) {
	db := testDB(t)
	c := testCipher(t)
	seed(t, db, Hash("ak-original"), StatusActive, nil)
	seedRotated(t, db, Hash("ak-new"), Hash("ak-original"), time.Now().Add(time.Hour))
	s := NewPostgresStore(db, c)

	k, err := s.ByHash(context.Background(), Hash("ak-original"))
	require.NoError(t, err, "窗口内旧凭据必须仍然可用")
	require.True(t, k.ViaPrevKey, "要能看出这次是靠旧凭据进来的")
	require.NotNil(t, k.PrevKeyExpiresAt)
	require.Equal(t, "sk-upstream-secret", k.UpstreamKey)
}

func TestPostgresStoreAcceptsNewKeyAfterRotation(t *testing.T) {
	db := testDB(t)
	c := testCipher(t)
	seed(t, db, Hash("ak-original"), StatusActive, nil)
	seedRotated(t, db, Hash("ak-new"), Hash("ak-original"), time.Now().Add(time.Hour))
	s := NewPostgresStore(db, c)

	k, err := s.ByHash(context.Background(), Hash("ak-new"))
	require.NoError(t, err)
	require.False(t, k.ViaPrevKey, "新凭据不是走 prev 路径")
}

func TestPostgresStoreRejectsPrevKeyAfterWindow(t *testing.T) {
	// 设计文档 D5：到期判断在 SQL 里，因此窗口一过旧凭据当场失效——
	// 这条测试全程不运行任何 worker，正是要证明正确性不依赖后台任务。
	db := testDB(t)
	c := testCipher(t)
	seed(t, db, Hash("ak-original"), StatusActive, nil)
	seedRotated(t, db, Hash("ak-new"), Hash("ak-original"), time.Now().Add(-time.Minute))
	s := NewPostgresStore(db, c)

	_, err := s.ByHash(context.Background(), Hash("ak-original"))
	require.ErrorIs(t, err, ErrKeyNotFound, "过期的旧凭据必须查不到")

	// 新凭据不受影响。
	_, err = s.ByHash(context.Background(), Hash("ak-new"))
	require.NoError(t, err)
}

func TestPostgresStoreUnrotatedKeyHasNoPrevMarkers(t *testing.T) {
	db := testDB(t)
	c := testCipher(t)
	seed(t, db, Hash("ak-plain"), StatusActive, nil)
	s := NewPostgresStore(db, c)

	k, err := s.ByHash(context.Background(), Hash("ak-plain"))
	require.NoError(t, err)
	require.False(t, k.ViaPrevKey)
	require.Nil(t, k.PrevKeyExpiresAt)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/apikey/ -run TestPostgresStore -count=1
```

Expected: FAIL —— `k.ViaPrevKey undefined`

- [ ] **Step 3: 写实现**

`internal/apikey/store.go` 的 `Key` 结构体，在 `ExpiresAt` 之后加两个字段：

```go
	ExpiresAt *time.Time
	// ViaPrevKey 表示这次是用轮换前的旧凭据进来的。供可观测性使用：
	// 客户端若到窗口结束都没换上新凭据，到期瞬间会集体 401。
	ViaPrevKey bool
	// PrevKeyExpiresAt 是旧凭据的停用时刻，仅在 ViaPrevKey 为真时有意义。
	PrevKeyExpiresAt *time.Time
```

`internal/apikey/postgres.go` 的 `ByHash` 整体替换为：

```go
// ByHash 按凭据哈希查密钥，兼容轮换共存窗口内的旧凭据。
//
// 到期判断写在 SQL 里而不是 Go 里：窗口一过，旧凭据在那一刻自己失效，
// 哪怕清理 worker 从此再没跑过。清理 worker 因此纯属卫生——它失败只会
// 留下难看的死数据，不会留下一把还能用的旧密钥。
func (s *PostgresStore) ByHash(ctx context.Context, hash string) (*Key, error) {
	var (
		k   Key
		enc string
	)
	err := s.db.QueryRowContext(ctx, `
		SELECT id, key_prefix, org_id, user_id, upstream_key_enc, status, expires_at,
		       (key_hash <> $1) AS via_prev, prev_key_expires_at
		FROM api_keys
		WHERE key_hash = $1
		   OR (prev_key_hash = $1 AND prev_key_expires_at > now())`, hash).
		Scan(&k.ID, &k.Prefix, &k.OrgID, &k.UserID, &enc, &k.Status, &k.ExpiresAt,
			&k.ViaPrevKey, &k.PrevKeyExpiresAt)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, ErrKeyNotFound
	}
	if err != nil {
		return nil, fmt.Errorf("查询密钥失败: %w", err)
	}

	upstream, err := s.cipher.Decrypt(enc)
	if err != nil {
		return nil, fmt.Errorf("解密上游密钥失败: %w", err)
	}
	k.UpstreamKey = upstream
	return &k, nil
}
```

未轮换的行 `prev_key_expires_at` 为 NULL，`PrevKeyExpiresAt` 因此也是 nil，与 `ViaPrevKey = false` 自洽。

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/apikey/ -count=1 -v
```

Expected: 全部 PASS（含既有测试）

- [ ] **Step 5: 让 Edge 把旧凭据的使用打出来**

`internal/edge/auth.go` 里，在 `key.Validate` 的 switch 结束之后、`next.ServeHTTP` 之前插入：

```go
			// 客户端若到窗口结束都没换上新凭据，到期瞬间会集体 401。
			// 打一条日志让运维能提前看到，而不是等故障发生。
			// 刻意只打日志、不写库：鉴权是热路径、目前零写入，
			// 为一个观测需求给每个请求加一次 DB 写不划算。
			if key.ViaPrevKey && key.PrevKeyExpiresAt != nil {
				slog.Warn("客户端仍在使用轮换前的旧凭据",
					"key_id", key.ID,
					"prev_expires_at", key.PrevKeyExpiresAt.UTC(),
					"remaining", time.Until(*key.PrevKeyExpiresAt).Round(time.Second))
			}
```

该文件 import 补 `"log/slog"`（若尚未引入）。

- [ ] **Step 6: 运行 Edge 全量测试确认零回归**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1
```

Expected: ok

- [ ] **Step 7: 提交**

```bash
git add internal/apikey/ internal/edge/auth.go
```

```bash
git commit -m "feat: Edge 兼容轮换共存窗口内的旧凭据"
```

---

## Task 6: KeyStore 的批量吊销

**Files:**
- Modify: `internal/control/model.go`, `internal/control/keystore.go`
- Test: `internal/control/keystore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加：

```go
// seedOrg 造一个组织节点（物化路径直接给定）。
func seedOrg(t *testing.T, db *sql.DB, id, path string) {
	t.Helper()
	_, err := db.Exec(
		`INSERT INTO organizations (id, name, path) VALUES ($1,$1,$2)`, id, path)
	require.NoError(t, err)
}

func TestRevokeByOrgSubtreeCoversDescendants(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "root", "/root")
	seedOrg(t, db, "rd", "/root/rd")
	seedOrg(t, db, "gw2", "/root/rd/gw2")
	seedKey(t, db, "k-rd", "h-rd", "rd", uid, "active")
	seedKey(t, db, "k-deep", "h-deep", "gw2", uid, "active")

	n, err := keys.RevokeByOrgSubtree(ctx, "/root/rd")
	require.NoError(t, err)
	require.Equal(t, int64(2), n, "节点自身与后代都要吊销")

	for _, id := range []string{"k-rd", "k-deep"} {
		got, err := keys.Get(ctx, id)
		require.NoError(t, err)
		require.Equal(t, "revoked", got.Status)
	}
}

func TestRevokeByOrgSubtreeSpareSiblingWithSamePrefix(t *testing.T) {
	// 第三次遇到这个陷阱（P1.2b 权限判定、P1.3b 审批人查找各踩过一次）：
	// /root/rd 是 /root/rd2 的前缀，但 rd2 并不在 rd 的子树里。
	// 必须加分隔符再比前缀，否则会把无关部门的密钥一起吊掉。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "root", "/root")
	seedOrg(t, db, "rd", "/root/rd")
	seedOrg(t, db, "rd2", "/root/rd2")
	seedKey(t, db, "k-rd", "h-rd", "rd", uid, "active")
	seedKey(t, db, "k-rd2", "h-rd2", "rd2", uid, "active")

	n, err := keys.RevokeByOrgSubtree(ctx, "/root/rd")
	require.NoError(t, err)
	require.Equal(t, int64(1), n)

	sibling, err := keys.Get(ctx, "k-rd2")
	require.NoError(t, err)
	require.Equal(t, "active", sibling.Status, "同前缀兄弟节点必须毫发无伤")
}

func TestRevokeByOrgSubtreeCoversPendingToo(t *testing.T) {
	// 正在签发中的密钥上游可能已经建成，批量吊销必须一并覆盖。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "rd", "/rd")
	seedKey(t, db, "k-pending", "h-pending", "rd", uid, "pending")

	n, err := keys.RevokeByOrgSubtree(ctx, "/rd")
	require.NoError(t, err)
	require.Equal(t, int64(1), n)
}

func TestRevokeByOrgSubtreeSkipsAlreadyRevoked(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "rd", "/rd")
	seedKey(t, db, "k-dead", "h-dead", "rd", uid, "revoked")

	n, err := keys.RevokeByOrgSubtree(ctx, "/rd")
	require.NoError(t, err)
	require.Zero(t, n, "已吊销的不该重复计数")
}

func TestRevokeAllCoversEverything(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "a", "/a")
	seedOrg(t, db, "b", "/b")
	seedKey(t, db, "k-a", "h-a", "a", uid, "active")
	seedKey(t, db, "k-b", "h-b", "b", uid, "pending")

	n, err := keys.RevokeAll(ctx)
	require.NoError(t, err)
	require.Equal(t, int64(2), n)

	for _, id := range []string{"k-a", "k-b"} {
		got, err := keys.Get(ctx, id)
		require.NoError(t, err)
		require.Equal(t, "revoked", got.Status)
	}
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestRevokeByOrgSubtree|TestRevokeAll" -count=1
```

Expected: FAIL —— `keys.RevokeByOrgSubtree undefined`

- [ ] **Step 3: 写实现**

`internal/control/model.go` 的 `KeyStore` 接口加两个方法：

```go
	// RevokeByOrgSubtree 吊销该路径子树下全部 active/pending 密钥，返回条数。
	RevokeByOrgSubtree(ctx context.Context, orgPath string) (int64, error)
	// RevokeAll 吊销全系统 active/pending 密钥，返回条数。
	RevokeAll(ctx context.Context) (int64, error)
```

`internal/control/keystore.go` 末尾追加：

```go
// RevokeByOrgSubtree 按物化路径前缀吊销整棵子树。
//
// 必须加分隔符再比前缀：/root/rd 是 /root/rd2 的前缀，但 rd2 并不在 rd 的
// 子树里。这个陷阱在 P1.2b 的权限判定与 P1.3b 的审批人查找里各踩过一次。
//
// pending 也要覆盖：那些密钥上游可能已经建成。
func (s *postgresKeyStore) RevokeByOrgSubtree(
	ctx context.Context, orgPath string,
) (int64, error) {
	res, err := s.db.ExecContext(ctx, `
		UPDATE api_keys SET status = 'revoked'
		WHERE status IN ('active','pending')
		  AND org_id IN (
		      SELECT id FROM organizations
		      WHERE path = $1 OR path LIKE $1 || '/%'
		  )`, orgPath)
	if err != nil {
		return 0, fmt.Errorf("按子树吊销密钥失败: %w", err)
	}
	return res.RowsAffected()
}

// RevokeAll 是 break glass：吊销全系统密钥。不可逆。
func (s *postgresKeyStore) RevokeAll(ctx context.Context) (int64, error) {
	res, err := s.db.ExecContext(ctx,
		`UPDATE api_keys SET status = 'revoked' WHERE status IN ('active','pending')`)
	if err != nil {
		return 0, fmt.Errorf("全局吊销密钥失败: %w", err)
	}
	return res.RowsAffected()
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestRevokeByOrgSubtree|TestRevokeAll" -count=1 -v
```

Expected: 5 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
git add internal/control/model.go internal/control/keystore.go internal/control/keystore_test.go
```

```bash
git commit -m "feat: 按组织子树与全局的批量吊销"
```

---

## Task 7: 上游封禁的记账方法

**Files:**
- Modify: `internal/control/model.go`, `internal/control/keystore.go`
- Test: `internal/control/keystore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keystore_test.go` 末尾追加：

```go
func TestListRevokedUnblockedOnlyPicksRevokedAndUnstamped(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "o", "/o")
	seedKey(t, db, "k-live", "h1", "o", uid, "active")   // 没吊销，不该捞
	seedKey(t, db, "k-todo", "h2", "o", uid, "revoked")  // 该捞
	seedKey(t, db, "k-done", "h3", "o", uid, "revoked")  // 已盖戳，不该捞
	require.NoError(t, keys.MarkUpstreamBlocked(ctx, "k-done"))

	got, err := keys.ListRevokedUnblocked(ctx, 5, 100)
	require.NoError(t, err)
	require.Len(t, got, 1)
	require.Equal(t, "k-todo", got[0].ID)
}

func TestListRevokedUnblockedRespectsAttemptCap(t *testing.T) {
	// 重试到上限后就不再捞它，免得一行坏数据每轮都刷日志。
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "o", "/o")
	seedKey(t, db, "k-bad", "h1", "o", uid, "revoked")
	for i := 0; i < 3; i++ {
		require.NoError(t, keys.RecordBlockAttempt(ctx, "k-bad"))
	}

	got, err := keys.ListRevokedUnblocked(ctx, 3, 100)
	require.NoError(t, err)
	require.Empty(t, got, "尝试次数已达上限，不该再被捞出来")

	// 把上限抬高就又能捞到了，证明拦住它的确实是次数而不是别的。
	got, err = keys.ListRevokedUnblocked(ctx, 4, 100)
	require.NoError(t, err)
	require.Len(t, got, 1)
	require.Equal(t, 3, got[0].UpstreamBlockAttempts)
}

func TestListRevokedUnblockedRespectsLimit(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "o", "/o")
	seedKey(t, db, "k1", "h1", "o", uid, "revoked")
	seedKey(t, db, "k2", "h2", "o", uid, "revoked")

	got, err := keys.ListRevokedUnblocked(ctx, 5, 1)
	require.NoError(t, err)
	require.Len(t, got, 1, "批量上限必须生效，积压时一轮不该把整张表拉进内存")
}

func TestMarkUpstreamBlockedStamps(t *testing.T) {
	_, _, _, keys, db, uid := issuerFixture(t)
	ctx := context.Background()
	seedOrg(t, db, "o", "/o")
	seedKey(t, db, "k1", "h1", "o", uid, "revoked")

	require.NoError(t, keys.MarkUpstreamBlocked(ctx, "k1"))

	got, err := keys.Get(ctx, "k1")
	require.NoError(t, err)
	require.NotNil(t, got.UpstreamBlockedAt)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestListRevokedUnblocked|TestMarkUpstreamBlocked" -count=1
```

Expected: FAIL —— `keys.MarkUpstreamBlocked undefined`

- [ ] **Step 3: 写实现**

`internal/control/model.go` 的 `KeyStore` 接口加三个方法：

```go
	// ListRevokedUnblocked 捞出已吊销、尚未确认上游封禁、且未超重试上限的密钥。
	ListRevokedUnblocked(ctx context.Context, maxAttempts, limit int) ([]*APIKey, error)
	// MarkUpstreamBlocked 记下上游封禁已确认。
	MarkUpstreamBlocked(ctx context.Context, id string) error
	// RecordBlockAttempt 计一次失败尝试，不改状态。
	RecordBlockAttempt(ctx context.Context, id string) error
```

`internal/control/keystore.go` 末尾追加：

```go
func (s *postgresKeyStore) ListRevokedUnblocked(
	ctx context.Context, maxAttempts, limit int,
) ([]*APIKey, error) {
	rows, err := s.db.QueryContext(ctx, `
		SELECT `+keyColumns+` FROM api_keys
		WHERE status = 'revoked' AND upstream_blocked_at IS NULL
		  AND upstream_block_attempts < $1
		ORDER BY created_at LIMIT $2`, maxAttempts, limit)
	if err != nil {
		return nil, fmt.Errorf("查询待封禁密钥失败: %w", err)
	}
	defer func() { _ = rows.Close() }()
	return collectKeys(rows)
}

func (s *postgresKeyStore) MarkUpstreamBlocked(ctx context.Context, id string) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE api_keys SET upstream_blocked_at = $1 WHERE id = $2`,
		time.Now().UTC(), id)
	if err != nil {
		return fmt.Errorf("标记上游已封禁失败: %w", err)
	}
	return nil
}

func (s *postgresKeyStore) RecordBlockAttempt(ctx context.Context, id string) error {
	_, err := s.db.ExecContext(ctx,
		`UPDATE api_keys SET upstream_block_attempts = upstream_block_attempts + 1
		 WHERE id = $1`, id)
	if err != nil {
		return fmt.Errorf("记录封禁尝试失败: %w", err)
	}
	return nil
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestListRevokedUnblocked|TestMarkUpstreamBlocked" -count=1 -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
git add internal/control/model.go internal/control/keystore.go internal/control/keystore_test.go
```

```bash
git commit -m "feat: 上游封禁的记账方法"
```

---

## Task 8: 上游封禁兜底扫描

**Files:**
- Modify: `internal/control/keyissue.go`, `internal/control/fakekeyadmin_test.go`
- Test: `internal/control/keyissue_test.go`

设计文档 D4：本地吊销立即生效，上游封禁由扫描收敛。这同时补上一个既有缺口——今天 `BlockKey` 失败只记一行 WARN 且**永不重试**。

- [ ] **Step 1: 给 fakeKeyAdmin 加一个「返回 404」的开关**

`internal/control/fakekeyadmin_test.go` 的字段块加一行：

```go
	blockNotFound bool
```

`BlockKey` 改为：

```go
func (f *fakeKeyAdmin) BlockKey(_ context.Context, key string) error {
	f.mu.Lock()
	defer f.mu.Unlock()
	f.calls = append(f.calls, "block:"+key)
	if f.blockNotFound {
		// 探针 P1 的真实形态：上游对不存在的 key 返回 404。
		return &litellm.APIError{Status: 404, Body: `{"error":{"message":"Key not found."}}`}
	}
	return f.blockErr
}
```

该文件已 import `litellm`。`litellm.APIError` 的字段是 `Status int` 与 `Body string`（`internal/litellm/client.go:44`）——**不是** `Message`。

- [ ] **Step 2: 写失败测试**

在 `internal/control/keyissue_test.go` 末尾追加：

```go
func TestBlockPendingUpstreamBlocksAndStamps(t *testing.T) {
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	// 造出「本地已吊销、上游没封成」的状态：直接改状态，绕开 Revoke。
	require.NoError(t, keys.Revoke(ctx, k.ID))
	admin.resetCalls()

	n, err := iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	require.Equal(t, 1, n)

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.NotNil(t, got.UpstreamBlockedAt, "封成了就要盖戳")
	require.Len(t, admin.callsSnapshot(), 1)
}

func TestBlockPendingUpstreamIsIdempotentAcrossRuns(t *testing.T) {
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	require.NoError(t, keys.Revoke(ctx, k.ID))

	_, err = iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	admin.resetCalls()

	n, err := iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	require.Zero(t, n)
	require.Empty(t, admin.callsSnapshot(), "盖过戳的不该再动上游")
}

func TestBlockPendingUpstreamTreats404AsDone(t *testing.T) {
	// 探针 P1：上游对不存在的 key 返回 404。那把密钥本来就不在了，
	// 当作封成功盖戳，否则它会一直占着重试额度。
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	require.NoError(t, keys.Revoke(ctx, k.ID))
	admin.blockNotFound = true

	n, err := iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	require.Equal(t, 1, n)

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.NotNil(t, got.UpstreamBlockedAt)
}

func TestBlockPendingUpstreamRetriesOnFailure(t *testing.T) {
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	require.NoError(t, keys.Revoke(ctx, k.ID))
	admin.blockErr = errUpstreamDown

	_, err = iss.BlockPendingUpstream(ctx)
	require.Error(t, err)

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.Nil(t, got.UpstreamBlockedAt, "没封成就不能盖戳")
	require.Equal(t, 1, got.UpstreamBlockAttempts)

	// 上游恢复后下一轮应当收敛。
	admin.blockErr = nil
	n, err := iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	require.Equal(t, 1, n)
}

func TestBlockPendingUpstreamGivesUpAfterMaxAttempts(t *testing.T) {
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	require.NoError(t, keys.Revoke(ctx, k.ID))
	admin.blockErr = errUpstreamDown

	for i := 0; i < maxBlockAttempts; i++ {
		_, _ = iss.BlockPendingUpstream(ctx)
	}
	admin.resetCalls()

	n, err := iss.BlockPendingUpstream(ctx)
	require.NoError(t, err)
	require.Zero(t, n)
	require.Empty(t, admin.callsSnapshot(),
		"重试耗尽后就停下来交给人，不让一行坏数据每轮刷日志")
}

func TestRevokeStampsUpstreamBlockedOnSuccess(t *testing.T) {
	// 单把吊销走内联封禁：成功即盖戳，扫描下一轮就不用再管它。
	iss, _, _, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)

	require.NoError(t, iss.Revoke(ctx, k.ID))

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.NotNil(t, got.UpstreamBlockedAt)
}

func TestRevokeLeavesStampEmptyWhenUpstreamFails(t *testing.T) {
	// 上游失败不该让吊销失败（既有语义），但必须留白让扫描重试——
	// 这正是本阶段补上的那个缺口。
	iss, _, admin, keys, _, uid := issuerFixture(t)
	ctx := context.Background()
	_, k, err := iss.Issue(ctx, IssueRequest{
		OrgID: "gw", UserID: uid, Name: "x", Models: []string{},
	})
	require.NoError(t, err)
	admin.blockErr = errUpstreamDown

	require.NoError(t, iss.Revoke(ctx, k.ID), "上游失败不影响吊销生效")

	got, err := keys.Get(ctx, k.ID)
	require.NoError(t, err)
	require.Equal(t, "revoked", got.Status)
	require.Nil(t, got.UpstreamBlockedAt, "没封成就留白，交给扫描")
}
```

- [ ] **Step 3: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestBlockPendingUpstream|TestRevokeStamps|TestRevokeLeaves" -count=1
```

Expected: FAIL —— `iss.BlockPendingUpstream undefined`

- [ ] **Step 4: 写实现**

`internal/control/keyissue.go` 的常量块加两条：

```go
	// maxBlockAttempts 是上游封禁的重试上限。
	// 无限重试会让一行坏数据每轮都刷一次错误日志，到点要停下来交给人。
	maxBlockAttempts = 5
	// blockScanBatchSize 是单轮扫描的批量上限。
	blockScanBatchSize = 100
```

把既有的 `Revoke` 里封禁那一段改为盖戳版本：

```go
	if err := i.deps.Admin.BlockKey(ctx, upstreamKey); err != nil {
		// 留白不盖戳，交给 BlockPendingUpstream 扫描重试。
		// 本地已吊销、Edge 已经在拒，所以这里不让整体失败。
		slog.Warn("封禁上游密钥失败，已留给兜底扫描重试",
			"key_id", id, "err", err)
		return nil
	}
	if err := i.deps.Keys.MarkUpstreamBlocked(ctx, id); err != nil {
		slog.Warn("标记上游已封禁失败，兜底扫描会再来一次", "key_id", id, "err", err)
	}
	return nil
}
```

文件末尾追加扫描与 404 判定：

```go
// BlockPendingUpstream 给已吊销但尚未确认封禁的密钥补做上游封禁，
// 返回本轮成功收敛的条数。
//
// 批量吊销只做本地 UPDATE 就返回（上游没有批量封禁接口，探针 P3 已证：
// /key/block 只收单数 key），因此上游那一半必须由这个扫描收敛。
// 它同时兜住单把吊销与离职对账里内联封禁失败的情况——在此之前，
// 那种失败只记一行 WARN 且永不重试，纵深就此静默消失。
func (i *KeyIssuer) BlockPendingUpstream(ctx context.Context) (int, error) {
	list, err := i.deps.Keys.ListRevokedUnblocked(ctx, maxBlockAttempts, blockScanBatchSize)
	if err != nil {
		return 0, err
	}

	done, failures := 0, 0
	for _, k := range list {
		upstreamKey, derr := i.deps.Cipher.Decrypt(k.UpstreamKeyEnc)
		if derr != nil {
			// 解不开就永远解不开，计一次尝试让它自然走到上限。
			slog.Warn("解密上游密钥失败，跳过", "key_id", k.ID, "err", derr)
			i.recordBlockFailure(ctx, k)
			failures++
			continue
		}

		berr := i.deps.Admin.BlockKey(ctx, upstreamKey)
		if berr != nil && !isUpstreamNotFound(berr) {
			slog.Warn("兜底封禁失败，下一轮重试", "key_id", k.ID, "err", berr)
			i.recordBlockFailure(ctx, k)
			failures++
			continue
		}
		// 404 视为已经不在了（探针 P1），与封成功同样处理。
		if err := i.deps.Keys.MarkUpstreamBlocked(ctx, k.ID); err != nil {
			slog.Warn("标记上游已封禁失败", "key_id", k.ID, "err", err)
			continue
		}
		done++
	}
	if failures > 0 {
		return done, fmt.Errorf("有 %d 把密钥的上游封禁失败", failures)
	}
	return done, nil
}

func (i *KeyIssuer) recordBlockFailure(ctx context.Context, k *APIKey) {
	if err := i.deps.Keys.RecordBlockAttempt(ctx, k.ID); err != nil {
		slog.Warn("记录封禁尝试失败", "key_id", k.ID, "err", err)
		return
	}
	if k.UpstreamBlockAttempts+1 >= maxBlockAttempts {
		slog.Error("上游封禁重试耗尽，已停止重试，请人工处理",
			"key_id", k.ID, "attempts", k.UpstreamBlockAttempts+1)
	}
}

// isUpstreamNotFound 判断上游是否回了 404。
// 探针 P1：对不存在的 key，/key/block 返回 404 "Key not found."。
func isUpstreamNotFound(err error) bool {
	var apiErr *litellm.APIError
	return errors.As(err, &apiErr) && apiErr.Status == http.StatusNotFound
}
```

`internal/control/keyissue.go` 的 import 补 `"errors"` 与 `"net/http"`（`litellm` 已引入）。

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestBlockPendingUpstream|TestRevokeStamps|TestRevokeLeaves" -count=1 -v
```

Expected: 7 个测试全部 PASS

- [ ] **Step 6: 运行全量测试**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -count=1
```

Expected: ok

- [ ] **Step 7: 提交**

```bash
git add internal/control/keyissue.go internal/control/keyissue_test.go internal/control/fakekeyadmin_test.go
```

```bash
git commit -m "feat: 上游封禁兜底扫描，补上失败永不重试的缺口"
```

---

## Task 9: 离职对账的封禁也盖戳

**Files:**
- Modify: `internal/control/reconciler.go`
- Test: `internal/control/reconciler_test.go`

P1.3b 让离职对账也封禁上游，但它同样只在失败时记一行日志。现在让它与单把吊销对齐：成功盖戳、失败留白交给扫描。

- [ ] **Step 1: 写失败测试**

在 `internal/control/reconciler_test.go` 末尾追加：

```go
func TestKeyRevokerStampsUpstreamBlockedOnSuccess(t *testing.T) {
	db := testDB(t)
	cleanTables(t, db)
	cipher := testCipher(t)
	uid := seedRevokableKey(t, db, cipher, "sk-airlock-leaver", "active")

	r := NewPostgresKeyRevoker(db, newFakeKeyAdmin(), cipher)
	_, err := r.RevokeByUsers(context.Background(), []string{uid})
	require.NoError(t, err)

	var blockedAt *time.Time
	require.NoError(t, db.QueryRow(
		`SELECT upstream_blocked_at FROM api_keys WHERE id='k1'`).Scan(&blockedAt))
	require.NotNil(t, blockedAt, "封成了就要盖戳，扫描下一轮不用再管它")
}

func TestKeyRevokerLeavesStampEmptyWhenUpstreamFails(t *testing.T) {
	// 留白不是遗漏，是交接：兜底扫描会把它收敛掉。
	db := testDB(t)
	cleanTables(t, db)
	cipher := testCipher(t)
	uid := seedRevokableKey(t, db, cipher, "sk-airlock-leaver", "active")

	admin := newFakeKeyAdmin()
	admin.blockErr = errUpstreamDown
	r := NewPostgresKeyRevoker(db, admin, cipher)
	_, err := r.RevokeByUsers(context.Background(), []string{uid})
	require.NoError(t, err, "上游失败不影响吊销生效")

	var blockedAt *time.Time
	require.NoError(t, db.QueryRow(
		`SELECT upstream_blocked_at FROM api_keys WHERE id='k1'`).Scan(&blockedAt))
	require.Nil(t, blockedAt)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestKeyRevokerStamps|TestKeyRevokerLeaves" -count=1
```

Expected: FAIL —— `TestKeyRevokerStampsUpstreamBlockedOnSuccess` 断言 `blockedAt` 非空失败（当前实现从不盖戳）

- [ ] **Step 3: 写实现**

`internal/control/reconciler.go` 的 `RevokeByUsers` 里，封禁那一段改为：

```go
	// 上游封禁尽力而为。成功即盖戳；失败留白，交给 BlockPendingUpstream
	// 扫描收敛——本地已标记 revoked，Edge 下一个请求就会拒绝该密钥。
	for _, t := range targets {
		upstreamKey, derr := k.cipher.Decrypt(t.enc)
		if derr != nil {
			slog.Warn("解密上游密钥失败，跳过上游封禁", "key_id", t.id, "err", derr)
			continue
		}
		if berr := k.admin.BlockKey(ctx, upstreamKey); berr != nil {
			slog.Warn("封禁上游密钥失败，已留给兜底扫描重试", "key_id", t.id, "err", berr)
			continue
		}
		if _, uerr := k.db.ExecContext(ctx,
			`UPDATE api_keys SET upstream_blocked_at = $1 WHERE id = $2`,
			time.Now().UTC(), t.id); uerr != nil {
			slog.Warn("标记上游已封禁失败，兜底扫描会再来一次",
				"key_id", t.id, "err", uerr)
		}
	}
```

`internal/control/reconciler.go` 已 import `time`。

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run TestKeyRevoker -count=1 -v
```

Expected: 5 个测试全部 PASS（含 P1.3b 的三条）

- [ ] **Step 5: 提交**

```bash
git add internal/control/reconciler.go internal/control/reconciler_test.go
```

```bash
git commit -m "feat: 离职对账的上游封禁也盖戳，与兜底扫描对齐"
```

---

## Task 10: HTTP —— 轮换接口

**Files:**
- Modify: `internal/control/keyapi.go`
- Test: `internal/control/keyapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/keyapi_test.go` 末尾追加：

```go
// rotateReq 发一次轮换请求。ownerID 为空表示不带用户上下文。
func rotateReq(
	t *testing.T, api *KeyAPI, callerID, keyID, body string,
) *httptest.ResponseRecorder {
	t.Helper()
	req := httptest.NewRequest(http.MethodPost,
		"/api/keys/"+keyID+"/rotate", strings.NewReader(body))
	req.SetPathValue("id", keyID)
	req = req.WithContext(context.WithValue(req.Context(), userCtxKey,
		&User{ID: callerID, Status: UserStatusActive}))
	rec := httptest.NewRecorder()
	api.HandleRotate(rec, req)
	return rec
}

func TestRotateAPIByOwnerReturnsNewPlaintext(t *testing.T) {
	// 责任人可以自助轮换自己的密钥，与 P1.3b 的自助领取一脉相承。
	api, _, _, _, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	require.Equal(t, http.StatusCreated, issued.Code)
	var created struct {
		ID  string `json:"id"`
		Key string `json:"key"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	rec := rotateReq(t, api, uid, created.ID, `{}`)
	require.Equal(t, http.StatusOK, rec.Code)

	var got struct {
		ID  string `json:"id"`
		Key string `json:"key"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &got))
	require.Equal(t, created.ID, got.ID, "密钥身份跨轮换稳定")
	require.True(t, strings.HasPrefix(got.Key, "ak-"))
	require.NotEqual(t, created.Key, got.Key)
}

func TestRotateAPIByStrangerIs403(t *testing.T) {
	// 既不是责任人、也没有 key:write，不能轮换别人的密钥。
	api, _, _, _, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	rec := rotateReq(t, api, "someone-else", created.ID, `{}`)
	require.Equal(t, http.StatusForbidden, rec.Code)
	require.Contains(t, rec.Body.String(), "permission_denied")
}

func TestRotateAPIByAdminWithKeyWrite(t *testing.T) {
	// 管理员可以替人处置。
	api, _, _, _, rbac, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	adminID := "admin-user"
	require.NoError(t, rbac.CreateGrant(context.Background(), RoleGrant{
		ID: "g-admin", UserID: adminID, RoleID: authz.RoleOrgAdmin, OrgID: strp("gw"),
	}))

	rec := rotateReq(t, api, adminID, created.ID, `{}`)
	require.Equal(t, http.StatusOK, rec.Code)
}

func TestRotateAPIUnknownKeyIs404(t *testing.T) {
	api, _, _, _, _, uid := keyAPIFixture(t)
	rec := rotateReq(t, api, uid, "nope", `{}`)
	require.Equal(t, http.StatusNotFound, rec.Code)
}

func TestRotateAPIRevokedKeyIs409(t *testing.T) {
	api, _, _, keys, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))
	require.NoError(t, keys.Revoke(context.Background(), created.ID))

	rec := rotateReq(t, api, uid, created.ID, `{}`)
	require.Equal(t, http.StatusConflict, rec.Code)
	require.Contains(t, rec.Body.String(), "key_not_active")
}

func TestRotateAPIWindowTooLongIs400(t *testing.T) {
	api, _, _, _, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	rec := rotateReq(t, api, uid, created.ID, `{"window_seconds":99999999}`)
	require.Equal(t, http.StatusBadRequest, rec.Code)
	require.Contains(t, rec.Body.String(), "window_too_long")
}
```

该文件 import 若缺 `"context"`、`"encoding/json"`、`"github.com/softmatrix/airlock/internal/authz"`，补上。

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run TestRotateAPI -count=1
```

Expected: FAIL —— `api.HandleRotate undefined`

- [ ] **Step 3: 写实现**

`internal/control/keyapi.go` 末尾追加：

```go
// HandleRotate 换发一把密钥的客户端凭据。新的 ak- 明文只在这里返回一次。
//
// 权限判定下沉到这里：路径里只有密钥 ID，中间件拿不到它所属的节点，
// 与 HandleRevoke 是同一类例外。允许两种人轮换——
// 责任人本人（自助，与 P1.3b 的自助领取一脉相承），
// 或在该密钥所属节点上持有 key:write 的管理员（替人处置）。
func (a *KeyAPI) HandleRotate(w http.ResponseWriter, r *http.Request) {
	var body struct {
		// WindowSeconds 是共存窗口秒数；0 或缺省表示用默认值。
		WindowSeconds int64 `json:"window_seconds"`
	}
	// 空请求体是合法的（表示全用默认值），因此解码失败不当错误处理。
	_ = json.NewDecoder(r.Body).Decode(&body)

	id := r.PathValue("id")
	k, err := a.keys.Get(r.Context(), id)
	if err != nil {
		writeKeyError(w, err, "轮换密钥失败")
		return
	}

	u, ok := UserFromContext(r.Context())
	if !ok {
		writeError(w, http.StatusInternalServerError, "internal_error", "上下文缺少用户")
		return
	}
	if u.ID != k.UserID {
		allowed, err := a.resolver.Can(r.Context(), subjectOf(u), authz.PermKeyWrite, &k.OrgID)
		if err != nil {
			writeError(w, http.StatusInternalServerError, "internal_error", "权限判定失败")
			return
		}
		if !allowed {
			writeError(w, http.StatusForbidden, "permission_denied", "没有轮换该密钥的权限")
			return
		}
	}

	plaintext, rotated, err := a.issuer.Rotate(
		r.Context(), id, time.Duration(body.WindowSeconds)*time.Second)
	if err != nil {
		writeKeyError(w, err, "轮换密钥失败")
		return
	}

	writeJSON(w, http.StatusOK, struct {
		keyView
		Key string `json:"key"`
	}{keyView: viewOf(rotated), Key: plaintext})
}
```

`writeKeyError` 加两个分支（放在 `ErrAPIKeyNotFound` 之后）：

```go
	case errors.Is(err, ErrKeyNotActive):
		writeError(w, http.StatusConflict, "key_not_active",
			"密钥不处于可用状态，无法轮换")
	case errors.Is(err, ErrWindowTooLong):
		writeError(w, http.StatusBadRequest, "window_too_long",
			"共存窗口超过上限（最长 30 天）")
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run TestRotateAPI -count=1 -v
```

Expected: 6 个测试全部 PASS

- [ ] **Step 5: 提交**

```bash
git add internal/control/keyapi.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: 轮换接口，责任人自助或管理员替办"
```

---

## Task 11: HTTP —— 批量与紧急吊销

**Files:**
- Modify: `internal/control/keyapi.go`
- Test: `internal/control/keyapi_test.go`

本任务给 `KeyAPI` 加一个 `orgs OrgStore`（按子树吊销要先把节点 ID 换成物化路径），构造函数因此变成四个参数。调用点有两处：`keyAPIFixture`（Task 11 内改）与 `internal/app/control.go`（Task 13 内改）。

- [ ] **Step 1: 改夹具并写失败测试**

`internal/control/keyapi_test.go` 的 `keyAPIFixture` 改为把 `orgs` 传进去：

```go
func keyAPIFixture(t *testing.T) (api *KeyAPI, orgs *fakeOrgStore, admin *fakeKeyAdmin, keys KeyStore, rbac *fakeRBACStore, uid string) {
	t.Helper()
	iss, orgs, admin, keys, _, uid := issuerFixture(t)
	rbac = newFakeRBACStore()
	// resolver.Can 靠 OrgPath 算祖先链，fake RBAC store 不会自动跟着
	// fakeOrgStore 的树走，必须显式登记——否则权限判定直接因
	// authz.ErrOrgNotFound 拿到 500，而不是被测的业务状态码。
	rbac.setPath("gw", "/gw")
	return NewKeyAPI(iss, keys, orgs, authz.NewResolver(rbac)), orgs, admin, keys, rbac, uid
}
```

在文件末尾追加：

```go
func revokeOrgReq(t *testing.T, api *KeyAPI, callerID, orgID string) *httptest.ResponseRecorder {
	t.Helper()
	req := httptest.NewRequest(http.MethodPost, "/api/orgs/"+orgID+"/keys/revoke", nil)
	req.SetPathValue("id", orgID)
	req = req.WithContext(context.WithValue(req.Context(), userCtxKey,
		&User{ID: callerID, Status: UserStatusActive}))
	rec := httptest.NewRecorder()
	api.HandleRevokeOrg(rec, req)
	return rec
}

func revokeAllReq(t *testing.T, api *KeyAPI, body string) *httptest.ResponseRecorder {
	t.Helper()
	req := httptest.NewRequest(http.MethodPost, "/api/keys/revoke-all",
		strings.NewReader(body))
	req = req.WithContext(context.WithValue(req.Context(), userCtxKey,
		&User{ID: "someone", Status: UserStatusActive}))
	rec := httptest.NewRecorder()
	api.HandleRevokeAll(rec, req)
	return rec
}

func TestRevokeOrgAPIRevokesSubtree(t *testing.T) {
	api, _, _, keys, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	rec := revokeOrgReq(t, api, uid, "gw")
	require.Equal(t, http.StatusOK, rec.Code)

	var got struct {
		Revoked int64 `json:"revoked"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &got))
	require.Equal(t, int64(1), got.Revoked)

	k, err := keys.Get(context.Background(), created.ID)
	require.NoError(t, err)
	require.Equal(t, "revoked", k.Status)
}

func TestRevokeOrgAPIUnknownOrgIs404(t *testing.T) {
	api, _, _, _, _, uid := keyAPIFixture(t)
	rec := revokeOrgReq(t, api, uid, "nope")
	require.Equal(t, http.StatusNotFound, rec.Code)
}

func TestRevokeAllAPIRequiresConfirmString(t *testing.T) {
	// 不可逆操作靠事前防护：确认字符串必须精确匹配。
	api, _, _, _, _, _ := keyAPIFixture(t)

	require.Equal(t, http.StatusBadRequest, revokeAllReq(t, api, `{}`).Code)

	rec := revokeAllReq(t, api, `{"confirm":"revoke all keys"}`)
	require.Equal(t, http.StatusBadRequest, rec.Code, "大小写不同也必须拒绝")
	require.Contains(t, rec.Body.String(), "confirm_required")
}

func TestRevokeAllAPIWithConfirmRevokesEverything(t *testing.T) {
	api, _, _, keys, _, uid := keyAPIFixture(t)
	issued := postKey(t, api, `{"org_id":"gw","user_id":"`+uid+`","name":"x","models":[]}`)
	var created struct {
		ID string `json:"id"`
	}
	require.NoError(t, json.Unmarshal(issued.Body.Bytes(), &created))

	rec := revokeAllReq(t, api, `{"confirm":"REVOKE ALL KEYS"}`)
	require.Equal(t, http.StatusOK, rec.Code)

	k, err := keys.Get(context.Background(), created.ID)
	require.NoError(t, err)
	require.Equal(t, "revoked", k.Status)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestRevokeOrgAPI|TestRevokeAllAPI" -count=1
```

Expected: FAIL —— `too many arguments in call to NewKeyAPI` / `api.HandleRevokeOrg undefined`

- [ ] **Step 3: 写实现**

`internal/control/keyapi.go` 的结构体与构造函数改为：

```go
type KeyAPI struct {
	issuer   *KeyIssuer
	keys     KeyStore
	orgs     OrgStore
	resolver *authz.Resolver
}

// orgs 供 HandleRevokeOrg 用：按子树吊销要先把节点 ID 换成物化路径。
// resolver 供 HandleRevoke/HandleRotate 用：这两条路径里只有密钥 ID，
// 中间件拿不到它所属的节点，判定只能下沉到处理器自己做。
func NewKeyAPI(issuer *KeyIssuer, keys KeyStore, orgs OrgStore, resolver *authz.Resolver) *KeyAPI {
	return &KeyAPI{issuer: issuer, keys: keys, orgs: orgs, resolver: resolver}
}
```

文件末尾追加两个处理器：

```go
// revokeResult 是两个批量吊销端点的统一响应体。
type revokeResult struct {
	Revoked int64 `json:"revoked"`
}

// HandleRevokeOrg 吊销某节点子树下的全部密钥。
//
// 权限由中间件判定（路径里就是节点 ID，TargetFromPath 能取到）。
// 这里只做本地 UPDATE 就返回：上游没有批量封禁接口，逐把封会超时，
// 交给 BlockPendingUpstream 扫描收敛。此刻安全属性已经满足——
// Edge 每请求查库，下一个请求就拒。
func (a *KeyAPI) HandleRevokeOrg(w http.ResponseWriter, r *http.Request) {
	org, err := a.orgs.Get(r.Context(), r.PathValue("id"))
	if err != nil {
		writeKeyError(w, err, "吊销密钥失败")
		return
	}

	n, err := a.keys.RevokeByOrgSubtree(r.Context(), org.Path)
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "批量吊销失败")
		return
	}
	slog.Warn("按组织子树批量吊销密钥", "org_id", org.ID, "path", org.Path, "revoked", n)
	writeJSON(w, http.StatusOK, revokeResult{Revoked: n})
}

// revokeAllConfirmation 是紧急全局吊销必须原样带上的确认字符串。
// 这是不可逆操作唯一的事前防护，必须精确匹配。
const revokeAllConfirmation = "REVOKE ALL KEYS"

// HandleRevokeAll 紧急吊销全系统密钥。break glass，不可逆。
//
// 权限由中间件判定（全局权限 key:revoke_all）。确认字符串是第二道闸：
// 全局权限管住「谁能按」，确认字符串管住「不会手滑按到」。
func (a *KeyAPI) HandleRevokeAll(w http.ResponseWriter, r *http.Request) {
	var body struct {
		Confirm string `json:"confirm"`
	}
	if err := json.NewDecoder(r.Body).Decode(&body); err != nil {
		writeError(w, http.StatusBadRequest, "invalid_body", "请求体不是合法 JSON")
		return
	}
	if body.Confirm != revokeAllConfirmation {
		writeError(w, http.StatusBadRequest, "confirm_required",
			`紧急全局吊销不可逆，请在请求体中带上 {"confirm":"`+revokeAllConfirmation+`"}`)
		return
	}

	n, err := a.keys.RevokeAll(r.Context())
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "全局吊销失败")
		return
	}
	slog.Error("已执行紧急全局吊销", "revoked", n)
	writeJSON(w, http.StatusOK, revokeResult{Revoked: n})
}
```

`internal/control/keyapi.go` 的 import 补 `"log/slog"`。

全局吊销用 `slog.Error` 而不是 `Warn`：它是全系统最具破坏性的一次操作，日志级别应当反映这一点，便于告警规则直接抓。

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -run "TestRevokeOrgAPI|TestRevokeAllAPI" -count=1 -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 5: 运行 control 全量测试**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -count=1
```

Expected: ok（`internal/app` 此时还没改，但它不在本包，编译不受影响）

- [ ] **Step 6: 提交**

```bash
git add internal/control/keyapi.go internal/control/keyapi_test.go
```

```bash
git commit -m "feat: 按子树与全局紧急吊销的接口"
```

---

## Task 12: 路由装配

**Files:**
- Modify: `internal/control/routes.go`, `internal/control/routes_test.go`

- [ ] **Step 1: 写失败测试**

`internal/control/routes_test.go` 的 `TestDefaultRoutesCoverAllOrgEndpoints` 期望清单末尾追加：

```go
		"POST /api/keys/{id}/rotate",
		"POST /api/orgs/{id}/keys/revoke",
		"POST /api/keys/revoke-all",
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./internal/control/ -run TestDefaultRoutesCoverAllOrgEndpoints -count=1
```

Expected: FAIL —— `路由表缺少 POST /api/keys/{id}/rotate`

- [ ] **Step 3: 写实现**

`internal/control/routes.go` 的申请与审批那组路由之后追加：

```go
		// ---- 轮换与批量吊销 ----
		{
			// 路径里只有密钥 ID，中间件拿不到它所属的节点，
			// 判定下沉到 HandleRotate 自己做（责任人本人或节点上的 key:write）。
			Pattern: "POST /api/keys/{id}/rotate", Access: AccessAuthenticated,
			Handler: keyH(func(k *KeyAPI) http.HandlerFunc { return k.HandleRotate }),
		},
		{
			Pattern: "POST /api/orgs/{id}/keys/revoke", Access: AccessPermission,
			Permission: authz.PermKeyWrite, Target: TargetFromPath("id"),
			Handler: keyH(func(k *KeyAPI) http.HandlerFunc { return k.HandleRevokeOrg }),
		},
		{
			// 全系统最具破坏性的一次调用，单开一个全局权限：
			// 塞进 platform:configure 里等于谁能改配置谁就能清空全公司凭据。
			Pattern: "POST /api/keys/revoke-all", Access: AccessPermission,
			Permission: authz.PermKeyRevokeAll, Target: TargetGlobal(),
			Handler: keyH(func(k *KeyAPI) http.HandlerFunc { return k.HandleRevokeAll }),
		},
```

- [ ] **Step 4: 运行全量测试**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/ -count=1
```

Expected: ok（`TestEveryRouteDeclaresAccess` 等机械检查自动覆盖三条新路由）

- [ ] **Step 5: 提交**

```bash
git add internal/control/routes.go internal/control/routes_test.go
```

```bash
git commit -m "feat: 轮换与批量吊销的三条路由入表"
```

---

## Task 13: 配置与进程接线

**Files:**
- Modify: `internal/config/config.go`, `internal/config/config_test.go`, `internal/app/control.go`, `.env.example`

- [ ] **Step 1: 写配置的失败测试**

在 `internal/config/config_test.go` 末尾追加：

```go
func TestKeyMaintenanceDefault(t *testing.T) {
	cfg, err := Load(func(string) string { return "" })
	require.NoError(t, err)
	require.Equal(t, time.Minute, cfg.KeyMaintenanceInterval)
}

func TestKeyMaintenanceIntervalRejectsNonPositive(t *testing.T) {
	// 与前三个周期配置同样的理由：time.NewTicker 在 d <= 0 时 panic，
	// 且 panic 在没有 recover 的 goroutine 里，会崩掉整个进程。
	env := map[string]string{"KEY_MAINTENANCE_INTERVAL": "0"}
	_, err := Load(func(k string) string { return env[k] })
	require.Error(t, err)
	require.Contains(t, err.Error(), "KEY_MAINTENANCE_INTERVAL")
}

func TestKeyMaintenanceIntervalAcceptsPositive(t *testing.T) {
	env := map[string]string{"KEY_MAINTENANCE_INTERVAL": "15s"}
	cfg, err := Load(func(k string) string { return env[k] })
	require.NoError(t, err)
	require.Equal(t, 15*time.Second, cfg.KeyMaintenanceInterval)
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
go test ./internal/config/ -run TestKeyMaintenance -count=1
```

Expected: FAIL —— `cfg.KeyMaintenanceInterval undefined`

- [ ] **Step 3: 加配置项**

`internal/config/config.go` 的 `Config` 结构体末尾加：

```go
	// KeyMaintenanceInterval 是密钥维护循环的周期：补做上游封禁、
	// 清理过期的旧凭据。
	KeyMaintenanceInterval time.Duration
```

在解析 `APPROVAL_WORKER_INTERVAL` 那段之后照抄同一形状加一段：

```go
	cfg.KeyMaintenanceInterval = time.Minute
	if raw := getenv("KEY_MAINTENANCE_INTERVAL"); raw != "" {
		d, err := time.ParseDuration(raw)
		if err != nil {
			return Config{}, fmt.Errorf(
				"KEY_MAINTENANCE_INTERVAL 不是合法的时间间隔（如 1m、30s）: %w", err)
		}
		if d <= 0 {
			return Config{}, fmt.Errorf(
				"KEY_MAINTENANCE_INTERVAL 必须为正数，当前为 %s", raw)
		}
		cfg.KeyMaintenanceInterval = d
	}
```

```bash
go test ./internal/config/ -count=1
```

Expected: ok

- [ ] **Step 4: 接线**

`internal/app/control.go` 里，`NewKeyAPI` 的调用改为四个参数：

```go
			KeyAPI:     control.NewKeyAPI(issuer, keyStore, orgs, resolver),
```

在既有的「清理滞留 pending」那个 goroutine 之后，追加维护循环：

```go
	// 密钥维护循环：补做上游封禁 + 清理过期的旧凭据。
	//
	// 上游封禁是兜底——批量吊销只做本地 UPDATE 就返回（上游没有批量封禁
	// 接口），单把吊销与离职对账内联失败时也留白给这里收敛。
	// 清理过期旧凭据纯属卫生：到期判断在 Edge 的 SQL 里，
	// 这个循环从不运行也不会留下一把还能用的旧密钥。
	go func() {
		ticker := time.NewTicker(cfg.KeyMaintenanceInterval)
		defer ticker.Stop()
		for {
			if n, err := issuer.BlockPendingUpstream(runCtx); err != nil {
				slog.Error("补做上游封禁失败，将在下轮重试", "err", err)
			} else if n > 0 {
				slog.Info("已补做上游封禁", "count", n)
			}
			if n, err := keyStore.RetireExpiredPrevKeys(runCtx, time.Now()); err != nil {
				slog.Error("清理过期旧凭据失败，将在下轮重试", "err", err)
			} else if n > 0 {
				slog.Info("已清理过期的轮换旧凭据", "count", n)
			}

			select {
			case <-runCtx.Done():
				return
			case <-ticker.C:
			}
		}
	}()
	slog.Info("密钥维护循环已启动", "interval", cfg.KeyMaintenanceInterval)
```

`.env.example` 的审批 worker 那段之后加：

```
# 密钥维护循环：补做上游封禁、清理过期的轮换旧凭据
KEY_MAINTENANCE_INTERVAL=1m
```

- [ ] **Step 5: 验证编译、lint 与全量测试**

```bash
go build ./... && go vet ./... && gofmt -l .
```

Expected: 无输出

```bash
make lint
```

Expected: `包边界检查通过`

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" make test
```

Expected: 全部包 `ok`

- [ ] **Step 6: 提交**

```bash
git add internal/config/ internal/app/control.go .env.example
```

```bash
git commit -m "feat: 密钥维护循环接线"
```

---

## Task 14: 端到端验收

**Files:**
- Create: `docs/superpowers/findings/2026-09-02-p1.3c-acceptance.md`

对着真实运行的 Postgres + LiteLLM + Edge 跑完设计文档 §8 的 11 项验收标准，**每一项都记录真实输出，不留占位符**。

> **操作纪律（P1.3b 验收时踩过的坑，务必遵守）**：手工 E2E 验收与自动化测试**不能共享同一个数据库实例同时进行**。`internal/control` 的 `cleanTables` 与 `internal/apikey` 的 `seed()` 都会无条件清表，会把手工搭的验收数据冲掉。一旦启动 control/edge 进程开始手工验收，就**不要再跑 `go test` / `make test`**，直到全部手工步骤记录完毕；`make test` 只在收尾时跑一次。

- [ ] **Step 1: 起栈并启动进程**

```bash
docker compose --env-file .env -f deploy/docker-compose.yml up -d postgres litellm
```

```bash
make build
```

启动 control（`KEY_MAINTENANCE_INTERVAL=10s` 便于观察扫描）：

```bash
env POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" CONTROL_LISTEN_ADDR=:8081 OIDC_ISSUER=http://localhost:8000 OIDC_CLIENT_ID=airlock OIDC_CLIENT_SECRET=airlock-dev-secret OIDC_REDIRECT_URL=http://localhost:8081/auth/callback AIRLOCK_BOOTSTRAP_ADMIN=admin@example.com AIRLOCK_ENCRYPTION_KEY=YWlybG9jay1kZXYtb25seS0zMmJ5dGUta2V5ISEhISE= LITELLM_BASE_URL=http://localhost:4000 LITELLM_MASTER_KEY=sk-airlock-master-dev-only KEY_MAINTENANCE_INTERVAL=10s nohup ./bin/airlock control > /tmp/airlock-p13c.log 2>&1 &
```

```bash
sleep 3 && cat /tmp/airlock-p13c.log
```

Expected: 日志含 `密钥维护循环已启动 interval=10s`

启动 edge（ClickHouse 的用户名/库名取自 `.env` 的 `CLICKHOUSE_USER`/`CLICKHOUSE_DB`，**不是** `default`）：

```bash
env POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" CLICKHOUSE_DSN="tcp://airlock:airlock_dev@localhost:9000?database=airlock" EDGE_LISTEN_ADDR=:8080 EDGE_UPSTREAM_BASE_URL=http://localhost:4000 AIRLOCK_ENCRYPTION_KEY=YWlybG9jay1kZXYtb25seS0zMmJ5dGUta2V5ISEhISE= nohup ./bin/airlock edge > /tmp/airlock-edge-p13c.log 2>&1 &
```

- [ ] **Step 2: 造验收夹具**

用 `python3 -c 'import hashlib; ...'` 算会话哈希（**不要手敲 sha256**，P1.2c 验收时手敲少一位字符排查了很久），并用 `SELECT length(id)` 确认是 64。

造一个 key holder 节点 `gw-p13c`、一个持 `platform_admin` 全局授予的管理员、一个普通成员，各配一个会话。

- [ ] **Step 3: 验收项 1、2 —— 轮换并用新凭据调用**

签发一把密钥拿到 `ak-` 原始明文，然后轮换：

```bash
curl -sS -X POST "http://localhost:8081/api/keys/<key-id>/rotate" -H "Cookie: airlock_session=<成员 token>" -H 'Content-Type: application/json' -d '{"window_seconds":3600}' | python3 -m json.tool
```

Expected: 200，响应体里 `key` 是一把新的 `ak-`，`id` 与轮换前**相同**。**记录实际输出。**

用新凭据经 Edge 调用：

```bash
curl -sS -X POST "http://localhost:8080/v1/chat/completions" -H "Authorization: Bearer <新 ak->" -H 'Content-Type: application/json' -d '{"model":"qwen-plus","messages":[{"role":"user","content":"hi"}],"max_tokens":5}' -w "\n%{http_code}\n"
```

Expected: 请求穿透到上游（若本机 LiteLLM 的 `qwen-plus` 未配供应商凭据，会在模型分发层报错——这与 Airlock 无关，用 `/key/info` 的 `last_active` 被更新来佐证鉴权已通过；P1.3b 验收记录里有同样的处置）。**记录实际输出。**

- [ ] **Step 4: 验收项 3 —— 窗口内旧凭据仍可用**

```bash
curl -sS -X POST "http://localhost:8080/v1/chat/completions" -H "Authorization: Bearer <旧 ak->" -H 'Content-Type: application/json' -d '{"model":"qwen-plus","messages":[{"role":"user","content":"hi"}],"max_tokens":5}' -w "\n%{http_code}\n"
```

Expected: 与新凭据**同样**的结果（即通过了鉴权），**且 edge 日志里出现 `客户端仍在使用轮换前的旧凭据`**：

```bash
grep "旧凭据" /tmp/airlock-edge-p13c.log
```

**记录实际输出。**

- [ ] **Step 5: 验收项 4 —— 窗口一过旧凭据当场失效（证明 D5）**

**这一步刻意不运行任何 worker**：把到期时间直接改到过去，立刻再试一次旧凭据。

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "UPDATE api_keys SET prev_key_expires_at = now() - interval '1 minute' WHERE id='<key-id>'"
```

```bash
curl -sS -X POST "http://localhost:8080/v1/chat/completions" -H "Authorization: Bearer <旧 ak->" -H 'Content-Type: application/json' -d '{"model":"qwen-plus","messages":[{"role":"user","content":"hi"}],"max_tokens":5}' -w "\n%{http_code}\n"
```

Expected: **401 `invalid_api_key`**，且此时 `prev_key_hash` 在库里**仍然存在**（清理 worker 还没跑），证明失效由 SQL 的到期判断保证、与后台任务无关：

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT prev_key_hash IS NOT NULL AS still_there FROM api_keys WHERE id='<key-id>'"
```

Expected: `still_there = t`。**记录两段输出——这是整个设计里最值得留证据的一条。**

- [ ] **Step 6: 验收项 5 —— 轮换前后上游是同一把密钥、预算连续**

解密 `upstream_key_enc`（用一个临时的 `main` 包，放在仓库内以便 import `internal/cryptobox`，用完删掉），对比轮换前后：

```bash
curl -sS "http://localhost:4000/key/info?key=<上游 sk->" -H "Authorization: Bearer sk-airlock-master-dev-only" | python3 -c 'import json,sys; d=json.load(sys.stdin)["info"]; print("max_budget =", d["max_budget"], "| spend =", d["spend"], "| key_alias =", d["key_alias"])'
```

Expected: `key_alias` 等于密钥 ID、`max_budget` 与 `spend` 与轮换前一致——证明 D1 保住了预算桶。**记录实际输出。**

- [ ] **Step 7: 验收项 6 —— 轮换已吊销的密钥被拒**

```bash
curl -sS -X POST "http://localhost:8081/api/keys/<已吊销 key-id>/rotate" -H "Cookie: airlock_session=<成员 token>" -H 'Content-Type: application/json' -d '{}' -w "\n%{http_code}\n"
```

Expected: **409 `key_not_active`**。**记录实际输出。**

- [ ] **Step 8: 验收项 7 —— 按子树吊销，同前缀兄弟毫发无伤**

造 `/root/rd` 与 `/root/rd2` 两个节点，各签一把密钥，然后：

```bash
curl -sS -X POST "http://localhost:8081/api/orgs/rd/keys/revoke" -H "Cookie: airlock_session=<管理员 token>" -w "\n%{http_code}\n"
```

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT org_id, status FROM api_keys WHERE org_id IN ('rd','rd2')"
```

Expected: `rd` 的密钥 `revoked`，`rd2` 的仍是 `active`。**记录实际输出。**

- [ ] **Step 9: 验收项 8 —— 全局紧急吊销的确认字符串**

```bash
curl -sS -X POST "http://localhost:8081/api/keys/revoke-all" -H "Cookie: airlock_session=<管理员 token>" -H 'Content-Type: application/json' -d '{}' -w "\n%{http_code}\n"
```

Expected: **400 `confirm_required`**。

```bash
curl -sS -X POST "http://localhost:8081/api/keys/revoke-all" -H "Cookie: airlock_session=<管理员 token>" -H 'Content-Type: application/json' -d '{"confirm":"REVOKE ALL KEYS"}' -w "\n%{http_code}\n"
```

Expected: 200，响应体 `{"revoked":N}`，且库里再无 `active`/`pending` 密钥。**记录两段输出。**

另外确认无 `key:revoke_all` 的普通成员调用同一端点得到 **403**。

- [ ] **Step 10: 验收项 9 —— 兜底扫描把上游逐一封禁**

等一轮维护循环（`KEY_MAINTENANCE_INTERVAL=10s`）：

```bash
sleep 12 && docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT count(*) FILTER (WHERE upstream_blocked_at IS NOT NULL) AS stamped, count(*) AS total FROM api_keys WHERE status='revoked'"
```

Expected: `stamped = total`。

任取一把，从上游侧取证（探针 P4）：

```bash
curl -sS "http://localhost:4000/key/info?key=<某把上游 sk->" -H "Authorization: Bearer sk-airlock-master-dev-only" | python3 -c 'import json,sys; print("blocked =", json.load(sys.stdin)["info"]["blocked"])'
```

Expected: `blocked = True`。**记录实际输出。**

- [ ] **Step 11: 验收项 10 —— 收敛历史上封禁失败的密钥**

模拟一把「本地已 revoked、上游仍活着」的历史遗留：

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "UPDATE api_keys SET upstream_blocked_at = NULL, upstream_block_attempts = 0 WHERE id='<某把 key-id>'"
```

```bash
curl -sS -X POST "http://localhost:4000/key/unblock" -H "Authorization: Bearer sk-airlock-master-dev-only" -H 'Content-Type: application/json' -d '{"key":"<对应上游 sk->"}' -o /dev/null -w "  HTTP=%{http_code}\n"
```

等一轮扫描后确认它被重新封禁、且盖上了戳：

```bash
sleep 12 && curl -sS "http://localhost:4000/key/info?key=<对应上游 sk->" -H "Authorization: Bearer sk-airlock-master-dev-only" | python3 -c 'import json,sys; print("blocked =", json.load(sys.stdin)["info"]["blocked"])'
```

Expected: `blocked = True`——证明本阶段补上的那个既有缺口（封禁失败永不重试）确实收敛了。**记录实际输出。**

- [ ] **Step 12: 验收项 11 —— Edge 零回归**

先停掉两个进程，再跑测试（见本任务开头的操作纪律）：

```bash
pkill -f "bin/airlock control"; pkill -f "bin/airlock edge"
```

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- PASS'
```

Expected: `33`

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- FAIL'
```

Expected: `0`

- [ ] **Step 13: 收尾**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" make test
```

```bash
make lint && gofmt -l . && git status --short
```

Expected: 全部测试 PASS，lint 通过，工作区干净（**不要** `docker compose down`，栈留着）。

- [ ] **Step 14: 写验收记录**

创建 `docs/superpowers/findings/2026-09-02-p1.3c-acceptance.md`，结构照 `2026-09-01-p1.3b-acceptance.md`：

- 表头（日期、验收人、设计文档链接、实施计划链接）
- 11 项验收标准的结果表
- 每一项的真实输出
- 验收过程中发现的问题（如有）及处置

**每个单元格填真实数据，不留占位符。**

- [ ] **Step 15: 提交**

```bash
git add docs/superpowers/findings/2026-09-02-p1.3c-acceptance.md
```

```bash
git commit -m "docs: P1.3c 端到端验收记录"
```

---

## 完成标准

- [ ] `make test` 全绿，含 `-race`
- [ ] `make lint` 通过，`gofmt -l .` 无输出
- [ ] 路由机械检查覆盖 3 条新端点；`authz` 权限总数 14
- [ ] 验收记录 11 项全部通过，且无占位符
- [ ] **窗口一过旧凭据当场失效，且当时 `prev_key_hash` 仍在库里**（D5 的硬证据）
- [ ] **轮换前后 `/key/info` 是同一把上游密钥、`spend` 连续**（D1 的硬证据）
- [ ] 同前缀兄弟节点在子树吊销中毫发无伤
- [ ] 兜底扫描把「本地 revoked、上游仍活」的密钥收敛掉
- [ ] `internal/edge` 33 PASS / 0 FAIL
- [ ] 所有代码已提交，工作区干净

---

## 自查记录

对照设计文档的覆盖情况：

| 设计文档章节 | 对应任务 |
|---|---|
| §1 范围（轮换 + 批量/紧急吊销 + 封禁兜底） | Task 3–13 |
| §1 范围外（定时轮换、`sk-` 轮换、ID 列表吊销、吊销通知、Key 扩展属性、钉钉/企微） | 全程无对应代码，`Rotate` 只有手动入口 |
| §2 探针 P1（404） | Task 8 的 `isUpstreamNotFound` 与 `TestBlockPendingUpstreamTreats404AsDone` |
| §2 探针 P2（幂等） | Task 8 的 `TestBlockPendingUpstreamIsIdempotentAcrossRuns` |
| §2 探针 P3（无批量接口） | Task 11 的本地 UPDATE 即返回 + Task 8 的扫描 |
| §2 探针 P4（`blocked` 可见） | Task 14 Step 10 的上游取证 |
| §3 D1 一行两哈希 | Task 2（列）、Task 3（Rotate）、Task 4（保上游密钥不变的测试） |
| §3 D2 只轮换 `ak-` | Task 4 的 `TestRotateReturnsNewPlaintextAndKeepsUpstreamKey` |
| §3 D3 零上游调用 | Task 4 的 `require.Empty(t, admin.callsSnapshot())` |
| §3 D4 本地吊销与上游封禁分离 | Task 8、9、11 |
| §3 D5 到期判断在 SQL 里 | Task 5 的 `TestPostgresStoreRejectsPrevKeyAfterWindow` + Task 14 Step 5 |
| §3 D6 不发吊销通知 | 全程无 `notifications` 相关改动 |
| §3 D7 单开全局权限、不可逆、确认字符串 | Task 1、Task 11、Task 12 |
| §4 数据模型 | Task 2 |
| §5.1 轮换接口与窗口默认值/上限 | Task 3、4 |
| §5.2 Edge 读取与可观测性 | Task 5 |
| §5.3 批量与紧急吊销 | Task 6、11、12 |
| §5.4 兜底扫描 | Task 7、8、13 |
| §6 错误处理表 | Task 10（409/400/403/404）、Task 8（上游失败与 404） |
| §7 测试策略 | 各任务的测试步骤 |
| §8 验收标准 11 项 | Task 14 |

**规模**：14 个任务，设计文档预估 11–14，落在区间上限。

**计划撰写期间发现、设计文档未写明而已在计划里定死的事项**：

1. **`NewKeyAPI` 要多接一个 `OrgStore`**：按子树吊销必须先把节点 ID 换成物化路径，而 `KeyAPI` 原本拿不到 `OrgStore`。签名改动影响两处调用点（`keyAPIFixture`、`internal/app/control.go`），已分别落在 Task 11 与 Task 13。
2. **`KeyStore` 是接口且 `NewPostgresKeyStore` 返回接口**，因此每个任务必须同时加接口方法与实现，包才编译得过。任务因此按「一组方法 + 其测试」切分，而不是「先加全部接口再逐个实现」。
3. **`litellm.APIError` 的字段是 `Status`/`Body`**（不是 `Message`），Task 8 的 fake 与 `isUpstreamNotFound` 都按此写。
4. **既有 `revoked` 行会被扫描重新封禁一遍**：迁移后它们的 `upstream_blocked_at` 为 NULL。这是期望行为（它们可能正是当初封禁失败的那些），探针 P1/P2 保证了这一遍是安全的。已写进 Task 2 的迁移说明。
5. **验收的操作纪律**：手工 E2E 期间绝不能跑 `go test`（P1.3b 验收时被自己的测试清表冲掉过一次）。已写在 Task 14 开头。
