# P1.4c 角色与权限页 + 组织页补齐 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补上 P1.4 最后一块「后端齐全、界面完全缺失」的能力——授权：新增「角色与权限」页（回答「谁对这个节点有权」），并把组织页的改名、移动、密钥边界与成员管理补齐。

**Architecture:** 后端补三处（有效权限视图、可授角色过滤、用户列表），前端新增一个页面与四个组件。有效权限视图把祖先链从物化路径切成 ID 段后逐个精确比较，全程不做字符串前缀匹配——与 `authz.Resolver.ancestorChain` 同一手法。

**Tech Stack:** Go 1.25（`net/http` + pgx）、Next.js 15 静态导出 + Ant Design 5、Vitest + React Testing Library、Postgres。

**设计文档:** `docs/superpowers/specs/2026-09-03-airlock-p1.4c-grants-and-org-completion-design.md`

---

## 与设计文档的一处出入（撰写计划时查证后修正）

设计文档 §4 B3 写的是 `GET /api/users` 用「`grant:read`（全局目标）」把门。**这是错的**：`grant:read` 在 `permission.go:62` 里是 `ScopeOrg` 权限，组织管理员是在**节点**上持有它的；用全局目标判定会把他们全部挡在门外，而他们正是这两个页面的主要用户。

改为「**在任意位置持有 `grant:read`**」——路由声明为 `AccessAuthenticated`，判定下沉到处理器，复用 P1.4a 已有的 `holdsAnywhere`（`workbench.go:58`）。这与 P1.4a D2 是同一个教训的第二次应用：判断「某人是否在某处持有某权限」不能用全局视角去问。

---

## 文件结构

**后端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `internal/control/model.go` | `EffectiveGrant` 类型、`RBACStore` 接口 | 改 |
| `internal/control/grantstore.go` | `ListEffectiveGrantsForOrg` 实现 | 改 |
| `internal/control/grantstore_test.go` | store 测试（含第六次同前缀兄弟） | 改 |
| `internal/control/grantapi.go` | 三个处理器：有效授予、可授角色过滤、用户列表 | 改 |
| `internal/control/grantapi_test.go` | 处理器测试 | 改 |
| `internal/control/fakerbac_test.go` | fake 补 `ListEffectiveGrantsForOrg` | 改 |
| `internal/control/routes.go` | 两条新路由 | 改 |
| `internal/control/routes_test.go` | 覆盖清单 | 改 |

**前端**

| 文件 | 职责 | 动作 |
|---|---|---|
| `web/src/lib/types.ts` | 加 `Role`、`EffectiveGrant` | 改 |
| `web/src/lib/users.ts` + `.test.ts` | `userLabel`、`byID`（纯函数） | 新建 |
| `web/src/components/GrantsTable.tsx` + `.test.tsx` | 有效权限表；**撤销按钮的可见性规则在这里** | 新建 |
| `web/src/components/GrantModal.tsx` | 授予角色弹窗 | 新建 |
| `web/src/components/OrgNodeProps.tsx` | 节点属性：改名 / 移动 / 密钥边界 | 新建 |
| `web/src/components/OrgMembers.tsx` | 该节点成员：列表 + 加入 / 移出 | 新建 |
| `web/src/app/platform/grants/page.tsx` | 角色与权限页 | 新建 |
| `web/src/app/platform/orgs/page.tsx` | 接入上面两个组织组件 | 改 |
| `web/src/components/workbenches.ts` | 导航加「角色与权限」 | 改 |

**为什么这么切**：`GrantsTable` 单独成文件是因为「只有直授行可撤销」是安全相关规则，要能被一条测试单独钉住；`OrgNodeProps` 与 `OrgMembers` 各自封装一段完整交互（含自己的请求与错误处理），塞进组织页会让那个文件失控。

---

## Task 1: `EffectiveGrant` 与 `ListEffectiveGrantsForOrg`

**Files:**
- Modify: `internal/control/model.go`
- Modify: `internal/control/grantstore.go`
- Test: `internal/control/grantstore_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/grantstore_test.go` 末尾追加：

```go
func seedGrantOrg(t *testing.T, db *sql.DB, id, path string) {
	t.Helper()
	_, err := db.Exec(
		`INSERT INTO organizations (id, name, path) VALUES ($1,$1,$2)`, id, path)
	require.NoError(t, err)
}

func seedGrantRow(t *testing.T, db *sql.DB, id, userID, roleID string, orgID *string) {
	t.Helper()
	_, err := db.Exec(
		`INSERT INTO role_grants (id, user_id, role_id, org_id) VALUES ($1,$2,$3,$4)`,
		id, userID, roleID, orgID)
	require.NoError(t, err)
}

func TestListEffectiveGrantsCoversThreeSources(t *testing.T) {
	db := testDB(t)
	cleanTables(t, db)
	s := NewPostgresRBACStore(db)
	require.NoError(t, s.SyncBuiltinRoles(context.Background()))
	ctx := context.Background()

	uid := seedUserID(t, db, "eff-user")
	seedGrantOrg(t, db, "root", "/root")
	seedGrantOrg(t, db, "rd", "/root/rd")

	root := "root"
	rd := "rd"
	seedGrantRow(t, db, "g-direct", uid, "org_admin", &rd)
	seedGrantRow(t, db, "g-inherited", uid, "auditor", &root)
	seedGrantRow(t, db, "g-global", uid, "platform_admin", nil)

	got, err := s.ListEffectiveGrantsForOrg(ctx, "rd")
	require.NoError(t, err)

	bySource := map[string]string{}
	for _, g := range got {
		bySource[g.Source] = g.ID
	}
	require.Equal(t, "g-direct", bySource[GrantSourceDirect])
	require.Equal(t, "g-inherited", bySource[GrantSourceInherited], "祖先节点的授予要算进来")
	require.Equal(t, "g-global", bySource[GrantSourceGlobal], "全局授予对任何节点都算数")
	require.Len(t, got, 3)
}

func TestListEffectiveGrantsSpareSiblingWithSamePrefix(t *testing.T) {
	// 第六次遇到这个陷阱（P1.2b 权限判定、P1.3b 审批人查找、P1.3c 子树吊销、
	// P1.4a 待审列表、P1.4b 子树密钥查询各踩过一次）：
	// /root/rd 是 /root/rd2 的字符串前缀，但 rd2 既不是 rd 的祖先也不是后代。
	//
	// 这条尤其要紧：权限页多列一个人，管理员会以为那个人有权；
	// 而他其实管的是隔壁部门。
	db := testDB(t)
	cleanTables(t, db)
	s := NewPostgresRBACStore(db)
	require.NoError(t, s.SyncBuiltinRoles(context.Background()))
	ctx := context.Background()

	uid := seedUserID(t, db, "eff-sibling-user")
	seedGrantOrg(t, db, "root", "/root")
	seedGrantOrg(t, db, "rd", "/root/rd")
	seedGrantOrg(t, db, "rd2", "/root/rd2")

	rd := "rd"
	rd2 := "rd2"
	seedGrantRow(t, db, "g-rd", uid, "org_admin", &rd)
	seedGrantRow(t, db, "g-rd2", uid, "org_admin", &rd2)

	got, err := s.ListEffectiveGrantsForOrg(ctx, "rd")
	require.NoError(t, err)
	require.Len(t, got, 1)
	require.Equal(t, "g-rd", got[0].ID, "同前缀兄弟节点的授予不能混进来")
}

func TestListEffectiveGrantsUnknownOrgIsNotFound(t *testing.T) {
	db := testDB(t)
	cleanTables(t, db)
	s := NewPostgresRBACStore(db)

	_, err := s.ListEffectiveGrantsForOrg(context.Background(), "nope")
	require.ErrorIs(t, err, ErrOrgNotFound)
}

func TestListEffectiveGrantsEmptyReturnsEmptySlice(t *testing.T) {
	db := testDB(t)
	cleanTables(t, db)
	s := NewPostgresRBACStore(db)
	seedGrantOrg(t, db, "lonely", "/lonely")

	got, err := s.ListEffectiveGrantsForOrg(context.Background(), "lonely")
	require.NoError(t, err)
	require.NotNil(t, got, "空结果要是空切片，否则 JSON 会序列化成 null")
	require.Empty(t, got)
}
```

若 `grantstore_test.go` 尚未 import `database/sql`，在 import 块里补上。

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListEffectiveGrants
```

Expected: 编译失败 —— `s.ListEffectiveGrantsForOrg undefined`、`GrantSourceDirect undefined`

- [ ] **Step 3: 加类型与接口方法**

`internal/control/model.go`，在 `Role` 结构体**之前**插入：

```go
// 有效授予的来源。
const (
	GrantSourceDirect    = "direct"    // 直接挂在这个节点上
	GrantSourceInherited = "inherited" // 挂在祖先节点上，对本节点同样生效
	GrantSourceGlobal    = "global"    // 全局授予，对任何节点都生效
)

// EffectiveGrant 是「谁对某个节点有权」的一行。
//
// 与 RoleGrant 的区别在于它回答的是**有效**权限：除了本节点直授，
// 还含祖先节点的授予与全局授予。RoleGrant.OrgID 本身就是来源节点，
// 因此不需要额外的 SourceOrgID 字段。
type EffectiveGrant struct {
	RoleGrant
	Source string
}
```

`RBACStore` 接口里，在 `ListGlobalGrants` 那条**之后**加：

```go
	// ListEffectiveGrantsForOrg 返回对该节点有效的全部授予：
	// 本节点直授 + 祖先节点授予 + 全局授予，每条标注 Source。
	// 节点不存在时返回 ErrOrgNotFound。
	ListEffectiveGrantsForOrg(ctx context.Context, orgID string) ([]EffectiveGrant, error)
```

- [ ] **Step 4: 写实现**

`internal/control/grantstore.go`，在 `ListGlobalGrants` 函数**之后**加：

```go
// ListEffectiveGrantsForOrg 返回对该节点有效的全部授予。
//
// 祖先链直接从物化路径切出来，与 authz.Resolver.ancestorChain 同一手法：
// 切成 ID 段后逐个精确比较，全程不做字符串前缀匹配。这样在结构上就不可能
// 把 /root/rd2 误当成 /root/rd 的祖先——权限页多列一个人，管理员就会以为
// 那个人有权，而他其实管的是隔壁部门。
func (s *postgresRBACStore) ListEffectiveGrantsForOrg(
	ctx context.Context, orgID string,
) ([]EffectiveGrant, error) {
	var path string
	err := s.db.QueryRowContext(ctx,
		`SELECT path FROM organizations WHERE id = $1`, orgID).Scan(&path)
	if errors.Is(err, sql.ErrNoRows) {
		return nil, ErrOrgNotFound
	}
	if err != nil {
		return nil, fmt.Errorf("查询节点路径失败: %w", err)
	}

	chain := strings.Split(strings.Trim(path, "/"), "/")

	rows, err := s.db.QueryContext(ctx,
		`SELECT `+grantColumns+` FROM role_grants
		 WHERE org_id = ANY($1) OR org_id IS NULL
		 ORDER BY created_at`, chain)
	if err != nil {
		return nil, fmt.Errorf("查询有效授予失败: %w", err)
	}
	defer func() { _ = rows.Close() }()

	grants, err := scanGrants(rows)
	if err != nil {
		return nil, err
	}

	out := make([]EffectiveGrant, 0, len(grants))
	for _, g := range grants {
		switch {
		case g.OrgID == nil:
			out = append(out, EffectiveGrant{RoleGrant: g, Source: GrantSourceGlobal})
		case *g.OrgID == orgID:
			out = append(out, EffectiveGrant{RoleGrant: g, Source: GrantSourceDirect})
		default:
			out = append(out, EffectiveGrant{RoleGrant: g, Source: GrantSourceInherited})
		}
	}
	return out, nil
}
```

在 `grantstore.go` 的 import 块里补上 `"strings"`。

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListEffectiveGrants -v
```

Expected: 4 个测试全部 PASS

- [ ] **Step 6: 提交**

```bash
git add internal/control/model.go internal/control/grantstore.go internal/control/grantstore_test.go
```

```bash
git commit -m "feat: 按节点查询有效授予，祖先链从物化路径精确切段"
```

---

## Task 2: `GET /api/orgs/{id}/effective-grants`

**Files:**
- Modify: `internal/control/grantapi.go`
- Modify: `internal/control/fakerbac_test.go`
- Modify: `internal/control/routes.go`, `internal/control/routes_test.go`
- Test: `internal/control/grantapi_test.go`

- [ ] **Step 1: 给 fake 补上新方法**

`internal/control/fakerbac_test.go`，在 `ListGlobalGrants` 之后加。**分类逻辑不在 fake 里重做**——它活在真实 store 里、由 Task 1 对着真实 Postgres 测过；这里只回放预置数据，让处理器测试专注于处理器自己的职责：

```go
// effective 由测试预置。分类逻辑活在真实 store 里（见 Task 1 的测试），
// fake 不重做一遍——重做的 fake 迟早会和真实实现说不同的话。
func (f *fakeRBACStore) ListEffectiveGrantsForOrg(
	_ context.Context, orgID string,
) ([]EffectiveGrant, error) {
	f.mu.Lock()
	defer f.mu.Unlock()
	if f.effectiveErr != nil {
		return nil, f.effectiveErr
	}
	return append([]EffectiveGrant{}, f.effective[orgID]...), nil
}

func (f *fakeRBACStore) setEffective(orgID string, list []EffectiveGrant) {
	f.mu.Lock()
	defer f.mu.Unlock()
	if f.effective == nil {
		f.effective = map[string][]EffectiveGrant{}
	}
	f.effective[orgID] = list
}
```

在 `fakeRBACStore` 结构体里加两个字段：

```go
	effective    map[string][]EffectiveGrant
	effectiveErr error
```

（若结构体没有 `mu sync.Mutex` 字段，去掉上面的加锁两行；照该文件既有写法为准。）

- [ ] **Step 2: 写失败测试**

在 `internal/control/grantapi_test.go` 末尾追加：

```go
func TestEffectiveGrantsViewCarriesSource(t *testing.T) {
	api, _, rbac := grantFixture(t)
	root := "root"
	rd := "rd"
	rbac.setEffective("rd", []EffectiveGrant{
		{RoleGrant: RoleGrant{ID: "g1", UserID: "u1", RoleID: "org_admin", OrgID: &rd},
			Source: GrantSourceDirect},
		{RoleGrant: RoleGrant{ID: "g2", UserID: "u2", RoleID: "auditor", OrgID: &root},
			Source: GrantSourceInherited},
		{RoleGrant: RoleGrant{ID: "g3", UserID: "u3", RoleID: "platform_admin", OrgID: nil},
			Source: GrantSourceGlobal},
	})

	req := asUser(httptest.NewRequest(http.MethodGet, "/api/orgs/rd/effective-grants", nil),
		&User{ID: "admin", Status: UserStatusActive})
	req.SetPathValue("id", "rd")
	rec := httptest.NewRecorder()
	api.HandleEffectiveGrants(rec, req)

	require.Equal(t, http.StatusOK, rec.Code)
	var got []struct {
		ID          string  `json:"id"`
		Source      string  `json:"source"`
		SourceOrgID *string `json:"source_org_id"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &got))
	require.Len(t, got, 3)
	require.Equal(t, "direct", got[0].Source)
	require.Equal(t, "rd", *got[0].SourceOrgID)
	require.Equal(t, "inherited", got[1].Source)
	require.Equal(t, "root", *got[1].SourceOrgID, "继承行要指出授予实际挂在哪个节点")
	require.Equal(t, "global", got[2].Source)
	require.Nil(t, got[2].SourceOrgID, "全局授予没有来源节点")
}

func TestEffectiveGrantsUnknownOrgIs404(t *testing.T) {
	api, _, rbac := grantFixture(t)
	rbac.effectiveErr = ErrOrgNotFound

	req := asUser(httptest.NewRequest(http.MethodGet, "/api/orgs/nope/effective-grants", nil),
		&User{ID: "admin", Status: UserStatusActive})
	req.SetPathValue("id", "nope")
	rec := httptest.NewRecorder()
	api.HandleEffectiveGrants(rec, req)

	require.Equal(t, http.StatusNotFound, rec.Code)
	require.Contains(t, rec.Body.String(), "org_not_found")
}
```

- [ ] **Step 3: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestEffectiveGrants
```

Expected: 编译失败 —— `api.HandleEffectiveGrants undefined`

- [ ] **Step 4: 写处理器**

`internal/control/grantapi.go`，在 `HandleListGrants` **之后**加：

```go
// effectiveGrantView 是有效权限视图的一行。
//
// source_org_id 就是授予实际挂在哪个节点：direct 时等于被查询的节点，
// inherited 时是某个祖先，global 时为空。
type effectiveGrantView struct {
	ID          string    `json:"id"`
	UserID      string    `json:"user_id"`
	RoleID      string    `json:"role_id"`
	Source      string    `json:"source"`
	SourceOrgID *string   `json:"source_org_id"`
	CreatedAt   time.Time `json:"created_at"`
}

// HandleEffectiveGrants 回答「谁对这个节点有权」。
//
// 与 HandleListGrants 的区别是这一条把三类授予合起来：本节点直授、
// 继承自祖先节点、全局。只答直授的页面会系统性地少告诉管理员一批人，
// 而那批人恰恰权限最大——祖先上的 org_admin 与全局 platform_admin
// 对这个节点都有完全权限。
func (a *GrantAPI) HandleEffectiveGrants(w http.ResponseWriter, r *http.Request) {
	list, err := a.rbac.ListEffectiveGrantsForOrg(r.Context(), r.PathValue("id"))
	if err != nil {
		if errors.Is(err, ErrOrgNotFound) {
			writeError(w, http.StatusNotFound, "org_not_found", "组织节点不存在")
			return
		}
		writeError(w, http.StatusInternalServerError, "internal_error", "查询有效授予失败")
		return
	}

	out := make([]effectiveGrantView, 0, len(list))
	for _, g := range list {
		out = append(out, effectiveGrantView{
			ID: g.ID, UserID: g.UserID, RoleID: g.RoleID,
			Source: g.Source, SourceOrgID: g.OrgID, CreatedAt: g.CreatedAt,
		})
	}
	writeJSON(w, http.StatusOK, out)
}
```

在 `grantapi.go` 的 import 块里补上 `"time"`。

- [ ] **Step 5: 注册路由**

`internal/control/routes.go`，在 `GET /api/orgs/{id}/grants` 那一条**之后**插入：

```go
		{
			// 「谁对这个节点有权」——直授 + 继承 + 全局。与上面那条只答
			// 直授的路由是两个问题，返回形状也不同，因此单开一条路由
			// 而不是给它加查询参数。
			Pattern: "GET /api/orgs/{id}/effective-grants", Access: AccessPermission,
			Permission: authz.PermGrantRead, Target: TargetFromPath("id"),
			Handler: grantH(func(g *GrantAPI) http.HandlerFunc { return g.HandleEffectiveGrants }),
		},
```

`internal/control/routes_test.go`，在覆盖清单里 `"GET /api/orgs/{id}/grants",` 之后加：

```go
		"GET /api/orgs/{id}/effective-grants",
```

- [ ] **Step 6: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run 'TestEffectiveGrants|TestRoute|TestHandler' -v
```

Expected: 全部 PASS，其中 `TestHandlerDoesNotPanicWithConsoleRoutes` 确认新路由没有和既有 pattern 冲突（P1.4a 踩过 `ServeMux` 注册期 panic）

- [ ] **Step 7: 提交**

```bash
git add internal/control/grantapi.go internal/control/grantapi_test.go internal/control/fakerbac_test.go internal/control/routes.go internal/control/routes_test.go
```

```bash
git commit -m "feat: 有效权限视图接口"
```

---

## Task 3: `GET /api/roles?grantable_at=`

**Files:**
- Modify: `internal/control/grantapi.go`
- Test: `internal/control/grantapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/grantapi_test.go` 末尾追加：

```go
func rolesReq(t *testing.T, api *GrantAPI, u *User, query string) *httptest.ResponseRecorder {
	t.Helper()
	req := asUser(httptest.NewRequest(http.MethodGet, "/api/roles"+query, nil), u)
	rec := httptest.NewRecorder()
	api.HandleListRoles(rec, req)
	return rec
}

func roleIDs(t *testing.T, rec *httptest.ResponseRecorder) []string {
	t.Helper()
	var roles []struct {
		ID string `json:"ID"`
	}
	require.NoError(t, json.Unmarshal(rec.Body.Bytes(), &roles))
	out := []string{}
	for _, r := range roles {
		out = append(out, r.ID)
	}
	return out
}

func TestListRolesGrantableAtFiltersByAntiEscalation(t *testing.T) {
	// 只在 rd 上持 org_admin 的人，不能把 platform_admin 授给别人——
	// 那会让他间接拿到自己没有的全局权限。下拉里就不该出现这个选项。
	api, _, rbac := grantFixture(t)
	_ = rbac.CreateGrant(context.Background(), RoleGrant{
		ID: "g-orgadmin", UserID: "boss", RoleID: authz.RoleOrgAdmin, OrgID: strptr("rd"),
	})
	boss := &User{ID: "boss", Status: UserStatusActive}

	all := roleIDs(t, rolesReq(t, api, boss, ""))
	require.Contains(t, all, authz.RolePlatformAdmin, "不带参数时行为不变：返回全部角色")

	grantable := roleIDs(t, rolesReq(t, api, boss, "?grantable_at=rd"))
	require.NotContains(t, grantable, authz.RolePlatformAdmin,
		"授不了的角色不能出现在下拉里")
	require.Contains(t, grantable, authz.RoleOrgAdmin, "自己持有的角色可以往下授")
}

func TestListRolesGrantableAtForPlatformAdminKeepsEverything(t *testing.T) {
	api, _, rbac := grantFixture(t)
	_ = rbac.CreateGrant(context.Background(), RoleGrant{
		ID: "g-platform", UserID: "root", RoleID: authz.RolePlatformAdmin,
	})

	grantable := roleIDs(t, rolesReq(t,
		api, &User{ID: "root", Status: UserStatusActive}, "?grantable_at=rd"))
	require.Contains(t, grantable, authz.RolePlatformAdmin)
}
```

若 `grantapi_test.go` 里还没有 `strptr` 辅助函数，在文件末尾加：

```go
func strptr(s string) *string { return &s }
```

（若已存在同名函数，跳过这一步。）

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListRolesGrantableAt
```

Expected: FAIL —— `grantable` 里仍含 `platform_admin`（参数还没被识别）

- [ ] **Step 3: 写实现**

`internal/control/grantapi.go`，把 `HandleListRoles` 整个替换为：

```go
// HandleListRoles 列出角色。任何已登录用户都能看——
// 知道系统里有哪些角色不构成信息泄漏，而授予界面需要它。
//
// 带 ?grantable_at=<orgID> 时只返回调用者在该节点**确实能授予**的角色。
// 这个判定必须留在服务端：GET /api/roles 不返回权限集，前端算不出
// 「角色的权限是否为我已持有权限的子集」，让它算等于把授权判定复制一份
// 到前端（P1.4a D2 否决过同一条路）。
func (a *GrantAPI) HandleListRoles(w http.ResponseWriter, r *http.Request) {
	roles, err := a.rbac.ListRoles(r.Context())
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "查询角色失败")
		return
	}

	if at := r.URL.Query().Get("grantable_at"); at != "" {
		u, ok := UserFromContext(r.Context())
		if !ok {
			writeError(w, http.StatusInternalServerError, "internal_error", "上下文缺少用户")
			return
		}
		filtered := make([]Role, 0, len(roles))
		for _, role := range roles {
			ok, err := a.resolver.CanGrant(r.Context(), subjectOf(u), role.ID, &at)
			if err != nil {
				if errors.Is(err, authz.ErrOrgNotFound) {
					writeError(w, http.StatusNotFound, "org_not_found", "组织节点不存在")
					return
				}
				writeError(w, http.StatusInternalServerError, "internal_error", "权限判定失败")
				return
			}
			if ok {
				filtered = append(filtered, role)
			}
		}
		roles = filtered
	}

	writeJSON(w, http.StatusOK, roles)
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListRoles -v
```

Expected: 全部 PASS，含既有的 `TestListRolesReturnsAllBuiltins`（不带参数行为不变）

- [ ] **Step 5: 提交**

```bash
git add internal/control/grantapi.go internal/control/grantapi_test.go
```

```bash
git commit -m "feat: 角色列表可按节点过滤出可授予的角色"
```

---

## Task 4: `GET /api/users`

**Files:**
- Modify: `internal/control/grantapi.go`, `internal/control/routes.go`, `internal/control/routes_test.go`
- Test: `internal/control/grantapi_test.go`

- [ ] **Step 1: 写失败测试**

在 `internal/control/grantapi_test.go` 末尾追加：

```go
func TestListUsersNeedsGrantReadSomewhere(t *testing.T) {
	// 门槛是「在任意位置持有 grant:read」而不是全局 grant:read：
	// grant:read 是 ScopeOrg 权限，组织管理员是在节点上持有它的，
	// 用全局视角判定会把这两个页面的主要用户全部挡在门外。
	api, users, rbac := grantFixture(t)
	addUser(t, users, "someone", "someone@x.com")

	nobody := &User{ID: "nobody", Status: UserStatusActive}
	req := asUser(httptest.NewRequest(http.MethodGet, "/api/users", nil), nobody)
	rec := httptest.NewRecorder()
	api.HandleListUsers(rec, req)
	require.Equal(t, http.StatusForbidden, rec.Code, "一条授予都没有的人看不到通讯录")

	// 只在 rd 这一个节点上持 org_admin（含 grant:read）就够了。
	_ = rbac.CreateGrant(context.Background(), RoleGrant{
		ID: "g-rd-admin", UserID: "boss", RoleID: authz.RoleOrgAdmin, OrgID: strptr("rd"),
	})
	req2 := asUser(httptest.NewRequest(http.MethodGet, "/api/users", nil),
		&User{ID: "boss", Status: UserStatusActive})
	rec2 := httptest.NewRecorder()
	api.HandleListUsers(rec2, req2)

	require.Equal(t, http.StatusOK, rec2.Code)
	var got []struct {
		ID    string `json:"ID"`
		Email string `json:"Email"`
	}
	require.NoError(t, json.Unmarshal(rec2.Body.Bytes(), &got))
	require.NotEmpty(t, got, "节点级的 grant:read 就足以拿到用户列表")
}
```

- [ ] **Step 2: 运行测试确认失败**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -run TestListUsers
```

Expected: 编译失败 —— `api.HandleListUsers undefined`

- [ ] **Step 3: 写处理器**

`internal/control/grantapi.go`，在 `HandleListRoles` **之后**加：

```go
// HandleListUsers 列出活跃用户。
//
// 两个页面都要它：角色与权限页拿它把 user_id 还原成人名、并在授予时选人；
// 组织与成员页拿它按 primary_org_id 过滤出该节点的成员。
//
// 门槛是「在任意位置持有 grant:read」而不是全局 grant:read——后者会把只在
// 某个子树上持 org_admin 的人挡在外面，而他们正是这两个页面的主要用户。
// 用 Scopes 判定，与 P1.4a 的工作台可见性同一手法。
//
// 不按调用者的可见子树裁剪是有意识的选择：要把某人授权到自己的子树上，
// 那个人此刻多半还不在你的子树里，甚至没有归属节点。企业内部控制台向
// 「有权管理授权的人」展示员工名录是正常的。
func (a *GrantAPI) HandleListUsers(w http.ResponseWriter, r *http.Request) {
	u, ok := UserFromContext(r.Context())
	if !ok {
		writeError(w, http.StatusInternalServerError, "internal_error", "上下文缺少用户")
		return
	}
	allowed, err := holdsAnywhere(r.Context(), a.resolver, subjectOf(u),
		[]string{authz.PermGrantRead})
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "权限判定失败")
		return
	}
	if !allowed {
		writeError(w, http.StatusForbidden, "permission_denied", "没有查看用户列表的权限")
		return
	}

	list, err := a.users.ListActive(r.Context())
	if err != nil {
		writeError(w, http.StatusInternalServerError, "internal_error", "查询用户失败")
		return
	}
	writeJSON(w, http.StatusOK, list)
}
```

- [ ] **Step 4: 注册路由**

`internal/control/routes.go`，在 `PUT /api/users/{id}/primary-org` 那一条**之前**插入：

```go
		{
			// 判定下沉到处理器：门槛是「在任意位置持有 grant:read」，
			// 而 grant:read 是 ScopeOrg 权限，中间件的全局目标表达不了这个。
			Pattern: "GET /api/users", Access: AccessAuthenticated,
			Handler: grantH(func(g *GrantAPI) http.HandlerFunc { return g.HandleListUsers }),
		},
```

`internal/control/routes_test.go`，在覆盖清单里 `"PUT /api/users/{id}/primary-org",` 之前加：

```go
		"GET /api/users",
```

- [ ] **Step 5: 运行测试确认通过**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/control/... -count=1
```

Expected: `ok`（整包通过）

- [ ] **Step 6: 提交**

```bash
git add internal/control/grantapi.go internal/control/grantapi_test.go internal/control/routes.go internal/control/routes_test.go
```

```bash
git commit -m "feat: 用户列表接口"
```

---

## Task 5: 前端类型与用户展示辅助

**Files:**
- Modify: `web/src/lib/types.ts`
- Create: `web/src/lib/users.ts`, `web/src/lib/users.test.ts`

- [ ] **Step 1: 加类型**

`web/src/lib/types.ts` 末尾追加：

```ts
/** GET /api/roles —— PascalCase（Role 没有 json tag） */
export type Role = {
  ID: string
  Name: string
  Description: string
  IsBuiltin: boolean
}

/** GET /api/orgs/{id}/effective-grants —— snake_case（视图类型有 json tag） */
export type EffectiveGrant = {
  id: string
  user_id: string
  role_id: string
  /** direct = 本节点直授；inherited = 继承自祖先；global = 全局授予 */
  source: 'direct' | 'inherited' | 'global'
  /** 授予实际挂在哪个节点；global 时为 null */
  source_org_id: string | null
  created_at: string
}
```

- [ ] **Step 2: 写失败测试**

创建 `web/src/lib/users.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { byID, userLabel } from './users'
import type { User } from './types'

function user(over: Partial<User>): User {
  return {
    ID: 'u1',
    ExternalID: 'ext-1',
    Email: 'a@x.com',
    DisplayName: '张三',
    Status: 'active',
    PrimaryOrgID: null,
    ...over,
  }
}

describe('userLabel', () => {
  it('有显示名时给出「显示名（邮箱）」', () => {
    expect(userLabel(user({}), 'u1')).toBe('张三（a@x.com）')
  })

  it('没有显示名时退回邮箱', () => {
    expect(userLabel(user({ DisplayName: '' }), 'u1')).toBe('a@x.com')
  })

  it('查不到这个人时露出原始 ID，而不是显示空白', () => {
    // 授予表里的 user_id 可能指向一个已停用、不在活跃名单里的人。
    // 显示空白会让那一行看起来像坏数据，露出 ID 至少还能追查。
    expect(userLabel(undefined, 'u-ghost')).toBe('u-ghost')
  })
})

describe('byID', () => {
  it('建出 id → user 的查找表', () => {
    const map = byID([user({ ID: 'a' }), user({ ID: 'b' })])
    expect(Object.keys(map).sort()).toEqual(['a', 'b'])
    expect(map.a.Email).toBe('a@x.com')
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

```bash
cd web && npm test -- users
```

Expected: FAIL —— 找不到模块 `./users`

- [ ] **Step 4: 写实现**

创建 `web/src/lib/users.ts`：

```ts
import type { User } from './types'

/**
 * 把用户显示成人话。
 *
 * 查不到人时露出原始 ID 而不是空白：授予表里的 user_id 可能指向一个
 * 已停用、不在活跃名单里的人，显示空白会让那一行看起来像坏数据。
 */
export function userLabel(u: User | undefined, fallbackID: string): string {
  if (!u) return fallbackID
  if (u.DisplayName) return `${u.DisplayName}（${u.Email}）`
  return u.Email || fallbackID
}

/** 建 id → user 的查找表。 */
export function byID(users: User[]): Record<string, User> {
  return Object.fromEntries(users.map((u) => [u.ID, u]))
}
```

- [ ] **Step 5: 运行测试与类型检查**

```bash
cd web && npm test -- users && npm run typecheck
```

Expected: 4 个测试 PASS，类型检查无输出

- [ ] **Step 6: 提交**

```bash
git add web/src/lib/types.ts web/src/lib/users.ts web/src/lib/users.test.ts
```

```bash
git commit -m "feat: 角色与授予的前端类型、用户展示辅助"
```

---

## Task 6: `GrantsTable` 组件

**Files:**
- Create: `web/src/components/GrantsTable.tsx`, `web/src/components/GrantsTable.test.tsx`

- [ ] **Step 1: 写失败测试**

创建 `web/src/components/GrantsTable.test.tsx`：

```tsx
import { describe, expect, it, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { GrantsTable } from './GrantsTable'
import type { EffectiveGrant, Org, Role, User } from '@/lib/types'

const users: User[] = [
  {
    ID: 'u1', ExternalID: 'e1', Email: 'a@x.com', DisplayName: '张三',
    Status: 'active', PrimaryOrgID: null,
  },
]
const roles: Role[] = [
  { ID: 'org_admin', Name: '组织管理员', Description: '', IsBuiltin: true },
]
const orgs: Org[] = [
  {
    ID: 'root', ParentID: null, Name: '总部', Path: '/root',
    ExternalSource: null, ExternalID: null, IsKeyHolder: false,
  },
]

function grant(over: Partial<EffectiveGrant>): EffectiveGrant {
  return {
    id: 'g1', user_id: 'u1', role_id: 'org_admin',
    source: 'direct', source_org_id: 'rd',
    created_at: '2026-09-03T00:00:00Z',
    ...over,
  }
}

function renderTable(grants: EffectiveGrant[]) {
  return render(
    <GrantsTable
      grants={grants}
      users={users}
      roles={roles}
      orgs={orgs}
      onRevoke={vi.fn()}
    />,
  )
}

describe('GrantsTable', () => {
  it('直授行才有撤销按钮', () => {
    renderTable([grant({ id: 'g-direct', source: 'direct' })])
    expect(screen.getByRole('button', { name: '撤销' })).toBeInTheDocument()
  })

  it('继承来的授予不给撤销按钮，只说清该去哪里撤销', () => {
    // 在这一页撤销一条继承来的授予，改动的是另一个节点上的行，
    // 会同时波及那个节点下所有其它子树。做成就手可点的按钮等于邀请误伤。
    renderTable([grant({ id: 'g-inherited', source: 'inherited', source_org_id: 'root' })])
    expect(screen.queryByRole('button', { name: '撤销' })).not.toBeInTheDocument()
    expect(screen.getByText(/需在「总部」上撤销/)).toBeInTheDocument()
  })

  it('全局授予同样不给撤销按钮', () => {
    renderTable([grant({ id: 'g-global', source: 'global', source_org_id: null })])
    expect(screen.queryByRole('button', { name: '撤销' })).not.toBeInTheDocument()
    expect(screen.getByText(/全局授予/)).toBeInTheDocument()
  })

  it('把 user_id 还原成人名，把 role_id 还原成角色名', () => {
    renderTable([grant({})])
    expect(screen.getByText('张三（a@x.com）')).toBeInTheDocument()
    expect(screen.getByText('组织管理员')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd web && npm test -- GrantsTable
```

Expected: FAIL —— 找不到模块 `./GrantsTable`

- [ ] **Step 3: 写组件**

创建 `web/src/components/GrantsTable.tsx`：

```tsx
'use client'

import { Button, Popconfirm, Table, Tag, Typography } from 'antd'
import { byID, userLabel } from '@/lib/users'
import type { EffectiveGrant, Org, Role, User } from '@/lib/types'

const SOURCE_TAG: Record<string, { text: string; color: string }> = {
  direct: { text: '直授', color: 'blue' },
  inherited: { text: '继承', color: 'gold' },
  global: { text: '全局', color: 'purple' },
}

/**
 * 有效权限表：谁对这个节点有权。
 *
 * 只有 source === 'direct' 的行能就地撤销。继承与全局的行只给一句说明——
 * 在这一页撤销一条继承来的授予，实际改动的是**另一个节点**上的行，会同时
 * 波及那个节点下所有其它子树。把它做成就手可点的按钮，等于邀请人误伤。
 */
export function GrantsTable({
  grants,
  users,
  roles,
  orgs,
  onRevoke,
}: {
  grants: EffectiveGrant[]
  users: User[]
  roles: Role[]
  orgs: Org[]
  onRevoke: (id: string) => void | Promise<void>
}) {
  const userMap = byID(users)
  const roleName = (id: string) => roles.find((r) => r.ID === id)?.Name ?? id
  const orgName = (id: string | null) =>
    id === null ? null : (orgs.find((o) => o.ID === id)?.Name ?? id)

  return (
    <Table<EffectiveGrant>
      rowKey="id"
      dataSource={grants}
      pagination={false}
      locale={{ emptyText: '该节点上没有任何人有权' }}
      columns={[
        {
          title: '用户',
          dataIndex: 'user_id',
          render: (id: string) => userLabel(userMap[id], id),
        },
        { title: '角色', dataIndex: 'role_id', render: (id: string) => roleName(id) },
        {
          title: '来源',
          dataIndex: 'source',
          render: (s: string) => {
            const meta = SOURCE_TAG[s] ?? { text: s, color: 'default' }
            return <Tag color={meta.color}>{meta.text}</Tag>
          },
        },
        {
          title: '授予时间',
          dataIndex: 'created_at',
          render: (t: string) => new Date(t).toLocaleString('zh-CN'),
        },
        {
          title: '操作',
          render: (_, g) => {
            if (g.source === 'direct') {
              return (
                <Popconfirm
                  title="确认撤销该授予？"
                  description="撤销后该用户将失去这个节点上的这个角色。"
                  onConfirm={() => onRevoke(g.id)}
                >
                  <Button type="link" danger>
                    撤销
                  </Button>
                </Popconfirm>
              )
            }
            if (g.source === 'global') {
              return <Typography.Text type="secondary">全局授予，需在全局撤销</Typography.Text>
            }
            return (
              <Typography.Text type="secondary">
                需在「{orgName(g.source_org_id)}」上撤销
              </Typography.Text>
            )
          },
        },
      ]}
    />
  )
}
```

- [ ] **Step 4: 运行测试与类型检查**

```bash
cd web && npm test -- GrantsTable && npm run typecheck
```

Expected: 4 个测试 PASS，类型检查无输出

- [ ] **Step 5: 提交**

```bash
git add web/src/components/GrantsTable.tsx web/src/components/GrantsTable.test.tsx
```

```bash
git commit -m "feat: 有效权限表，撤销只对直授行开放"
```

---

## Task 7: `GrantModal` 授予弹窗

**Files:**
- Create: `web/src/components/GrantModal.tsx`

- [ ] **Step 1: 写组件**

创建 `web/src/components/GrantModal.tsx`：

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { App, Modal, Select, Typography } from 'antd'
import { apiGet, apiSend, ApiError } from '@/lib/api'
import { userLabel } from '@/lib/users'
import type { Role, User } from '@/lib/types'

/**
 * 授予角色弹窗。
 *
 * 角色下拉来自 GET /api/roles?grantable_at=<节点>——服务端已经用反提权闸
 * 滤掉了调用者授不了的角色，所以这里不会出现注定拿 403 的选项。前端不自己
 * 算这个：角色接口不返回权限集，算不出来（设计文档 D2）。
 */
export function GrantModal({
  orgID,
  orgName,
  users,
  onClose,
  onGranted,
}: {
  orgID: string | null
  orgName: string
  users: User[]
  onClose: () => void
  onGranted: () => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [roles, setRoles] = useState<Role[]>([])
  const [userID, setUserID] = useState<string | undefined>()
  const [roleID, setRoleID] = useState<string | undefined>()
  const [busy, setBusy] = useState(false)

  const loadRoles = useCallback(async () => {
    if (!orgID) return
    try {
      setRoles(await apiGet<Role[]>(`/api/roles?grantable_at=${orgID}`))
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      setRoles([])
    }
  }, [orgID, message])

  useEffect(() => {
    void loadRoles()
  }, [loadRoles])

  const submit = async () => {
    if (!orgID || !userID || !roleID) return
    setBusy(true)
    try {
      await apiSend('POST', '/api/grants', {
        user_id: userID,
        role_id: roleID,
        org_id: orgID,
      })
      message.success('已授予')
      setUserID(undefined)
      setRoleID(undefined)
      await onGranted()
      onClose()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('授予失败')
    } finally {
      setBusy(false)
    }
  }

  return (
    <Modal
      title={`在「${orgName}」上授予角色`}
      open={orgID !== null}
      onCancel={onClose}
      onOk={submit}
      okButtonProps={{ disabled: !userID || !roleID }}
      confirmLoading={busy}
    >
      <Typography.Paragraph type="secondary">
        下拉里只有你在这个节点上能授予的角色——授不了的已由服务端滤掉。
      </Typography.Paragraph>
      <Select
        showSearch
        style={{ width: '100%', marginBottom: 12 }}
        placeholder="选择用户"
        value={userID}
        onChange={setUserID}
        optionFilterProp="label"
        options={users.map((u) => ({ value: u.ID, label: userLabel(u, u.ID) }))}
      />
      <Select
        style={{ width: '100%' }}
        placeholder="选择角色"
        value={roleID}
        onChange={setRoleID}
        options={roles.map((r) => ({ value: r.ID, label: r.Name }))}
      />
    </Modal>
  )
}
```

- [ ] **Step 2: 类型检查**

```bash
cd web && npm run typecheck
```

Expected: 无输出

- [ ] **Step 3: 提交**

```bash
git add web/src/components/GrantModal.tsx
```

```bash
git commit -m "feat: 授予角色弹窗"
```

---

## Task 8: 角色与权限页

**Files:**
- Create: `web/src/app/platform/grants/page.tsx`
- Modify: `web/src/components/workbenches.ts`

- [ ] **Step 1: 建目录**

```bash
mkdir -p web/src/app/platform/grants
```

- [ ] **Step 2: 写页面**

创建 `web/src/app/platform/grants/page.tsx`：

```tsx
'use client'

import { useCallback, useEffect, useState } from 'react'
import { App, Button, Space } from 'antd'
import { AppShell } from '@/components/AppShell'
import { OrgTree } from '@/components/OrgTree'
import { GrantsTable } from '@/components/GrantsTable'
import { GrantModal } from '@/components/GrantModal'
import { useSession } from '@/lib/session'
import { apiGet, apiSend, ApiError } from '@/lib/api'
import type { EffectiveGrant, Org, Role, User } from '@/lib/types'

export default function GrantsPage() {
  const { session, loading } = useSession()
  const { message } = App.useApp()
  const [orgs, setOrgs] = useState<Org[]>([])
  const [users, setUsers] = useState<User[]>([])
  const [roles, setRoles] = useState<Role[]>([])
  const [selected, setSelected] = useState<string | null>(null)
  const [grants, setGrants] = useState<EffectiveGrant[]>([])
  const [granting, setGranting] = useState(false)

  useEffect(() => {
    apiGet<Org[]>('/api/orgs')
      .then(setOrgs)
      .catch(() => setOrgs([]))
    // 用户与角色只用来把 ID 还原成人话，取不到就退回显示原始 ID。
    apiGet<User[]>('/api/users')
      .then(setUsers)
      .catch(() => setUsers([]))
    apiGet<Role[]>('/api/roles')
      .then(setRoles)
      .catch(() => setRoles([]))
  }, [])

  const reload = useCallback(async () => {
    if (!selected) {
      setGrants([])
      return
    }
    try {
      // 有效视图：直授 + 继承 + 全局。只问直授的话，页面会漏掉
      // 祖先上的 org_admin 与全局 platform_admin——而他们权限最大。
      setGrants(await apiGet<EffectiveGrant[]>(`/api/orgs/${selected}/effective-grants`))
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      setGrants([])
    }
  }, [selected, message])

  useEffect(() => {
    void reload()
  }, [reload])

  const revoke = async (id: string) => {
    try {
      await apiSend('DELETE', `/api/grants/${id}`)
      message.success('已撤销')
      await reload()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
    }
  }

  if (loading || !session) return null

  const selectedName = orgs.find((o) => o.ID === selected)?.Name ?? ''

  return (
    <AppShell workbenches={session.workbenches}>
      <div style={{ display: 'flex', gap: 24, alignItems: 'flex-start' }}>
        <div style={{ width: 260, flexShrink: 0 }}>
          <OrgTree orgs={orgs} selected={selected} onSelect={setSelected} />
        </div>

        <div style={{ flex: 1, minWidth: 0 }}>
          <Space direction="vertical" style={{ width: '100%' }} size="middle">
            <Button type="primary" disabled={!selected} onClick={() => setGranting(true)}>
              授予角色
            </Button>

            {selected ? (
              <GrantsTable
                grants={grants}
                users={users}
                roles={roles}
                orgs={orgs}
                onRevoke={revoke}
              />
            ) : (
              <div style={{ color: '#999' }}>请先在左侧选择一个节点</div>
            )}
          </Space>
        </div>
      </div>

      <GrantModal
        orgID={granting ? selected : null}
        orgName={selectedName}
        users={users}
        onClose={() => setGranting(false)}
        onGranted={reload}
      />
    </AppShell>
  )
}
```

- [ ] **Step 3: 导航加一条**

`web/src/components/workbenches.ts`，把 `platform` 那项的 `nav` 改为：

```ts
    nav: [
      { label: '组织与成员', href: '/platform/orgs' },
      { label: '虚拟密钥', href: '/platform/keys' },
      { label: '角色与权限', href: '/platform/grants' },
    ],
```

- [ ] **Step 4: 全量测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部 PASS，构建产物里出现 `/platform/grants`

- [ ] **Step 5: 提交**

```bash
git add web/src/app/platform/grants web/src/components/workbenches.ts
```

```bash
git commit -m "feat: 角色与权限页"
```

---

## Task 9: `OrgNodeProps` 节点属性组件

**Files:**
- Create: `web/src/components/OrgNodeProps.tsx`

- [ ] **Step 1: 写组件**

创建 `web/src/components/OrgNodeProps.tsx`：

```tsx
'use client'

import { useEffect, useState } from 'react'
import { App, Button, Card, Descriptions, Input, Select, Switch } from 'antd'
import { apiSend, ApiError } from '@/lib/api'
import type { Org } from '@/lib/types'

/**
 * 节点属性：改名、移动、密钥边界开关。
 *
 * 三个操作的守卫都在服务端，前端只如实转述它的中文（设计文档 D5）：
 * 移动成环、移动到无权的父节点（403）、节点下还有未吊销密钥时取消
 * 密钥边界（409）。
 */
export function OrgNodeProps({
  org,
  orgs,
  onChanged,
}: {
  org: Org
  orgs: Org[]
  onChanged: () => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [name, setName] = useState(org.Name)
  const [parentID, setParentID] = useState<string | null>(org.ParentID)

  // 切换选中节点时把编辑中的值同步过来，否则会把上一个节点的名字带过来。
  useEffect(() => {
    setName(org.Name)
    setParentID(org.ParentID)
  }, [org.ID, org.Name, org.ParentID])

  const run = async (fn: () => Promise<unknown>, ok: string) => {
    try {
      await fn()
      message.success(ok)
      await onChanged()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('操作失败')
    }
  }

  return (
    <Card title="节点属性" size="small">
      <Descriptions column={1} size="small">
        <Descriptions.Item label="名称">
          <Input
            value={name}
            onChange={(e) => setName(e.target.value)}
            style={{ width: 220, marginRight: 8 }}
          />
          <Button
            disabled={name.trim() === '' || name === org.Name}
            onClick={() =>
              run(
                () => apiSend('PATCH', `/api/orgs/${org.ID}/name`, { name: name.trim() }),
                '已改名',
              )
            }
          >
            改名
          </Button>
        </Descriptions.Item>

        <Descriptions.Item label="父节点">
          <Select
            allowClear
            showSearch
            placeholder="（无父节点，即根节点）"
            style={{ width: 220, marginRight: 8 }}
            value={parentID ?? undefined}
            onChange={(v) => setParentID(v ?? null)}
            optionFilterProp="label"
            options={orgs
              .filter((o) => o.ID !== org.ID)
              .map((o) => ({ value: o.ID, label: o.Name }))}
          />
          <Button
            disabled={parentID === org.ParentID}
            onClick={() =>
              run(
                () => apiSend('PATCH', `/api/orgs/${org.ID}/parent`, { parent_id: parentID }),
                '已移动',
              )
            }
          >
            移动
          </Button>
        </Descriptions.Item>

        <Descriptions.Item label="密钥边界">
          <Switch
            checked={org.IsKeyHolder}
            onChange={(checked) =>
              run(
                () =>
                  apiSend('PUT', `/api/orgs/${org.ID}/key-holder`, { is_key_holder: checked }),
                checked ? '已标记为密钥边界' : '已取消密钥边界',
              )
            }
          />
        </Descriptions.Item>
      </Descriptions>
    </Card>
  )
}
```

- [ ] **Step 2: 类型检查**

```bash
cd web && npm run typecheck
```

Expected: 无输出

- [ ] **Step 3: 提交**

```bash
git add web/src/components/OrgNodeProps.tsx
```

```bash
git commit -m "feat: 组织节点属性组件"
```

---

## Task 10: `OrgMembers` 成员组件

**Files:**
- Create: `web/src/components/OrgMembers.tsx`

- [ ] **Step 1: 写组件**

创建 `web/src/components/OrgMembers.tsx`：

```tsx
'use client'

import { useState } from 'react'
import { App, Button, Card, Popconfirm, Select, Space, Table, Tag, Typography } from 'antd'
import { apiSend, ApiError } from '@/lib/api'
import { userLabel } from '@/lib/users'
import type { Org, User } from '@/lib/types'

/**
 * 该节点的成员——即 primary_org_id 指向这个节点的用户。
 *
 * 「移出」把 primary_org_id 置空。要如实提醒的是：这条路由用
 * TargetFromBody("org_id") 取判定目标，移进某节点只要该节点的
 * member:assign，而移出（org_id 传 null）目标退化为 nil、判定变成
 * **全局** member:assign。这是既有路由的行为，本期不改，但要让 403
 * 的原因说得通，不能让人对着按钮困惑。
 */
export function OrgMembers({
  org,
  users,
  onChanged,
}: {
  org: Org
  users: User[]
  onChanged: () => void | Promise<void>
}) {
  const { message } = App.useApp()
  const [adding, setAdding] = useState<string | undefined>()

  const members = users.filter((u) => u.PrimaryOrgID === org.ID)
  const outsiders = users.filter((u) => u.PrimaryOrgID !== org.ID)

  const assign = async (userID: string, orgID: string | null, ok: string) => {
    try {
      await apiSend('PUT', `/api/users/${userID}/primary-org`, { org_id: orgID })
      message.success(ok)
      await onChanged()
    } catch (e) {
      if (e instanceof ApiError) message.error(e.message)
      else message.error('操作失败')
    }
  }

  return (
    <Card title="成员" size="small" style={{ marginTop: 16 }}>
      <Space style={{ marginBottom: 12 }}>
        <Select
          showSearch
          style={{ width: 260 }}
          placeholder="选择要加入该节点的用户"
          value={adding}
          onChange={setAdding}
          optionFilterProp="label"
          options={outsiders.map((u) => ({ value: u.ID, label: userLabel(u, u.ID) }))}
        />
        <Button
          type="primary"
          disabled={!adding}
          onClick={async () => {
            if (!adding) return
            await assign(adding, org.ID, '已加入该节点')
            setAdding(undefined)
          }}
        >
          加入
        </Button>
      </Space>

      <Table<User>
        rowKey="ID"
        dataSource={members}
        size="small"
        pagination={false}
        locale={{ emptyText: '该节点下还没有成员' }}
        columns={[
          { title: '用户', render: (_, u) => userLabel(u, u.ID) },
          {
            title: '状态',
            dataIndex: 'Status',
            render: (s: string) => (
              <Tag color={s === 'active' ? 'green' : 'default'}>
                {s === 'active' ? '在职' : s}
              </Tag>
            ),
          },
          {
            title: '操作',
            render: (_, u) => (
              <Popconfirm
                title="确认移出该节点？"
                description="移出后该用户将没有归属节点。这一步需要全局的成员指派权限。"
                onConfirm={() => assign(u.ID, null, '已移出')}
              >
                <Button type="link" danger>
                  移出
                </Button>
              </Popconfirm>
            ),
          },
        ]}
      />
      <Typography.Text type="secondary">
        成员归属只决定「人属于哪个节点」，不等于权限；权限在「角色与权限」页管理。
      </Typography.Text>
    </Card>
  )
}
```

- [ ] **Step 2: 类型检查**

```bash
cd web && npm run typecheck
```

Expected: 无输出

- [ ] **Step 3: 提交**

```bash
git add web/src/components/OrgMembers.tsx
```

```bash
git commit -m "feat: 组织节点成员组件"
```

---

## Task 11: 组织与成员页接入两个组件

**Files:**
- Modify: `web/src/app/platform/orgs/page.tsx`

- [ ] **Step 1: 改页面**

`web/src/app/platform/orgs/page.tsx`：

1. 加 import：

```tsx
import { OrgNodeProps } from '@/components/OrgNodeProps'
import { OrgMembers } from '@/components/OrgMembers'
```

2. 类型 import 改为：

```tsx
import type { Org, User } from '@/lib/types'
```

3. 在 `const [creating, setCreating] = useState(false)` 之后加一个 state：

```tsx
  const [users, setUsers] = useState<User[]>([])
```

4. 在既有的 `useEffect(() => { void reload() }, [reload])` **之后**加一个：

```tsx
  // 成员块要把 user_id 还原成人名并列出该节点的人。取不到就退回空列表，
  // 页面其余部分照常可用。
  const reloadUsers = useCallback(async () => {
    try {
      setUsers(await apiGet<User[]>('/api/users'))
    } catch {
      setUsers([])
    }
  }, [])

  useEffect(() => {
    void reloadUsers()
  }, [reloadUsers])
```

5. 把 JSX 里的 `<OrgTree ... />` 那一段替换为左右布局——找到包住树的那个 `<Space direction="vertical">`，在 `<OrgTree ... />` 之后、`</Space>` 之前**不动**，而是在整个 `<Space>` 之外套一层。具体做法：把

```tsx
      <Space direction="vertical" style={{ width: '100%' }} size="middle">
```

改为

```tsx
      <Space direction="vertical" style={{ width: 320, flexShrink: 0 }} size="middle">
```

并在这个 `</Space>` 之后、`<Modal` 之前插入右栏与外层容器的收尾：

```tsx
        <div style={{ flex: 1, minWidth: 0 }}>
          {selectedOrg ? (
            <>
              <OrgNodeProps org={selectedOrg} orgs={orgs} onChanged={reload} />
              <OrgMembers org={selectedOrg} users={users} onChanged={reloadUsers} />
            </>
          ) : (
            <div style={{ color: '#999' }}>选择一个节点以查看属性与成员</div>
          )}
        </div>
      </div>
```

并在原来的 `<Space direction="vertical" ...>` **之前**插入外层容器开标签：

```tsx
      <div style={{ display: 'flex', gap: 24, alignItems: 'flex-start' }}>
```

6. 在 `if (loading || !session) return null` **之前**加一行派生值：

```tsx
  const selectedOrg = orgs.find((o) => o.ID === selected) ?? null
```

- [ ] **Step 2: 全量测试、类型检查与构建**

```bash
cd web && npm test && npm run typecheck && npm run build
```

Expected: 全部 PASS，构建成功。若 JSX 标签配对出错，`npm run build` 会明确指出行号——照它修。

- [ ] **Step 3: 提交**

```bash
git add web/src/app/platform/orgs/page.tsx
```

```bash
git commit -m "feat: 组织页接入节点属性与成员管理"
```

---

## Task 12: 端到端验收

**Files:**
- Create: `docs/superpowers/findings/2026-09-03-p1.4c-acceptance.md`

对着真实运行的 Postgres + Casdoor + control 跑完设计文档 §7 的 10 项验收标准，**每一项都记录真实输出，不留占位符**。

> **操作纪律一**（P1.3b 踩过）：手工 E2E 与 `make test` 不能共用同一个数据库。一旦启动 control 开始手工验收，就不再跑 `go test`，直到全部手工步骤记录完毕。
>
> **操作纪律二**（设计文档 §7 已写明）：**移动与删除节点会打乱夹具**，把第 7 项（移动）排在第 1–5 项之后。

- [ ] **Step 1: 起栈并构建**

```bash
docker ps --format '{{.Names}}\t{{.Status}}'
```

Expected: `deploy-postgres-1`、`deploy-casdoor-1` 在跑

```bash
make console && make build
```

Expected: `web/dist/index.html` 存在，`bin/airlock` 构建成功

- [ ] **Step 2: 启动 control**

```bash
env POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" CONTROL_LISTEN_ADDR=:8081 OIDC_ISSUER=http://localhost:8000 OIDC_CLIENT_ID=airlock OIDC_CLIENT_SECRET=airlock-dev-secret OIDC_REDIRECT_URL=http://localhost:8081/auth/callback AIRLOCK_BOOTSTRAP_ADMIN=admin@example.com AIRLOCK_ENCRYPTION_KEY=YWlybG9jay1kZXYtb25seS0zMmJ5dGUta2V5ISEhISE= LITELLM_BASE_URL=http://localhost:4000 LITELLM_MASTER_KEY=sk-airlock-master-dev-only nohup ./bin/airlock control > /tmp/airlock-p14c.log 2>&1 &
```

```bash
sleep 3 && tail -5 /tmp/airlock-p14c.log
```

Expected: `Control 启动`

- [ ] **Step 3: 造夹具**

把下面这段存成 `/tmp/p14c_fixtures.py` 并运行：

```python
import hashlib

tokens = {
    'u-p14c-admin': 'p14c-admin-token-aaa111',
    'u-p14c-rdadmin': 'p14c-rdadmin-token-bbb222',
    'u-p14c-member': 'p14c-member-token-ccc333',
}

sql = """-- rd 与 rd2 是同前缀兄弟节点，用于第六次验证那个陷阱
INSERT INTO organizations (id, parent_id, name, path, is_key_holder) VALUES
  ('p14c-root', NULL, 'P14c根', '/p14c-root', false),
  ('p14c-rd', 'p14c-root', 'P14c-RD', '/p14c-root/p14c-rd', false),
  ('p14c-rd2', 'p14c-root', 'P14c-RD2（同前缀兄弟节点）', '/p14c-root/p14c-rd2', false)
ON CONFLICT (id) DO NOTHING;

INSERT INTO users (id, external_id, email, display_name, status, primary_org_id) VALUES
  ('u-p14c-admin', 'ext-p14c-admin', 'admin@p14c.test', 'P14c管理员', 'active', 'p14c-root'),
  ('u-p14c-rdadmin', 'ext-p14c-rdadmin', 'rdadmin@p14c.test', 'P14cRD管理员', 'active', 'p14c-rd'),
  ('u-p14c-member', 'ext-p14c-member', 'member@p14c.test', 'P14c普通成员', 'active', NULL)
ON CONFLICT (id) DO NOTHING;

-- admin 全局 platform_admin；rdadmin 只在 p14c-rd 上持 org_admin；
-- 另在 p14c-rd2 上给 rdadmin 也来一条，用于验证兄弟节点不串台。
INSERT INTO role_grants (id, user_id, role_id, org_id) VALUES
  ('rg-p14c-admin', 'u-p14c-admin', 'platform_admin', NULL),
  ('rg-p14c-rdadmin', 'u-p14c-rdadmin', 'org_admin', 'p14c-rd'),
  ('rg-p14c-rd2admin', 'u-p14c-member', 'org_admin', 'p14c-rd2')
ON CONFLICT DO NOTHING;

INSERT INTO sessions (id, user_id, expires_at) VALUES
"""
lines = []
for uid, tok in tokens.items():
    h = hashlib.sha256(tok.encode()).hexdigest()
    assert len(h) == 64
    lines.append("  ('%s', '%s', now() + interval '4 hours')" % (h, uid))
sql += ",\n".join(lines) + "\nON CONFLICT (id) DO NOTHING;\n"

with open('/tmp/p14c_fixtures.sql', 'w') as f:
    f.write(sql)

for uid, tok in tokens.items():
    print(uid, '->', tok)
```

```bash
python3 /tmp/p14c_fixtures.py
```

```bash
docker exec -i deploy-postgres-1 psql -U airlock -d airlock < /tmp/p14c_fixtures.sql
```

Expected: 四条 `INSERT` 均成功

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT user_id, length(id)=64 AS hash_ok FROM sessions WHERE user_id LIKE 'u-p14c-%';"
```

Expected: 三行，`hash_ok` 都是 `t`

后续 `<admin token>` = `p14c-admin-token-aaa111`，`<rdadmin token>` = `p14c-rdadmin-token-bbb222`。

- [ ] **Step 4: 验收项 1 —— 有效视图三类来源齐全**

```bash
curl -sS http://localhost:8081/api/orgs/p14c-rd/effective-grants -H "Cookie: airlock_session=p14c-admin-token-aaa111" | python3 -c 'import json,sys; d=json.load(sys.stdin); print([(g["role_id"], g["source"], g["source_org_id"]) for g in d])'
```

Expected: 至少三条，分别是 `('org_admin','direct','p14c-rd')`、来自 `p14c-root` 的 `inherited`（如果 root 上有授予）与 `('platform_admin','global',None)`。**若 root 上此刻没有授予，先在 root 上授一条再重跑这一步**：

```bash
curl -sS -X POST http://localhost:8081/api/grants -H "Cookie: airlock_session=p14c-admin-token-aaa111" -H "Content-Type: application/json" -d '{"user_id":"u-p14c-member","role_id":"auditor","org_id":"p14c-root"}' -w "\nHTTP=%{http_code}\n"
```

界面上在「角色与权限」页选中 `P14c-RD`，记录三种来源 Tag（直授 / 继承 / 全局）各自出现。

- [ ] **Step 5: 验收项 2 —— 同前缀兄弟节点不混入（第六次陷阱）**

`u-p14c-member` 在 `p14c-rd2` 上持 `org_admin`。查 `p14c-rd` 的有效视图：

```bash
curl -sS http://localhost:8081/api/orgs/p14c-rd/effective-grants -H "Cookie: airlock_session=p14c-admin-token-aaa111" | python3 -c 'import json,sys; d=json.load(sys.stdin); print([g["source_org_id"] for g in d])'
```

Expected: 出现的 `source_org_id` 只有 `p14c-rd`、`p14c-root` 与 `None`，**绝不能出现 `p14c-rd2`**。反向再查一次 `p14c-rd2`，同样不能出现 `p14c-rd`。

- [ ] **Step 6: 验收项 3 —— 只有直授行可撤销**

界面上选中 `P14c-RD`：Expected: `direct` 行有「撤销」按钮；`inherited` 行显示「需在「P14c根」上撤销」；`global` 行显示「全局授予，需在全局撤销」。**记录三行各自的操作列文案。**

点直授行的「撤销」并确认：

```bash
curl -sS http://localhost:8081/api/orgs/p14c-rd/effective-grants -H "Cookie: airlock_session=p14c-admin-token-aaa111" | python3 -c 'import json,sys; d=json.load(sys.stdin); print([(g["role_id"],g["source"]) for g in d])'
```

Expected: 那条 `direct` 已消失，`inherited` 与 `global` 仍在（撤销没有误伤别的节点）。

- [ ] **Step 7: 验收项 4、5 —— 可授角色过滤与服务端闸**

以 `u-p14c-rdadmin`（只在 `p14c-rd` 上持 `org_admin`）：

```bash
curl -sS "http://localhost:8081/api/roles?grantable_at=p14c-rd" -H "Cookie: airlock_session=p14c-rdadmin-token-bbb222" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(sorted(r["ID"] for r in d))'
```

Expected: 列表里**没有** `platform_admin`

```bash
curl -sS -X POST http://localhost:8081/api/grants -H "Cookie: airlock_session=p14c-rdadmin-token-bbb222" -H "Content-Type: application/json" -d '{"user_id":"u-p14c-member","role_id":"platform_admin","org_id":"p14c-rd"}' -w "\nHTTP=%{http_code}\n"
```

Expected: `403` 且 code 为 `escalation_denied`。**这一条证明下拉过滤只是便利，闸在服务端。**

- [ ] **Step 8: 验收项 6 —— 改名**

界面上选中 `P14c-RD2`，把名称改为 `P14c-RD2-改名` 并点「改名」。

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT id, name FROM organizations WHERE id='p14c-rd2';"
```

Expected: `name` 已变

- [ ] **Step 9: 验收项 7 —— 移动（含两种失败）**

先做**成功**的一次：把 `p14c-rd2` 移到 `p14c-rd` 之下。

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT id, parent_id, path FROM organizations WHERE id LIKE 'p14c-%' ORDER BY path;"
```

Expected: `p14c-rd2` 的 `path` 变成 `/p14c-root/p14c-rd/p14c-rd2`

再做**成环**：把 `p14c-root` 移到 `p14c-rd2` 之下。

```bash
curl -sS -X PATCH http://localhost:8081/api/orgs/p14c-root/parent -H "Cookie: airlock_session=p14c-admin-token-aaa111" -H "Content-Type: application/json" -d '{"parent_id":"p14c-rd2"}' -w "\nHTTP=%{http_code}\n"
```

Expected: 非 2xx，且返回后端关于成环的中文错误

再做**无权目标**：以 `rdadmin` 身份把 `p14c-rd` 移到它无权的 `p14c-root` 之下。

```bash
curl -sS -X PATCH http://localhost:8081/api/orgs/p14c-rd/parent -H "Cookie: airlock_session=p14c-rdadmin-token-bbb222" -H "Content-Type: application/json" -d '{"parent_id":"p14c-root"}' -w "\nHTTP=%{http_code}\n"
```

Expected: `403`「没有在目标父节点下添加子节点的权限」

- [ ] **Step 10: 验收项 8 —— 密钥边界开关（含 409）**

界面上把 `P14c-RD` 的密钥边界开关打开。

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT id, is_key_holder FROM organizations WHERE id='p14c-rd';"
```

Expected: `t`

在该节点下签一把密钥，然后尝试关掉开关：

```bash
curl -sS -X POST http://localhost:8081/api/keys -H "Cookie: airlock_session=p14c-admin-token-aaa111" -H "Content-Type: application/json" -d '{"org_id":"p14c-rd","user_id":"u-p14c-member","name":"边界测试密钥","models":["qwen-plus"]}' >/dev/null
```

```bash
curl -sS -X PUT http://localhost:8081/api/orgs/p14c-rd/key-holder -H "Cookie: airlock_session=p14c-admin-token-aaa111" -H "Content-Type: application/json" -d '{"is_key_holder":false}' -w "\nHTTP=%{http_code}\n"
```

Expected: `409` `org_has_live_keys`，中文提示「该节点下还有未吊销的密钥，请先吊销后再取消密钥边界标记」。**界面上把开关关掉时应弹出同一句话。**

- [ ] **Step 11: 验收项 9 —— 成员加入与移出**

界面上在「组织与成员」页选中 `P14c-RD`，把 `P14c普通成员`（`primary_org_id` 为空）加入该节点。

```bash
docker exec deploy-postgres-1 psql -U airlock -d airlock -c "SELECT id, primary_org_id FROM users WHERE id='u-p14c-member';"
```

Expected: `primary_org_id` = `p14c-rd`

再点「移出」（以全局 platform_admin 身份，因为移出需要全局 `member:assign`）：

Expected: `primary_org_id` 变回 `NULL`。**同时以 `rdadmin` 身份试一次移出，记录 403——这是设计文档 §5 记下的那处不对称的实证。**

```bash
curl -sS -X PUT http://localhost:8081/api/users/u-p14c-member/primary-org -H "Cookie: airlock_session=p14c-rdadmin-token-bbb222" -H "Content-Type: application/json" -d '{"org_id":null}' -w "\nHTTP=%{http_code}\n"
```

- [ ] **Step 12: 收尾**

```bash
pkill -f "bin/airlock control"
```

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" make test
```

```bash
make lint && gofmt -l . && git status --short
```

Expected: 全部通过，工作区干净（**不要** `docker compose down`）

- [ ] **Step 13: 验收项 10 —— Edge 零回归**

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- PASS'
```

Expected: `33`

```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" go test ./internal/edge/... -race -count=1 -v 2>&1 | grep -c '^--- FAIL'
```

Expected: `0`

- [ ] **Step 14: 写验收记录**

创建 `docs/superpowers/findings/2026-09-03-p1.4c-acceptance.md`，结构照 `2026-09-03-p1.4b-acceptance.md`：表头、10 项结果表、每项的真实输出、过程中发现的问题及处置。**每个单元格填真实数据，不留占位符。**

- [ ] **Step 15: 提交**

```bash
git add docs/superpowers/findings/2026-09-03-p1.4c-acceptance.md
```

```bash
git commit -m "docs: P1.4c 端到端验收记录"
```

---

## 完成标准

- [ ] `make test` 全绿（Go `-race` + 前端 Vitest）
- [ ] `make lint` 通过（含前端 `tsc --noEmit`），`gofmt -l .` 无输出
- [ ] 路由机械检查覆盖两条新路由；`Handler()` 不 panic 的测试在位
- [ ] 验收记录 10 项全部通过，且无占位符
- [ ] **有效视图里直授 / 继承 / 全局三类来源齐全且标注正确**
- [ ] **同前缀兄弟节点的授予不混入**（第六次踩这个陷阱的证据）
- [ ] **只有直授行有撤销按钮；撤销后不误伤其它节点**
- [ ] **org_admin 的可授角色下拉不含 platform_admin，且绕开界面直接打接口仍 403 `escalation_denied`**
- [ ] 改名、移动（含成环与无权目标两种失败）、密钥边界开关（含 409）、成员加入与移出各有真实输出
- [ ] `internal/edge` 33 PASS / 0 FAIL
- [ ] 所有代码已提交，工作区干净

---

## 自查记录

对照设计文档的覆盖情况：

| 设计文档章节 | 对应任务 |
|---|---|
| §1 做什么（角色与权限页、组织页补齐、三处后端） | Task 1–11 |
| §1 范围外（LDAP 导入、同步状态、自定义角色、成本与审计） | 全程无对应代码 |
| §2 C1（`ListGrantsForOrg` 是单节点） | Task 1、2 |
| §2 C2（前端算不出可授角色） | Task 3 |
| §2 C3（`ListActive` 已存在） | Task 4 |
| §2 C4（组织三个操作已有服务端守卫） | Task 9、Task 12 Step 9/10 |
| §3 D1 有效权限视图 + 只有直授可撤销 | Task 1、2（后端）、Task 6（撤销按钮规则与其测试） |
| §3 D2 可授角色由服务端给出 | Task 3、Task 7 |
| §3 D3 用户列表不裁剪 | Task 4（含注释说明理由） |
| §3 D4 独立路由而非查询参数 | Task 2 Step 5 的路由注释 |
| §3 D5 错误透传后端中文 | Task 9、10 的 `run` 辅助 |
| §4 B1/B2/B3 | Task 1+2 / Task 3 / Task 4 |
| §5 前端结构与两个页面 | Task 5–11 |
| §5 移出成员的权限不对称 | Task 10 的组件注释与 Popconfirm 文案、Task 12 Step 11 的 403 实证 |
| §6 测试策略 | 各任务的测试步骤 |
| §7 验收标准 10 项 + 操作纪律 | Task 12 |

**规模**：12 个任务，略少于 P1.4a（15）与 P1.4b（16），因为后端只补三处、且组织页的三个操作都已有服务端守卫，前端只需如实转述。

**撰写计划期间查证后、与设计文档不一致而在此修正的一处**：

1. **`GET /api/users` 的门槛**。设计文档 §4 B3 写「`grant:read`（全局目标）」，但 `grant:read` 在 `permission.go:62` 是 `ScopeOrg` 权限——组织管理员是在节点上持有它的，全局目标会把他们全部挡在门外，而他们正是这两个页面的主要用户。改为「在任意位置持有 `grant:read`」，复用 P1.4a 的 `holdsAnywhere`。这是 P1.4a D2 那个教训的第二次应用。

**撰写计划期间查证、设计文档未覆盖而在此定死的事项**：

2. **祖先链的实现手法**。设计文档只说「必须按分隔符匹配，不能用裸字符串前缀」。实际采用的是更强的做法：照搬 `authz.Resolver.ancestorChain`（`resolver.go:166`）把物化路径切成 ID 段后逐个**精确比较**，全程不做任何字符串前缀匹配。同前缀兄弟节点的陷阱因此在结构上就不可能发生，测试是用来钉住这个性质、而不是用来发现它。
3. **分类逻辑放在 store、不在 fake 里重做**。`ListEffectiveGrantsForOrg` 的 direct/inherited/global 判定活在真实 store 里，对着真实 Postgres 测（Task 1）；`fakeRBACStore` 只回放预置数据（Task 2 Step 1），让处理器测试专注于视图映射与错误处理。重做一遍的 fake 迟早会和真实实现说不同的话。
4. **`GrantAPI` 不需要 `OrgStore`**。一度打算给它加一个（为了取节点路径），后来发现路径查询完全可以留在 store 内部，构造函数因此不动——`NewGrantAPI` 的三个调用点（`app/control.go`、`grantapi_test.go`、`server_test.go`）都不用改。
