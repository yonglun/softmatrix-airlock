# Airlock P1.5c 交付包 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Airlock 变成一个能在完全无外网的机器上、照文档 2 小时内装成功的交付包。

**Architecture:** 一个多阶段 Dockerfile 产出单镜像（靠子命令区分 control/edge）；一份生产 compose 把镜像按架构无关的 index digest 钉死并全部 `pull_policy: never`；打包脚本在有网的构建机上 `docker save` 出 `images.tar`，安装脚本在客户的无网机器上 `docker load` 并按序完成前置检查、配置生成、起栈等健康、schema 初始化与自检。

**Tech Stack:** Docker buildx（跨架构）、docker compose v2、Bash、Go 1.25（两处小改动）、Next.js 静态导出。

**设计文档：** `docs/superpowers/specs/2026-09-06-airlock-p1.5c-release-package-design.md`

---

## 写计划期间补做的实测

设计文档 §0 之外，写计划时又验了两件事，都直接改变了实现：

**1. LiteLLM 的数据库凭据现在是硬编码的。** `deploy/litellm/config.yaml` 最后一行是
`database_url: postgresql://airlock:airlock_dev@postgres:5432/litellm`——**开发口令写死在文件里**。而 install.sh 会生成随机 Postgres 口令，照抄这份配置的话 LiteLLM 必然连不上库。

**2. 改用 `DATABASE_URL` 环境变量是可行的，已实测。** 起了一个探针容器：只给 `DATABASE_URL` 环境变量、config.yaml 里**完全不写** `database_url`，LiteLLM 正常跑了 `prisma migrate deploy` 连上库，`POST /key/generate` 返回 200。所以生产配置走环境变量，不依赖 `os.environ/` 插值对这个字段是否生效（那一点没验证过，不能赌）。

---

## 文件结构

**新建：**

| 文件 | 职责 |
|---|---|
| `Dockerfile` | 三阶段构建，产出单个 airlock 镜像 |
| `.dockerignore` | 把 `.git`、`node_modules`、宿主机 `web/dist` 等挡在构建上下文外 |
| `scripts/build-release.sh` | **构建机侧**（需外网）：出镜像、拉依赖、save、打 tar |
| `deploy/release/docker-compose.yml` | 生产栈，digest 钉死 + `pull_policy: never` |
| `deploy/release/.env.example` | 生产配置模板 |
| `deploy/release/install.sh` | **客户侧**（不需外网）：五步安装 |
| `deploy/release/schema/postgres-init.sql` | 建 litellm / casdoor 库，带存在性守卫 |
| `deploy/release/schema/clickhouse-init.sql` | 建 `airlock.usage_records` |
| `deploy/release/litellm/config.yaml` | 生产版上游配置，**不含** `database_url` |
| `internal/notify/disabled.go` | `DisabledSender` |
| `internal/notify/disabled_test.go` | 它的测试 |
| `docs/deploy/deployment.md` | 部署文档 |
| `docs/deploy/operations.md` | 运维手册 |

**修改：**

| 文件 | 改动 |
|---|---|
| `cmd/airlock/main.go` | `version` 变量、`version` 子命令、把版本交给 `app` |
| `internal/app/app.go`（新建，或放进已有文件） | 导出 `Version` 变量 |
| `internal/app/edge.go:80` | 启动日志带 `version` |
| `internal/app/control.go:295-297` | 启动日志带 `version`；SMTP 装配分叉 |
| `internal/config/config.go` | 去掉 `SMTP_ADDR` 默认值 |
| `internal/config/config_test.go:143` | 更新旧断言 |
| `Makefile` | 新增 `release` 目标 |

**关于 `app.Version`：** 版本在 `main` 里由 ldflags 注入，但启动日志在 `internal/app` 里打。用一个导出的 `app.Version` 变量（默认 `"dev"`），`main` 在分发子命令前赋值一次。这是 Go 里做版本注入的常见写法，且把版本放在「Control 启动」那一行上——那正是支持排障时人们 grep 的行。

---

## Task 1：版本标记

**Files:**
- Create: `internal/app/version.go`
- Modify: `cmd/airlock/main.go`
- Modify: `internal/app/edge.go:80`
- Modify: `internal/app/control.go:295-297`
- Test: `internal/app/version_test.go`

- [ ] **Step 1：写失败的测试**

`internal/app/version.go` 的测试 `internal/app/version_test.go`：

```go
package app

import (
	"testing"

	"github.com/stretchr/testify/require"
)

// 未经 ldflags 注入时必须是 "dev"，而不是空字符串——
// 空字符串在日志里会显示成 version=，让人以为是个 bug。
func TestVersionDefaultsToDev(t *testing.T) {
	require.Equal(t, "dev", Version)
}
```

- [ ] **Step 2：运行，确认失败**

Run: `go test ./internal/app/ -run TestVersionDefaults -v`
Expected: 编译失败，`undefined: Version`

- [ ] **Step 3：写实现**

`internal/app/version.go`：

```go
package app

// Version 是构建时注入的版本号。
//
// 值由 cmd/airlock 在分发子命令前赋进来，最终来自
// go build -ldflags "-X main.version=..."。默认 "dev" 而不是空串：
// 空串在日志里显示成 version=，看着像 bug。
//
// 私有化交付后「你装的是哪一版」是每次支持对话的第一个问题，
// 而无外网机器上没有别的办法回答它。
var Version = "dev"
```

- [ ] **Step 4：运行，确认通过**

Run: `go test ./internal/app/ -run TestVersionDefaults -v`
Expected: PASS

- [ ] **Step 5：改 main.go**

`cmd/airlock/main.go` 完整改成：

```go
// Command airlock 是 Airlock 的唯一分发物。
// 通过子命令区分进程角色：edge（数据面）、control（管理面）、migrate（数据库迁移）。
// 单二进制分发是为了杜绝私有化交付时 edge 与 control 版本错配。
package main

import (
	"fmt"
	"log/slog"
	"os"

	"github.com/softmatrix/airlock/internal/app"
)

// version 由构建时的 ldflags 注入：
//
//	go build -ldflags "-X main.version=v1.0.0"
//
// 未注入时保持 "dev"。
var version = "dev"

const usage = `airlock —— 企业 AI 网关

用法：
  airlock edge      启动数据面（ak- 鉴权、转发、计费）
  airlock control   启动管理面（OIDC 登录、组织树、成员）
  airlock migrate   执行数据库迁移后退出
  airlock version   打印版本号后退出
`

func main() {
	if len(os.Args) < 2 {
		fmt.Fprint(os.Stderr, usage)
		os.Exit(2)
	}

	// 交给 app 层，让两个进程的启动日志都带上版本。
	app.Version = version

	var err error
	switch os.Args[1] {
	case "edge":
		err = app.RunEdge()
	case "control":
		err = app.RunControl()
	case "migrate":
		err = app.RunMigrate()
	case "version":
		fmt.Println(version)
		return
	default:
		fmt.Fprintf(os.Stderr, "未知子命令 %q\n\n%s", os.Args[1], usage)
		os.Exit(2)
	}

	if err != nil {
		slog.Error("airlock 退出", "subcommand", os.Args[1], "err", err)
		os.Exit(1)
	}
}
```

- [ ] **Step 6：启动日志带上版本**

`internal/app/edge.go:80`，把

```go
		slog.Info("Edge 启动", "addr", cfg.EdgeListenAddr, "upstream", cfg.UpstreamBaseURL)
```

改成

```go
		slog.Info("Edge 启动", "version", Version,
			"addr", cfg.EdgeListenAddr, "upstream", cfg.UpstreamBaseURL)
```

`internal/app/control.go:295-297`，把

```go
		slog.Info("Control 启动",
			"addr", cfg.ControlListenAddr, "issuer", cfg.OIDCIssuer,
			"reconcile_interval", cfg.ReconcileInterval)
```

改成

```go
		slog.Info("Control 启动", "version", Version,
			"addr", cfg.ControlListenAddr, "issuer", cfg.OIDCIssuer,
			"reconcile_interval", cfg.ReconcileInterval)
```

- [ ] **Step 7：实测 ldflags 注入确实生效**

Run:
```bash
go build -ldflags "-X main.version=v1.0.0-test" -o /tmp/airlock-vtest ./cmd/airlock && /tmp/airlock-vtest version
```
Expected: 打印 `v1.0.0-test`

再验默认值：
```bash
go build -o /tmp/airlock-vdefault ./cmd/airlock && /tmp/airlock-vdefault version
```
Expected: 打印 `dev`

两个都验完后 `rm -f /tmp/airlock-vtest /tmp/airlock-vdefault`。

- [ ] **Step 8：跑全量 Go 测试**

Run: `go build ./... && go test ./internal/app/ ./internal/config/ -count=1`
Expected: PASS

- [ ] **Step 9：提交**

```bash
git add internal/app/version.go internal/app/version_test.go cmd/airlock/main.go internal/app/edge.go internal/app/control.go
git commit -m "feat: 版本标记与 version 子命令"
```

---

## Task 2：SMTP 留空即禁用

**Files:**
- Create: `internal/notify/disabled.go`
- Create: `internal/notify/disabled_test.go`
- Modify: `internal/config/config.go`
- Modify: `internal/config/config_test.go:143`
- Modify: `internal/app/control.go`

- [ ] **Step 1：写失败的测试**

`internal/notify/disabled_test.go`：

```go
package notify

import (
	"context"
	"testing"

	"github.com/stretchr/testify/require"
)

// 未配置 SMTP 时投递必须「成功」。
//
// 返回错误的话，ApprovalWorker.DeliverPending 会重试到上限再标 failed，
// outbox 里就堆满看起来像真故障的记录，运维每天都要来问一次。
func TestDisabledSenderReportsSuccess(t *testing.T) {
	err := DisabledSender{}.Send(context.Background(), Message{
		Recipient: "someone@example.com", Subject: "标题", Body: "正文",
	})

	require.NoError(t, err)
}

func TestDisabledSenderChannelIsEmail(t *testing.T) {
	// 入队时 channel 硬编码成 "email"（control/approval.go），
	// 接口注释要求 Channel() 与它一致，这里跟着写 email。
	require.Equal(t, "email", DisabledSender{}.Channel())
}

// 它必须能满足 Sender 接口，否则装配处编译不过。
func TestDisabledSenderSatisfiesSender(t *testing.T) {
	var _ Sender = DisabledSender{}
}
```

- [ ] **Step 2：运行，确认失败**

Run: `go test ./internal/notify/ -run TestDisabledSender -v`
Expected: 编译失败，`undefined: DisabledSender`

- [ ] **Step 3：写实现**

`internal/notify/disabled.go`：

```go
package notify

import (
	"context"
	"log/slog"
)

// DisabledSender 是未配置 SMTP 时的投递实现：只记一条日志，不发信。
//
// Send 返回 nil 而不是错误，是刻意的取舍。返回错误的话，
// ApprovalWorker.DeliverPending 会重试到 maxDeliverAttempts 耗尽再标
// failed，outbox 里堆满看起来像真故障的记录。
//
// 代价是 notifications.status 会被标成「已送达」，而实际没有邮件发出——
// 这一点在运维手册里写明。彻底干净的做法是把入队时硬编码的
// Channel: "email" 改成从 sender 取、禁用时记成 none，那要动审批流的
// 两处入队点，留作后续。
type DisabledSender struct{}

// Channel 与入队时硬编码的取值保持一致。
func (DisabledSender) Channel() string { return "email" }

func (DisabledSender) Send(_ context.Context, m Message) error {
	slog.Info("邮件通知未启用，跳过投递",
		"recipient", m.Recipient, "subject", m.Subject)
	return nil
}
```

- [ ] **Step 4：运行，确认通过**

Run: `go test ./internal/notify/ -count=1`
Expected: PASS

- [ ] **Step 5：改 config 的默认值**

`internal/config/config.go`，把

```go
		SMTPAddr:          or(getenv("SMTP_ADDR"), "localhost:1025"),
```

改成

```go
		// 留空即禁用邮件通知，与 LDAP_URL / CLICKHOUSE_DSN /
		// LITELLM_MASTER_KEY 同一口径。私有化部署的机器上常常
		// 根本没有 mail relay。
		SMTPAddr:          getenv("SMTP_ADDR"),
```

- [ ] **Step 6：更新旧断言**

`internal/config/config_test.go:143` 附近，把

```go
	require.Equal(t, "localhost:1025", cfg.SMTPAddr)
```

改成

```go
	// P1.5c：SMTP 改为留空即禁用，不再有默认值。
	require.Empty(t, cfg.SMTPAddr)
```

`SMTPFrom` 那行（`airlock@example.com`）**保持不动**——发件人有个默认值是无害的，禁用与否由 `SMTPAddr` 决定。

- [ ] **Step 7：装配处分叉**

`internal/app/control.go` 里现在是：

```go
		Sender: notify.NewSMTPSender(notify.SMTPConfig{
			Addr: cfg.SMTPAddr, From: cfg.SMTPFrom,
		}),
```

在 `approvalWorker := control.NewApprovalWorker(...)` **之前**插入：

```go
	// 邮件通知是可选功能：没配 SMTP_ADDR 时用空实现，只记日志不发信。
	// 与 LDAP_URL / CLICKHOUSE_DSN / LITELLM_MASTER_KEY 未配置时的
	// 处理同理——私有化部署的机器上常常根本没有 mail relay。
	var sender notify.Sender = notify.DisabledSender{}
	if cfg.SMTPAddr != "" {
		sender = notify.NewSMTPSender(notify.SMTPConfig{
			Addr: cfg.SMTPAddr, From: cfg.SMTPFrom,
		})
		slog.Info("邮件通知已启用", "smtp", cfg.SMTPAddr, "from", cfg.SMTPFrom)
	} else {
		slog.Warn("未配置 SMTP_ADDR，邮件通知未启用")
	}
```

然后把 `NewApprovalWorker` 里那段改成 `Sender: sender,`。

同时把下方那条日志

```go
	slog.Info("审批 worker 已启动",
		"interval", cfg.ApprovalWorkerInterval, "smtp", cfg.SMTPAddr)
```

改成

```go
	slog.Info("审批 worker 已启动", "interval", cfg.ApprovalWorkerInterval)
```

——SMTP 的状态已经在上面那两条里说清楚了，这里再带一个可能为空的 `smtp=` 只会让人困惑。

- [ ] **Step 8：跑测试**

Run: `go build ./... && go test ./internal/config/ ./internal/notify/ -count=1`
Expected: PASS

- [ ] **Step 9：实测两种模式的启动日志**

Run（不配 SMTP_ADDR）：
```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" \
AIRLOCK_ENCRYPTION_KEY="YWlybG9jay1kZXYtb25seS0zMmJ5dGUta2V5ISEhISE=" \
OIDC_ISSUER=http://localhost:8000 OIDC_CLIENT_ID=airlock \
OIDC_CLIENT_SECRET=airlock-dev-secret \
OIDC_REDIRECT_URL=http://localhost:8081/auth/callback \
AIRLOCK_BOOTSTRAP_ADMIN=admin@example.com \
CONTROL_LISTEN_ADDR=:8091 \
timeout 8 go run ./cmd/airlock control 2>&1 | grep -E "SMTP|邮件"
```
Expected: 出现 `WARN 未配置 SMTP_ADDR，邮件通知未启用`

再加 `SMTP_ADDR=localhost:1025` 重跑同一条命令。
Expected: 出现 `INFO 邮件通知已启用 smtp=localhost:1025`

- [ ] **Step 10：提交**

```bash
git add internal/notify/disabled.go internal/notify/disabled_test.go internal/config/config.go internal/config/config_test.go internal/app/control.go
git commit -m "feat: 未配置 SMTP_ADDR 时禁用邮件通知"
```

---

## Task 3：.dockerignore 与 Dockerfile

**Files:**
- Create: `.dockerignore`
- Create: `Dockerfile`

- [ ] **Step 1：写 .dockerignore**

`.dockerignore`：

```
# 构建上下文越小越好：.git 有几十 MB，node_modules 更大，
# 而且它们都会在镜像里被重新生成或根本用不到。
.git
.gitignore
.claude
.superpowers

node_modules
web/node_modules

# 宿主机的构建产物。web/dist 尤其重要：它由 console 阶段重新生成，
# 带进去只会让构建缓存失效，还可能把一份过期的控制台塞进镜像。
web/dist
bin
build
dist

docs
deploy
*.log
*.key
.env
.env.*
coverage.out
```

- [ ] **Step 2：写 Dockerfile**

`Dockerfile`：

```dockerfile
# syntax=docker/dockerfile:1

# Airlock 单镜像：靠子命令区分 control / edge / migrate 三个角色。
#
# 前两个阶段固定用 $BUILDPLATFORM——也就是构建机的原生架构——只有最终
# 镜像是 $TARGETPLATFORM。Go 的 CGO_ENABLED=0 交叉编译零成本，而让 QEMU
# 模拟目标架构去跑 Next.js 构建会把时间从分钟级拖到十几分钟，且 QEMU
# 偶发段错误会把人引向完全错误的排查方向。

# ---- 阶段一：构建控制台静态站 ----
FROM --platform=$BUILDPLATFORM node:22-alpine AS console
WORKDIR /src/web
# 先只拷依赖清单，让 npm ci 这一层能被缓存
COPY web/package.json web/package-lock.json ./
RUN npm ci
COPY web/ ./
RUN npm run build

# ---- 阶段二：编译 Go 二进制 ----
FROM --platform=$BUILDPLATFORM golang:1.25-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
# web/dist 在 .dockerignore 里被排掉了，这里从 console 阶段取
COPY --from=console /src/web/dist ./web/dist

ARG TARGETARCH
ARG VERSION=dev
RUN CGO_ENABLED=0 GOOS=linux GOARCH=${TARGETARCH} \
    go build -trimpath -ldflags "-s -w -X main.version=${VERSION}" \
    -o /out/airlock ./cmd/airlock

# ---- 阶段三：运行时 ----
FROM alpine:3.21
# ca-certificates 不能省：OIDC 要连客户的 IdP，多半是 HTTPS，
# 缺了它就是一个极难查的 x509: certificate signed by unknown authority。
# wget 用于 compose 的 healthcheck（alpine 自带 busybox wget）。
RUN apk add --no-cache ca-certificates tzdata \
    && adduser -D -u 10001 airlock
COPY --from=build /out/airlock /usr/local/bin/airlock
USER airlock
ENTRYPOINT ["/usr/local/bin/airlock"]
```

- [ ] **Step 3：构建本机架构的镜像**

Run: `docker build --build-arg VERSION=v0.0.0-test -t airlock:build-test .`
Expected: 构建成功。首次会比较慢（要装 npm 依赖与下载 Go 模块）。

- [ ] **Step 4：验证镜像里的二进制能跑、版本注入生效**

Run: `docker run --rm airlock:build-test version`
Expected: 打印 `v0.0.0-test`

Run: `docker run --rm airlock:build-test` （不带子命令）
Expected: 打印用法说明，退出码 2

- [ ] **Step 5：验证控制台确实被嵌进去了**

镜像里没有 shell 之外的工具能直接看 embed 内容，改用间接办法——起 control 并请求首页。这一步需要数据库，放到 Task 9 的整栈验证里做；这里只确认二进制体积明显包含了前端产物：

Run: `docker run --rm --entrypoint sh airlock:build-test -c "ls -lh /usr/local/bin/airlock"`
Expected: 体积在 25 MB 以上（纯 Go 二进制约 20 MB，控制台静态站再加几 MB）。若只有十几 MB，说明 `COPY --from=console` 没生效，embed 到的是空目录。

- [ ] **Step 6：验证跨架构构建可行**

Run: `docker build --platform linux/amd64 --build-arg VERSION=v0.0.0-test -t airlock:build-test-amd64 .`
Expected: 构建成功（构建阶段仍跑在本机 arm64 上，只有最终镜像是 amd64）

Run: `docker image inspect airlock:build-test-amd64 --format '{{.Architecture}}'`
Expected: `amd64`

- [ ] **Step 7：清理测试镜像**

Run: `docker rmi airlock:build-test airlock:build-test-amd64`

- [ ] **Step 8：提交**

```bash
git add Dockerfile .dockerignore
git commit -m "build: Airlock 单镜像的多阶段 Dockerfile"
```

---

## Task 4：schema 初始化脚本

**Files:**
- Create: `deploy/release/schema/postgres-init.sql`
- Create: `deploy/release/schema/clickhouse-init.sql`

- [ ] **Step 1：写 Postgres 脚本**

`deploy/release/schema/postgres-init.sql`：

```sql
-- 建 LiteLLM 与 Casdoor 各自的库。
--
-- 不放 docker-entrypoint-initdb.d：那个钩子只在数据卷为空的首次启动时
-- 执行，安装失败后重跑（卷还在）就会被静默跳过，是最难查的一类故障。
-- 改由 install.sh 显式执行，因此这里必须幂等。
--
-- Postgres 的 CREATE DATABASE 没有 IF NOT EXISTS，也不能放进 DO 块
-- （CREATE DATABASE 不能在事务里跑），所以用 \gexec：先 SELECT 出需要
-- 执行的语句，再由 psql 逐条执行；已存在时 SELECT 不返回行，什么都不做。
SELECT 'CREATE DATABASE litellm'
 WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = 'litellm')\gexec

SELECT 'CREATE DATABASE casdoor'
 WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = 'casdoor')\gexec
```

- [ ] **Step 2：写 ClickHouse 脚本**

`deploy/release/schema/clickhouse-init.sql`：

```sql
-- 用量记录表。Go 侧从不创建它（internal/usage 里没有任何 CREATE TABLE），
-- 所以这份脚本是它唯一的来源。全部 IF NOT EXISTS，可重复执行。
CREATE DATABASE IF NOT EXISTS airlock;

CREATE TABLE IF NOT EXISTS airlock.usage_records
(
    request_id          String,
    ts                  DateTime64(3),
    org_id              String,
    user_id             String,
    key_id              String,
    provider            LowCardinality(String),
    model               LowCardinality(String),
    input_tokens        Int64,
    cached_input_tokens Int64,
    output_tokens       Int64,
    reasoning_tokens    Int64,
    cost_micro          Int64,
    status_code         UInt16,
    latency_ms          UInt32,
    ttft_ms             UInt32,
    stream              UInt8,
    error_type          LowCardinality(String)
)
ENGINE = MergeTree
PARTITION BY toYYYYMM(ts)
ORDER BY (org_id, ts, request_id);
```

> 这份表结构必须与 `deploy/clickhouse/init/01_usage.sql` 完全一致。落地时用
> `diff <(tail -n +2 deploy/clickhouse/init/01_usage.sql) <(tail -n +4 deploy/release/schema/clickhouse-init.sql)`
> 之类的方式核对字段，不要凭眼睛看。

- [ ] **Step 3：实测 Postgres 脚本幂等**

先确认现有开发库里 litellm/casdoor 都已存在，然后跑两遍脚本，两遍都该无错：

```bash
docker exec -i deploy-postgres-1 psql -U airlock -d postgres < deploy/release/schema/postgres-init.sql
docker exec -i deploy-postgres-1 psql -U airlock -d postgres < deploy/release/schema/postgres-init.sql
```
Expected: 两次都无错误输出（库已存在，`\gexec` 不产生任何语句）

- [ ] **Step 4：实测 ClickHouse 脚本幂等**

```bash
docker exec -i deploy-clickhouse-1 clickhouse-client --multiquery < deploy/release/schema/clickhouse-init.sql
docker exec -i deploy-clickhouse-1 clickhouse-client --multiquery < deploy/release/schema/clickhouse-init.sql
```
Expected: 两次都无错误输出

再确认表结构没被改坏：
```bash
docker exec deploy-clickhouse-1 clickhouse-client --query "SELECT count(*) FROM airlock.usage_records"
```
Expected: 返回一个数字（现有数据条数），不报错

- [ ] **Step 5：提交**

```bash
git add deploy/release/schema/
git commit -m "feat: 生产环境的 schema 初始化脚本，幂等可重跑"
```

---

## Task 5：生产 LiteLLM 配置

**Files:**
- Create: `deploy/release/litellm/config.yaml`

- [ ] **Step 1：写配置**

`deploy/release/litellm/config.yaml`：

```yaml
# 生产版上游模型配置。
#
# 与 deploy/litellm/config.yaml（开发栈）的关键差别：**不含 database_url**。
# 开发那份把 postgresql://airlock:airlock_dev@... 硬编码在文件里，而
# install.sh 会生成随机的 Postgres 口令，照抄必然连不上库。
# 生产改用 DATABASE_URL 环境变量——已实测：只给环境变量、配置里不写
# database_url 时，LiteLLM 正常执行 prisma migrate deploy 并连上库。
#
# 客户要接自己的模型时改这个文件，然后 docker compose restart litellm。

model_list:
  - model_name: deepseek-chat
    litellm_params:
      model: deepseek/deepseek-chat
      api_key: os.environ/DEEPSEEK_API_KEY

  - model_name: qwen-plus
    litellm_params:
      model: openai/qwen-plus
      api_base: https://dashscope.aliyuncs.com/compatible-mode/v1
      api_key: os.environ/DASHSCOPE_API_KEY

  - model_name: gpt-4o-mini
    litellm_params:
      model: openai/gpt-4o-mini
      api_key: os.environ/OPENAI_API_KEY

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
```

（开发栈里的 `gpt-5.6-luna` 是 Azure 联调用的条目，不进生产模板。）

- [ ] **Step 2：提交**

```bash
git add deploy/release/litellm/config.yaml
git commit -m "feat: 生产版 LiteLLM 配置，数据库凭据改走环境变量"
```

---

## Task 6：生产 compose

**Files:**
- Create: `deploy/release/docker-compose.yml`

- [ ] **Step 1：写 compose**

`deploy/release/docker-compose.yml`：

```yaml
# Airlock 生产栈。与 deploy/docker-compose.yml（开发栈）是两套独立文件：
# 受众不同、跑法也不同，合并只会两边都难读。
#
# 三条与开发栈的关键差别：
#   1. 镜像按 index digest 钉死，钉的是 P1.1–P1.5b 全程实测跑通的那一份
#   2. 每个服务 pull_policy: never——无外网机器上镜像缺失要立刻失败，
#      而不是卡五分钟 DNS 超时再抛一个看着毫无关系的错误
#   3. 数据依赖不对宿主机暴露端口，只在 compose 网络内可达

services:
  postgres:
    image: postgres:17@sha256:67f41722b7a8cbdb868a44a4995c846eddfdc2973bccb291ce937dce88ad5675
    pull_policy: never
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      timeout: 5s
      retries: 30

  clickhouse:
    image: clickhouse/clickhouse-server:25.3@sha256:b627d7a9bc0e0c1bac26cdbe9d2fc6316faa29c5d8a174f28f5abd57d0fa6ba2
    pull_policy: never
    restart: unless-stopped
    environment:
      CLICKHOUSE_USER: ${CLICKHOUSE_USER}
      CLICKHOUSE_PASSWORD: ${CLICKHOUSE_PASSWORD}
      CLICKHOUSE_DB: ${CLICKHOUSE_DB}
    volumes:
      - chdata:/var/lib/clickhouse
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 5s
      timeout: 5s
      retries: 30

  litellm:
    image: ghcr.io/berriai/litellm:main-stable@sha256:20b5044b619055374061a6d5b7b08754cad75aeabbf82ddf4f69cc0cf80ddaf4
    pull_policy: never
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
      # 配置文件里刻意不写 database_url，改走这个环境变量——
      # 否则口令要硬编码进 config.yaml，而口令是安装时随机生成的。
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/litellm
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      DASHSCOPE_API_KEY: ${DASHSCOPE_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
    command: ["--config", "/app/config.yaml", "--port", "4000"]
    volumes:
      - ./litellm/config.yaml:/app/config.yaml:ro

  casdoor:
    image: casbin/casdoor:latest@sha256:f332047c325588fb047bc1a34b0ce473f12ac044c0255a520f30f521fd7c249b
    pull_policy: never
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      RUNNING_IN_DOCKER: "true"
      driverName: postgres
      dataSourceName: "user=${POSTGRES_USER} password=${POSTGRES_PASSWORD} host=postgres port=5432 sslmode=disable dbname=casdoor"
      tableNamePrefix: casdoor_
      # 浏览器要能访问到这个地址，所以它必须是对外的主机名/IP，
      # 不能写 localhost（除非就在这台机器上用浏览器）。
      origin: ${CASDOOR_ORIGIN}
    ports:
      - "${CASDOOR_PORT}:8000"

  airlock-control:
    image: airlock:${AIRLOCK_VERSION}
    pull_policy: never
    restart: unless-stopped
    command: ["control"]
    depends_on:
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
    environment:
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      CLICKHOUSE_DSN: clickhouse://${CLICKHOUSE_USER}:${CLICKHOUSE_PASSWORD}@clickhouse:9000/${CLICKHOUSE_DB}
      AIRLOCK_ENCRYPTION_KEY: ${AIRLOCK_ENCRYPTION_KEY}
      CONTROL_LISTEN_ADDR: ":8081"
      OIDC_ISSUER: ${OIDC_ISSUER}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET}
      OIDC_REDIRECT_URL: ${OIDC_REDIRECT_URL}
      AIRLOCK_BOOTSTRAP_ADMIN: ${AIRLOCK_BOOTSTRAP_ADMIN}
      AIRLOCK_LICENSE_FILE: ${AIRLOCK_LICENSE_FILE}
      LITELLM_BASE_URL: http://litellm:4000
      LITELLM_MASTER_KEY: ${LITELLM_MASTER_KEY}
      SMTP_ADDR: ${SMTP_ADDR}
      SMTP_FROM: ${SMTP_FROM}
      LDAP_URL: ${LDAP_URL}
      LDAP_BIND_DN: ${LDAP_BIND_DN}
      LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD}
      LDAP_BASE_DN: ${LDAP_BASE_DN}
    volumes:
      # 授权文件。留空时 AIRLOCK_LICENSE_FILE 也为空，control 走试用模式。
      - ${LICENSE_HOST_PATH:-/dev/null}:/etc/airlock/license.txt:ro
    ports:
      - "${CONTROL_PORT}:8081"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8081/healthz"]
      interval: 5s
      timeout: 5s
      retries: 30

  airlock-edge:
    image: airlock:${AIRLOCK_VERSION}
    pull_policy: never
    restart: unless-stopped
    command: ["edge"]
    depends_on:
      postgres:
        condition: service_healthy
      clickhouse:
        condition: service_healthy
      airlock-control:
        condition: service_healthy
    environment:
      POSTGRES_DSN: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      CLICKHOUSE_DSN: clickhouse://${CLICKHOUSE_USER}:${CLICKHOUSE_PASSWORD}@clickhouse:9000/${CLICKHOUSE_DB}
      AIRLOCK_ENCRYPTION_KEY: ${AIRLOCK_ENCRYPTION_KEY}
      EDGE_LISTEN_ADDR: ":8080"
      EDGE_UPSTREAM_BASE_URL: http://litellm:4000
    ports:
      - "${EDGE_PORT}:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:8080/healthz"]
      interval: 5s
      timeout: 5s
      retries: 30

volumes:
  pgdata:
  chdata:
```

- [ ] **Step 2：校验 compose 语法**

Run:
```bash
docker compose -f deploy/release/docker-compose.yml --env-file deploy/release/.env.example config >/dev/null && echo "compose 语法正确"
```
Expected: 打印 `compose 语法正确`。这一步依赖 Task 7 的 `.env.example`——若尚未创建，先做 Task 7 再回来跑。

- [ ] **Step 3：确认 digest 与实测跑通的那份一致**

Run:
```bash
for img in postgres:17 clickhouse/clickhouse-server:25.3 ghcr.io/berriai/litellm:main-stable casbin/casdoor:latest; do
  printf "%-45s %s\n" "$img" "$(docker image inspect "$img" --format '{{index .RepoDigests 0}}')"
done
```
Expected: 四行 digest 与 compose 里写的完全一致。若不一致，说明本地镜像被重新拉过——**不要**改 compose 去迁就新 digest，那等于钉一个没跑过的版本；应先重跑一轮回归再决定。

- [ ] **Step 4：提交**

```bash
git add deploy/release/docker-compose.yml
git commit -m "feat: 生产 compose，digest 钉死且离线快速失败"
```

---

## Task 7：生产配置模板

**Files:**
- Create: `deploy/release/.env.example`

- [ ] **Step 1：写模板**

`deploy/release/.env.example`：

```bash
# Airlock 生产配置模板。
#
# install.sh 会以它为蓝本生成 .env，并**自动填入随机生成的**口令与加密
# 密钥（下面标了「安装时自动生成」的三项）。标了「必须人工填写」的几项
# 装完之后必须手工补齐，否则登录或模型调用会失败。

# ---- 版本（install.sh 从 VERSION 文件写入，不要手改）----
AIRLOCK_VERSION=

# ---- 安装时自动生成，不要手改 ----
POSTGRES_PASSWORD=
CLICKHOUSE_PASSWORD=
# 32 字节的 base64。换掉它会导致所有已签发密钥无法解密。
AIRLOCK_ENCRYPTION_KEY=
LITELLM_MASTER_KEY=

# ---- 数据库基本信息 ----
POSTGRES_USER=airlock
POSTGRES_DB=airlock
CLICKHOUSE_USER=airlock
CLICKHOUSE_DB=airlock

# ---- 对外端口 ----
# 控制台与管理 API
CONTROL_PORT=8081
# 数据面：应用调用 AI 的入口
EDGE_PORT=8080
# 自带的身份提供方
CASDOOR_PORT=8000

# ---- 必须人工填写：身份认证 ----
# 浏览器能访问到的 Casdoor 地址，形如 http://airlock.example.com:8000
# 写 localhost 只在「就在这台机器上开浏览器」时可用。
CASDOOR_ORIGIN=http://localhost:8000
OIDC_ISSUER=http://localhost:8000
OIDC_CLIENT_ID=airlock
OIDC_CLIENT_SECRET=
# 必须与浏览器访问控制台的地址一致，形如 http://airlock.example.com:8081/auth/callback
OIDC_REDIRECT_URL=http://localhost:8081/auth/callback
# 首个平台管理员的 email 或 OIDC sub。
# 系统内无管理员且此项为空时，control 拒绝启动。
AIRLOCK_BOOTSTRAP_ADMIN=

# ---- 必须人工填写：上游模型供应商 ----
# 至少填一个，否则所有模型调用都会拿到上游 401。
DEEPSEEK_API_KEY=
DASHSCOPE_API_KEY=
OPENAI_API_KEY=

# ---- 授权文件 ----
# 宿主机上 license 文件的绝对路径。留空则以试用模式运行（5 席位、不设到期）。
LICENSE_HOST_PATH=
# 容器内路径，与上面的挂载点对应。LICENSE_HOST_PATH 留空时这一项也要留空。
AIRLOCK_LICENSE_FILE=

# ---- 可选：邮件通知 ----
# 留空则不发信，审批仍可在控制台完成。填企业内网 relay，形如 smtp.example.com:25
SMTP_ADDR=
SMTP_FROM=airlock@example.com

# ---- 可选：LDAP 离职对账 ----
# 留空则不启用。
LDAP_URL=
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=
LDAP_BASE_DN=
```

- [ ] **Step 2：回头补做 Task 6 Step 2 的 compose 语法校验**

Run:
```bash
docker compose -f deploy/release/docker-compose.yml --env-file deploy/release/.env.example config >/dev/null && echo "compose 语法正确"
```
Expected: 打印 `compose 语法正确`

- [ ] **Step 3：确认模板覆盖了 compose 引用的每一个变量**

Run:
```bash
grep -oE '\$\{[A-Z_]+' deploy/release/docker-compose.yml | tr -d '${' | sort -u > /tmp/vars-used.txt
grep -oE '^[A-Z_]+=' deploy/release/.env.example | tr -d '=' | sort -u > /tmp/vars-declared.txt
comm -23 /tmp/vars-used.txt /tmp/vars-declared.txt
```
Expected: 无输出（compose 用到的变量全都在模板里声明了）。若有输出，把缺的补进模板。

- [ ] **Step 4：提交**

```bash
git add deploy/release/.env.example
git commit -m "feat: 生产配置模板"
```

---

## Task 8：安装脚本

**Files:**
- Create: `deploy/release/install.sh`

这是整个交付包里最要紧的一个文件。验收标准的「每个错误都有明确排查指引」几乎全落在它身上——**每一处失败都必须说清该做什么，而不是只抛一个退出码**。

- [ ] **Step 1：写脚本**

`deploy/release/install.sh`：

```bash
#!/usr/bin/env bash
#
# Airlock 一键安装。在客户的机器上运行，**不需要外网**。
#
# 五步：前置检查 → 导入镜像 → 生成配置 → 起栈等健康 → 初始化 schema 并自检。
# 可重复执行：不覆盖已有 .env，schema 步骤幂等，重复导入镜像无副作用。

set -euo pipefail

cd "$(dirname "$0")"

RED=$'\033[31m'; YELLOW=$'\033[33m'; GREEN=$'\033[32m'; RESET=$'\033[0m'

info()  { echo "${GREEN}==>${RESET} $*"; }
warn()  { echo "${YELLOW}警告:${RESET} $*"; }
# die 的第二个参数起是「该怎么办」，必须给，不能只报错不给出路。
die() {
    echo "${RED}错误:${RESET} $1" >&2
    shift
    if [ $# -gt 0 ]; then
        echo "" >&2
        echo "该怎么办：" >&2
        for line in "$@"; do echo "  - $line" >&2; done
    fi
    exit 1
}

# ---------- 第 1 步：前置检查 ----------
step_precheck() {
    info "第 1/5 步：前置检查"

    command -v docker >/dev/null 2>&1 || die \
        "未找到 docker 命令" \
        "先安装 Docker Engine 20.10 或更高版本" \
        "安装后执行 docker info 确认守护进程在运行"

    docker info >/dev/null 2>&1 || die \
        "docker 守护进程没有响应" \
        "执行 systemctl start docker 启动守护进程" \
        "若当前用户不在 docker 组，改用 sudo 运行本脚本，或把用户加入 docker 组后重新登录"

    docker compose version >/dev/null 2>&1 || die \
        "未找到 docker compose 插件（v2）" \
        "安装 docker-compose-plugin 包" \
        "注意本脚本要的是 docker compose（带空格），不是旧的 docker-compose"

    # openssl 用于生成随机口令与加密密钥。最小化安装的 Linux 上未必有，
    # 而这台机器没有外网、装不了包——必须在动任何东西之前就问清楚。
    command -v openssl >/dev/null 2>&1 || die \
        "未找到 openssl 命令" \
        "安装 openssl 包（RHEL/CentOS: yum install openssl；Debian/Ubuntu: apt install openssl）" \
        "本脚本用它生成数据库口令与加密密钥，没有替代方案"

    [ -f VERSION ] || die \
        "当前目录下没有 VERSION 文件" \
        "确认已解压完整的交付包，并在解压出来的目录里运行 ./install.sh"

    local pkg_arch host_arch
    pkg_arch=$(grep '^ARCH=' VERSION | cut -d= -f2-)
    host_arch=$(uname -m)
    # uname 说 x86_64/aarch64，Docker 说 amd64/arm64，先归一化
    case "$host_arch" in
        x86_64)  host_arch=amd64 ;;
        aarch64) host_arch=arm64 ;;
    esac

    if [ "$pkg_arch" != "$host_arch" ]; then
        die "交付包的架构与本机不符：包是 ${pkg_arch}，本机是 ${host_arch}（uname -m 报 $(uname -m)）" \
            "向供应方索取 ${host_arch} 架构的交付包" \
            "强行安装会在容器启动时报 exec format error，那个错误与真实原因看不出关系"
    fi

    info "  docker 与 compose 就绪，架构匹配（${pkg_arch}）"
}

# ---------- 第 2 步：导入镜像 ----------
step_load_images() {
    info "第 2/5 步：导入镜像"

    [ -f images.tar ] || die \
        "当前目录下没有 images.tar" \
        "确认交付包解压完整（tar 包里应含 images.tar，约 1 GB）" \
        "若下载中断，重新获取完整的 tar.gz 后再解压"

    docker load -i images.tar || die \
        "docker load 失败" \
        "检查磁盘剩余空间：docker 需要约 3 GB 可用空间来解压镜像" \
        "检查 images.tar 是否完整（大小应与交付说明一致）"

    # 逐个确认镜像真的在本地了。compose 设了 pull_policy: never，
    # 缺任何一个都会在起栈时立刻失败，不如在这里先说清楚缺的是哪个。
    local missing=0
    while IFS= read -r ref; do
        [ -n "$ref" ] || continue
        if ! docker image inspect "$ref" >/dev/null 2>&1; then
            echo "  缺少镜像: $ref" >&2
            missing=1
        fi
    done < <(grep '^IMAGE=' VERSION | cut -d= -f2-)

    [ "$missing" -eq 0 ] || die \
        "有镜像未成功导入" \
        "重新执行本脚本（重复导入无副作用）" \
        "若仍然缺失，说明 images.tar 不完整，请重新获取交付包"

    info "  镜像已全部导入"
}

# ---------- 第 3 步：生成配置 ----------
step_config() {
    info "第 3/5 步：生成配置"

    if [ -f .env ]; then
        warn ".env 已存在，保持不变（不覆盖已有配置）"
    else
        [ -f .env.example ] || die \
            "找不到 .env.example" \
            "确认交付包解压完整"

        cp .env.example .env

        # 随机口令与密钥。模板里的空值绝不能带进生产。
        local pg_pass ch_pass enc_key llm_key
        pg_pass=$(openssl rand -hex 24)
        ch_pass=$(openssl rand -hex 24)
        enc_key=$(openssl rand -base64 32)
        llm_key="sk-$(openssl rand -hex 24)"

        # 用 | 作分隔符：base64 里可能出现 /，用 / 会把 sed 打断
        sed -i.bak "s|^POSTGRES_PASSWORD=.*|POSTGRES_PASSWORD=${pg_pass}|"        .env
        sed -i.bak "s|^CLICKHOUSE_PASSWORD=.*|CLICKHOUSE_PASSWORD=${ch_pass}|"    .env
        sed -i.bak "s|^AIRLOCK_ENCRYPTION_KEY=.*|AIRLOCK_ENCRYPTION_KEY=${enc_key}|" .env
        sed -i.bak "s|^LITELLM_MASTER_KEY=.*|LITELLM_MASTER_KEY=${llm_key}|"      .env
        rm -f .env.bak

        chmod 600 .env
        info "  已生成 .env，随机口令与加密密钥已填入"
    fi

    # 版本号每次都从 VERSION 同步——升级后重跑脚本时它必须跟着变。
    local ver
    ver=$(grep '^VERSION=' VERSION | cut -d= -f2-)
    sed -i.bak "s|^AIRLOCK_VERSION=.*|AIRLOCK_VERSION=${ver}|" .env
    rm -f .env.bak

    # 必填项检查。这些填不上，装完也用不了。
    local missing=()
    grep -q '^AIRLOCK_BOOTSTRAP_ADMIN=.\+' .env || missing+=("AIRLOCK_BOOTSTRAP_ADMIN（首个平台管理员的 email）")
    grep -q '^OIDC_CLIENT_SECRET=.\+'      .env || missing+=("OIDC_CLIENT_SECRET（Casdoor 里 airlock 应用的密钥）")
    if ! grep -qE '^(DEEPSEEK|DASHSCOPE|OPENAI)_API_KEY=.\+' .env; then
        missing+=("至少一个模型供应商密钥（DEEPSEEK_API_KEY / DASHSCOPE_API_KEY / OPENAI_API_KEY）")
    fi

    if [ ${#missing[@]} -gt 0 ]; then
        echo ""
        warn "以下配置项还没填，现在用文本编辑器打开 $(pwd)/.env 补上，然后重新运行本脚本："
        for m in "${missing[@]}"; do echo "    - $m"; done
        echo ""
        echo "填好之后执行： ./install.sh"
        exit 3
    fi

    # license 的两项必须同时设或同时留空。只设 AIRLOCK_LICENSE_FILE 的话，
    # 容器里那个路径挂的是 /dev/null，control 会读到空文件并以「签名校验失败」
    # 拒绝启动——那个错误指向的方向完全不对。
    local host_lic ctr_lic
    host_lic=$(grep '^LICENSE_HOST_PATH=' .env | cut -d= -f2-)
    ctr_lic=$(grep '^AIRLOCK_LICENSE_FILE=' .env | cut -d= -f2-)
    if [ -n "$host_lic" ] && [ -z "$ctr_lic" ]; then
        die "配置了 LICENSE_HOST_PATH 但没配 AIRLOCK_LICENSE_FILE" \
            "在 .env 里设 AIRLOCK_LICENSE_FILE=/etc/airlock/license.txt" \
            "两项要么都填，要么都留空（留空即试用模式）"
    fi
    if [ -z "$host_lic" ] && [ -n "$ctr_lic" ]; then
        die "配置了 AIRLOCK_LICENSE_FILE 但没配 LICENSE_HOST_PATH" \
            "在 .env 里把 LICENSE_HOST_PATH 设成主机上 license 文件的绝对路径" \
            "或把 AIRLOCK_LICENSE_FILE 也清空，以试用模式运行"
    fi
    if [ -n "$host_lic" ] && [ ! -f "$host_lic" ]; then
        die "LICENSE_HOST_PATH 指向的文件不存在：${host_lic}" \
            "确认路径是主机上的绝对路径，且文件确实存在" \
            "不需要授权时把 LICENSE_HOST_PATH 与 AIRLOCK_LICENSE_FILE 都留空"
    fi

    info "  配置检查通过"
}

# ---------- 第 4 步：起栈并等健康 ----------
wait_healthy() {
    local svc=$1 timeout=${2:-180} waited=0
    while [ "$waited" -lt "$timeout" ]; do
        local cid status
        cid=$(docker compose ps -q "$svc" 2>/dev/null || true)
        if [ -n "$cid" ]; then
            status=$(docker inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}' "$cid" 2>/dev/null || echo "none")
            [ "$status" = "healthy" ] && return 0
            # 没有 healthcheck 的服务，跑起来就算数
            if [ "$status" = "none" ] && [ "$(docker inspect --format '{{.State.Running}}' "$cid")" = "true" ]; then
                return 0
            fi
        fi
        sleep 3
        waited=$((waited + 3))
    done

    echo "" >&2
    echo "${RED}服务 ${svc} 在 ${timeout} 秒内没有变健康。最后 50 行日志：${RESET}" >&2
    docker compose logs --tail=50 "$svc" >&2 || true
    return 1
}

step_up() {
    info "第 4/5 步：启动服务"

    docker compose up -d || die \
        "docker compose up 失败" \
        "若报镜像不存在，重新执行本脚本让第 2 步重新导入" \
        "若报端口被占用，改 .env 里的 CONTROL_PORT / EDGE_PORT / CASDOOR_PORT"

    for svc in postgres clickhouse litellm casdoor airlock-control airlock-edge; do
        info "  等待 ${svc} 就绪…"
        wait_healthy "$svc" || die \
            "${svc} 启动失败（日志见上）" \
            "对照运维手册的故障排查表定位上面的报错" \
            "修正 .env 后重新执行本脚本"
    done

    info "  全部服务已就绪"
}

# ---------- 第 5 步：初始化 schema 并自检 ----------
step_schema() {
    info "第 5/5 步：初始化 schema 并自检"

    # 一律用 -f2-（不是 -f2）：base64 的 padding 与 URL 查询串里都可能出现 =，
    # 截断了就会拿到半截口令，且错误现象是「认证失败」，与真实原因看不出关系。
    local pg_user ch_user ch_pass
    pg_user=$(grep '^POSTGRES_USER=' .env | cut -d= -f2-)
    ch_user=$(grep '^CLICKHOUSE_USER=' .env | cut -d= -f2-)
    ch_pass=$(grep '^CLICKHOUSE_PASSWORD=' .env | cut -d= -f2-)

    docker compose exec -T postgres psql -U "$pg_user" -d postgres < schema/postgres-init.sql >/dev/null || die \
        "创建 litellm / casdoor 数据库失败" \
        "确认 postgres 容器健康：docker compose ps postgres" \
        "查看日志：docker compose logs postgres"

    docker compose exec -T clickhouse clickhouse-client \
        --user "$ch_user" --password "$ch_pass" --multiquery < schema/clickhouse-init.sql >/dev/null || die \
        "创建 ClickHouse 用量表失败" \
        "确认 clickhouse 容器健康：docker compose ps clickhouse" \
        "查看日志：docker compose logs clickhouse"

    # 自检一：用量表确实存在。它只由上面这一步创建，Go 侧从不建表，
    # 漏了的话数据面会在第一次记账时失败。
    docker compose exec -T clickhouse clickhouse-client \
        --user "$ch_user" --password "$ch_pass" \
        --query "SELECT count(*) FROM airlock.usage_records" >/dev/null || die \
        "用量表 airlock.usage_records 不存在" \
        "重新执行本脚本（schema 步骤幂等，可安全重跑）"

    # 自检二：两个进程的健康端点都答话。
    # 走容器内的 wget 而不是宿主机的 curl——最小化安装的 Linux 上未必有 curl，
    # 而 alpine 基础镜像里的 busybox 一定带 wget。
    docker compose exec -T airlock-control \
        wget -q -O- http://localhost:8081/healthz >/dev/null || die \
        "控制台健康检查失败" \
        "查看日志：docker compose logs airlock-control"
    docker compose exec -T airlock-edge \
        wget -q -O- http://localhost:8080/healthz >/dev/null || die \
        "数据面健康检查失败" \
        "查看日志：docker compose logs airlock-edge"

    info "  schema 就绪，自检通过"
}

# ---------- 主流程 ----------
main() {
    echo ""
    echo "Airlock 安装程序"
    echo "================"
    echo ""

    step_precheck
    step_load_images
    step_config
    step_up
    step_schema

    local control_port casdoor_origin
    control_port=$(grep '^CONTROL_PORT=' .env | cut -d= -f2-)
    casdoor_origin=$(grep '^CASDOOR_ORIGIN=' .env | cut -d= -f2-)

    echo ""
    echo "${GREEN}安装完成。${RESET}"
    echo ""
    echo "控制台：      http://localhost:${control_port}"
    echo "身份提供方：  ${casdoor_origin}"
    echo ""
    echo "接下来："
    echo "  1. 在 Casdoor 里创建 airlock 应用与首批用户（见部署文档「配置身份提供方」）"
    echo "  2. 用 AIRLOCK_BOOTSTRAP_ADMIN 对应的账号登录控制台，它会自动成为平台管理员"
    echo "  3. 未配置授权文件时系统以试用模式运行（5 席位）；正式授权见部署文档「投放授权文件」"
    echo ""
}

main "$@"
```

- [ ] **Step 2：语法检查**

Run: `bash -n deploy/release/install.sh && echo "语法正确"`
Expected: 打印 `语法正确`

- [ ] **Step 3：加可执行位**

Run: `chmod +x deploy/release/install.sh && ls -l deploy/release/install.sh`
Expected: 权限含 `x`

- [ ] **Step 4：提交**

```bash
git add deploy/release/install.sh
git commit -m "feat: 一键安装脚本，每处失败都给出排查指引"
```

---

## Task 9：打包脚本与 make release

**Files:**
- Create: `scripts/build-release.sh`
- Modify: `Makefile`

- [ ] **Step 1：写打包脚本**

`scripts/build-release.sh`：

```bash
#!/usr/bin/env bash
#
# 打交付包。在**我们的构建机**上运行，需要外网。
#
#   scripts/build-release.sh [--version v1.0.0] [--platform linux/amd64] [--allow-dirty]
#
# 产出 dist/airlock-<版本>-<os>-<arch>.tar.gz。

set -euo pipefail

cd "$(dirname "$0")/.."

PLATFORM="linux/amd64"
VERSION=""
ALLOW_DIRTY=0

while [ $# -gt 0 ]; do
    case "$1" in
        --version)     VERSION="$2"; shift 2 ;;
        --platform)    PLATFORM="$2"; shift 2 ;;
        --allow-dirty) ALLOW_DIRTY=1; shift ;;
        *) echo "未知参数: $1" >&2; exit 2 ;;
    esac
done

# ---- 版本号 ----
if [ -z "$VERSION" ]; then
    VERSION=$(git describe --tags --always)
fi

if [ -n "$(git status --porcelain)" ]; then
    if [ "$ALLOW_DIRTY" -eq 0 ]; then
        echo "错误: 工作区有未提交的改动，拒绝打包。" >&2
        echo "" >&2
        echo "从带未提交改动的树里打出来的包无法复现——客户报障时我们拿不出" >&2
        echo "与他手上一模一样的代码。请先提交，或显式传 --allow-dirty。" >&2
        exit 1
    fi
    VERSION="${VERSION}-dirty"
fi

ARCH="${PLATFORM##*/}"
OS="${PLATFORM%%/*}"
NAME="airlock-${VERSION}-${OS}-${ARCH}"
OUT="dist/${NAME}"

echo "==> 版本 ${VERSION}，目标平台 ${PLATFORM}"

rm -rf "$OUT"
mkdir -p "$OUT"

# ---- 1. 构建 airlock 镜像 ----
echo "==> 构建 airlock 镜像"
AIRLOCK_IMAGE="airlock:${VERSION}"
docker build --platform "$PLATFORM" \
    --build-arg "VERSION=${VERSION}" \
    -t "$AIRLOCK_IMAGE" .

# ---- 2. 拉第三方镜像 ----
# 从生产 compose 里读，避免两处各写一份 digest 迟早不一致。
echo "==> 拉取第三方镜像（按 digest）"
# 排掉带 ${} 的那行（airlock 自己的镜像，上面刚 build 出来）。
# 注意不能用 grep -oE '^\s+image: [^$]\S+' —— airlock:${VER} 是以 a 开头的，
# 那个 [^$] 排不掉它，会把自己的镜像又拉一遍（且 registry 上根本没有）。
# 也不用 mapfile：那是 bash 4+ 的内建，而 macOS 自带的还是 bash 3.2。
THIRD_PARTY=()
while IFS= read -r img; do
    [ -n "$img" ] && THIRD_PARTY+=("$img")
done < <(grep -E '^[[:space:]]+image: ' deploy/release/docker-compose.yml \
         | grep -v '\${' | awk '{print $2}')

[ ${#THIRD_PARTY[@]} -gt 0 ] || { echo "错误: 没从 compose 里解析出第三方镜像" >&2; exit 1; }

for img in "${THIRD_PARTY[@]}"; do
    echo "  $img"
    # --platform 必须显式传：构建机可能是 arm64，靠默认行为会打出一个
    # 在客户 x86 服务器上报 exec format error 的包。
    docker pull --platform "$PLATFORM" "$img"
done

# ---- 3. save ----
echo "==> 导出镜像到 images.tar"
docker save -o "${OUT}/images.tar" "$AIRLOCK_IMAGE" "${THIRD_PARTY[@]}"

# ---- 4. VERSION 文件 ----
echo "==> 生成 VERSION"
{
    echo "VERSION=${VERSION}"
    echo "ARCH=${ARCH}"
    echo "OS=${OS}"
    echo "BUILT_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
    echo "GIT_COMMIT=$(git rev-parse HEAD)"
    echo "IMAGE=${AIRLOCK_IMAGE}"
    for img in "${THIRD_PARTY[@]}"; do
        echo "IMAGE=${img}"
    done
} > "${OUT}/VERSION"

# ---- 5. 拷贝其余材料 ----
echo "==> 组装交付包"
cp deploy/release/docker-compose.yml "$OUT/"
cp deploy/release/.env.example       "$OUT/"
cp deploy/release/install.sh         "$OUT/"
chmod +x "${OUT}/install.sh"
cp -r deploy/release/schema          "$OUT/"
cp -r deploy/release/litellm         "$OUT/"
mkdir -p "${OUT}/docs"
cp docs/deploy/deployment.md "${OUT}/docs/"
cp docs/deploy/operations.md "${OUT}/docs/"

# ---- 6. 打 tar ----
echo "==> 打包"
tar -czf "dist/${NAME}.tar.gz" -C dist "$NAME"

echo ""
echo "完成: dist/${NAME}.tar.gz ($(du -h "dist/${NAME}.tar.gz" | cut -f1))"
```

- [ ] **Step 2：语法检查并加可执行位**

Run: `bash -n scripts/build-release.sh && chmod +x scripts/build-release.sh && echo "语法正确"`
Expected: 打印 `语法正确`

- [ ] **Step 3：把 dist/ 挡在 git 之外**

`.gitignore` 里已有 `dist/` 一行（用于 `web/dist`），它同样覆盖仓库根的 `dist/`。确认一下：

Run: `git check-ignore -v dist/airlock-test.tar.gz`
Expected: 输出匹配到 `dist/` 那条规则

- [ ] **Step 4：加 Makefile 目标**

`Makefile` 顶部的 `.PHONY` 行追加 `release`，并在文件末尾加：

```make
# 打交付包。需要外网（要拉第三方镜像）。
# 目标平台默认 linux/amd64，出 arm64 包：
#   make release PLATFORM=linux/arm64
PLATFORM ?= linux/amd64
release:
	scripts/build-release.sh --platform $(PLATFORM)
```

- [ ] **Step 5：提交**

```bash
git add scripts/build-release.sh Makefile
git commit -m "build: 交付包打包脚本与 make release"
```

---

## Task 10：部署文档

**Files:**
- Create: `docs/deploy/deployment.md`

- [ ] **Step 1：写文档**

`docs/deploy/deployment.md`，按以下结构写，每节的实际内容都要写全（不是列提纲）：

**标题**：`# Airlock 部署文档`

**§1 前置要求**——写成一张表：

| 项 | 要求 |
|---|---|
| 操作系统 | Linux x86_64（amd64 包）或 aarch64（arm64 包）；内核 3.10+ |
| Docker | Engine 20.10 或更高，含 compose v2 插件 |
| CPU / 内存 | 最低 4 核 8 GB，推荐 8 核 16 GB |
| 磁盘 | 至少 20 GB 可用（镜像约 3 GB，其余留给数据） |
| 端口 | 8080（数据面）、8081（控制台）、8000（身份提供方）需对使用者放通 |
| 外网 | **不需要**。全程离线安装 |

**§2 安装步骤**——逐条命令：

1. 解压：`tar -xzf airlock-<版本>-linux-amd64.tar.gz && cd airlock-<版本>-linux-amd64`
2. 首次运行：`./install.sh`。脚本会在第 3 步停下来，提示哪几项必须人工填
3. 编辑 `.env` 补齐必填项（见 §3）
4. 再次运行 `./install.sh`，走完全部五步
5. 按脚本末尾的提示配置 Casdoor（见 §4）

**§3 必须人工填写的配置**——逐项说明：`AIRLOCK_BOOTSTRAP_ADMIN`（写谁的 email、它为什么重要：系统内无管理员且此项为空时 control 拒绝启动）、`OIDC_CLIENT_SECRET`（从 Casdoor 里取，§4 有步骤）、`CASDOOR_ORIGIN` 与 `OIDC_REDIRECT_URL`（**必须是浏览器能访问到的地址，写 localhost 只在本机开浏览器时可用**——这是最常见的配置错误）、至少一个供应商密钥。

**§4 配置身份提供方**——Casdoor 里建 airlock 应用的步骤：登录 `http://<主机>:8000`（默认 admin/123，**第一件事是改掉这个口令**）、新建 Application 名为 `airlock`、Redirect URL 填 `.env` 里的 `OIDC_REDIRECT_URL`、把生成的 Client Secret 回填进 `.env`、建首批用户（其中一个的 email 必须与 `AIRLOCK_BOOTSTRAP_ADMIN` 一致）。

**§5 投放授权文件**——把供应方给的 license 文件放到主机某个路径，在 `.env` 里设 `LICENSE_HOST_PATH=<主机绝对路径>` 与 `AIRLOCK_LICENSE_FILE=/etc/airlock/license.txt`，然后 `docker compose up -d airlock-control`。不配置则以试用模式运行（5 席位、不设到期），控制台顶部会有橙色提示条。

**§6 验证清单**——装完后逐条确认：
- `docker compose ps` 六个服务都是 healthy 或 running
- 浏览器打开 `http://<主机>:8081`，能跳到 Casdoor 登录页
- 用 bootstrap 管理员账号登录，能看到「平台管理」工作台
- 控制台顶部横幅：试用模式显示橙条，正式授权且不临期时不显示任何条
- 在控制台签发一把密钥，用它打一次 `POST http://<主机>:8080/v1/chat/completions`，返回 200

**§7 遇到问题**——指向 `operations.md` 的故障排查表。

- [ ] **Step 2：确认文档里的命令都是真的**

逐条核对：文档里出现的每个环境变量名都要在 `deploy/release/.env.example` 里存在；每个端口都要与 compose 的默认值一致；`docker compose ps` 列出的服务名要与 compose 里的一致（`postgres`、`clickhouse`、`litellm`、`casdoor`、`airlock-control`、`airlock-edge`）。

Run:
```bash
grep -oE '\b[A-Z][A-Z_]{4,}\b' docs/deploy/deployment.md | sort -u | while read -r v; do
  grep -q "^${v}=" deploy/release/.env.example || echo "文档提到但模板里没有: $v"
done
```
Expected: 无输出，或只输出确实不是配置项的词（如 `README`）——逐个确认。

- [ ] **Step 3：提交**

```bash
git add docs/deploy/deployment.md
git commit -m "docs: 部署文档"
```

---

## Task 11：运维手册

**Files:**
- Create: `docs/deploy/operations.md`

- [ ] **Step 1：写手册**

`docs/deploy/operations.md`，标题 `# Airlock 运维手册`，按以下结构写全：

**§1 日常操作**——每条给出确切命令：
- 看状态：`docker compose ps`
- 看日志：`docker compose logs -f airlock-control`（服务名可换）
- 重启单个服务：`docker compose restart airlock-control`
- 停止全部：`docker compose down`（**不会删数据卷**）
- 查版本：`docker compose exec airlock-control airlock version`

**§2 备份与恢复**——
- 备份 Postgres：`docker compose exec -T postgres pg_dumpall -U airlock > airlock-pg-$(date +%F).sql`
- 备份 ClickHouse：说明 `chdata` 卷的位置与用 `docker run --rm -v <卷> -v $(pwd):/backup alpine tar czf /backup/ch.tar.gz /var/lib/clickhouse` 的做法
- **强调**：`AIRLOCK_ENCRYPTION_KEY` 必须与数据库一起备份。丢了它，所有已签发密钥的上游凭据都无法解密，等于全部作废
- 恢复步骤

**§3 升级**——
1. 备份（§2）
2. 解压新版交付包到新目录
3. 把旧目录的 `.env` 拷过去
4. 在新目录运行 `./install.sh`
5. 验证（部署文档 §6 的清单）
6. 回滚：停新版、回到旧目录 `docker compose up -d`

**§4 授权管理**——
- 续期：拿到新 license 文件，替换 `LICENSE_HOST_PATH` 指向的文件，`docker compose restart airlock-control`
- 看当前授权状态：控制台顶部横幅，或 `curl -b <会话> http://localhost:8081/api/license`
- 席位满了：停用离职成员即可腾出席位（控制台里把用户置为停用），或联系供应方扩容
- 过期后的行为：**AI 调用不受影响**，管理写操作暂停，查询与吊销仍可用

**§5 密钥吊销**——控制台里单把吊销、按组织子树批量吊销、全局吊销的位置与影响范围；强调吊销在授权过期后仍然可用。

**§6 邮件通知**——`SMTP_ADDR` 留空时**不发信**，这是预期行为不是故障。此时审批仍可在控制台完成；日志里会有 `邮件通知未启用，跳过投递`。注意：数据库里 `notifications.status` 仍会被标成 `sent`，那表示「已交给通知渠道」，而当前渠道是「不发送」。

**§7 故障排查表**——完整写出这张表：

| 现象 | 原因 | 动作 |
|---|---|---|
| 容器启动即退出，日志有 `exec format error` | 交付包架构与机器不符 | `uname -m` 对照包内 `VERSION` 的 `ARCH=`；索取对应架构的包 |
| `docker compose up` 立刻报镜像不存在 | `images.tar` 未导入或导入不全 | 重跑 `./install.sh`（第 2 步会重新导入，无副作用） |
| control 启动失败：`授权文件校验失败` | license 文件损坏或被改过 | 向供应方索取完好的 license；不要手工编辑该文件 |
| control 启动失败：`未配置 AIRLOCK_ENCRYPTION_KEY` | `.env` 里该项为空 | 由 `install.sh` 自动生成；若手工清空过，**不能随便重新生成**——换密钥会导致已签发密钥无法解密 |
| control 启动失败：无管理员且 bootstrap 为空 | `AIRLOCK_BOOTSTRAP_ADMIN` 没填 | 在 `.env` 里填首个管理员的 email，重启 control |
| 登录时报 `x509: certificate signed by unknown authority` | 客户 IdP 用内部 CA 签的证书 | 把 CA 证书挂进 airlock-control 容器的 `/etc/ssl/certs/` 并重启 |
| 登录后跳回登录页 / 回调失败 | `OIDC_REDIRECT_URL` 与浏览器实际访问的地址不一致 | 两者必须完全一致，含协议、主机名与端口 |
| 控制台能开但数据面调用报上游 401 | 供应商密钥未配或无效 | 检查 `.env` 里的 `DEEPSEEK_API_KEY` 等，改完 `docker compose restart litellm` |
| 用量与成本页面空白 | `airlock.usage_records` 表不存在 | 重跑 `./install.sh`（第 5 步幂等）；确认 `CLICKHOUSE_DSN` 已配置 |
| 控制台顶部红条，写操作返回 402 | 授权已过期 | 换新 license 后 `docker compose restart airlock-control`；AI 调用不受影响 |
| 新成员登录报「席位已满」 | 席位用尽 | 停用离职成员腾出席位，或联系供应方扩容 |
| 审批邮件没收到 | 未配置 `SMTP_ADDR` | 预期行为，见 §6。需要发信就填内网 relay 后重启 control |
| 端口冲突，compose 起不来 | 8080/8081/8000 被占用 | 改 `.env` 里的 `CONTROL_PORT` / `EDGE_PORT` / `CASDOOR_PORT`；改了 `CASDOOR_PORT` 要同步改 `CASDOOR_ORIGIN` 与 `OIDC_*` |

**§8 收集诊断信息**——报障时该给供应方什么：`VERSION` 文件内容、`docker compose ps` 输出、相关服务最近的日志、`.env` **去掉口令后**的内容。

- [ ] **Step 2：核对表里的错误文案与代码一致**

排查表里引用的几条报错必须与代码里真实的文案一致，否则客户按现象查表查不到。逐条核对：

Run:
```bash
grep -rn "授权文件校验失败" internal/app/control.go
grep -rn "未配置 AIRLOCK_ENCRYPTION_KEY" internal/app/control.go
grep -rn "席位已满" internal/control/license.go
grep -rn "邮件通知未启用" internal/notify/disabled.go
```
Expected: 四条都能匹配到。若文案对不上，以代码为准改文档。

- [ ] **Step 3：提交**

```bash
git add docs/deploy/operations.md
git commit -m "docs: 运维手册与故障排查表"
```

---

## Task 12：端到端验收

**Files:**
- Create: `docs/superpowers/findings/2026-09-06-airlock-p1.5c-acceptance.md`

**开始前的纪律**：手工验收期间**不要**跑 `go test ./internal/control/...`——它的 `cleanTables` 会清空共享开发库。要跑测试就在验收开始前跑完。

- [ ] **Step 1：验收前跑完全量测试**

Run: `make lint`
Expected: 全绿

Run:
```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" \
CLICKHOUSE_DSN="clickhouse://airlock:airlock_dev@localhost:9000/airlock" \
go test ./... -race -count=1
```
Expected: 全包绿

Run: `cd web && npx vitest run && cd ..`
Expected: 全绿

- [ ] **Step 2：打 arm64 包（用于验证安装流程）**

Run: `make release PLATFORM=linux/arm64`
Expected: 产出 `dist/airlock-<版本>-linux-arm64.tar.gz`，末尾打印体积

- [ ] **Step 3：打 amd64 包（用于验证架构正确性）**

Run: `make release`
Expected: 产出 `dist/airlock-<版本>-linux-amd64.tar.gz`

- [ ] **Step 4：验证 amd64 包里的镜像确实是 amd64**

```bash
mkdir -p /tmp/p15c-amd64 && tar -xzf dist/airlock-*-linux-amd64.tar.gz -C /tmp/p15c-amd64 --strip-components=1
cat /tmp/p15c-amd64/VERSION
docker load -i /tmp/p15c-amd64/images.tar
grep '^IMAGE=' /tmp/p15c-amd64/VERSION | cut -d= -f2- | while read -r img; do
  printf "%-70s %s\n" "$img" "$(docker image inspect "$img" --format '{{.Architecture}}')"
done
```
Expected: 五行全部是 `amd64`；`VERSION` 里 `ARCH=amd64`

**这是 amd64 包唯一能在本机做的验证**——容器本身在 arm64 上跑不起来。

- [ ] **Step 5：验证架构检查确实拦得住**

在 arm64 机器上拿 amd64 包跑安装脚本，应当在第 1 步就被拦下：

```bash
cd /tmp/p15c-amd64 && ./install.sh; echo "exit=$?"
```
Expected: 第 1 步报错「交付包的架构与本机不符：包是 amd64，本机是 arm64」，并给出「向供应方索取 arm64 架构的交付包」的指引，退出码 1。**没有任何容器被启动。**

- [ ] **Step 6：走完整的 arm64 安装流程**

先停掉开发栈——它的 casdoor 也占 8000 端口。用 `stop` 而不是 `down`：
`down` 会删掉容器，而恢复时需要 `.env`，那个文件在 worktree 里并不存在。
`stop`/`start` 只是暂停，不需要任何配置文件。

```bash
docker compose -p deploy stop
mkdir -p /tmp/p15c-arm64 && tar -xzf dist/airlock-*-linux-arm64.tar.gz -C /tmp/p15c-arm64 --strip-components=1
cd /tmp/p15c-arm64 && ./install.sh; echo "exit=$?"
```
Expected: 走到第 3 步停下，退出码 3，列出三项必填配置

- [ ] **Step 7：填配置后完成安装**

编辑 `/tmp/p15c-arm64/.env`：`AIRLOCK_BOOTSTRAP_ADMIN=admin@example.com`、`OIDC_CLIENT_SECRET=<占位值>`、`DEEPSEEK_API_KEY=<占位值>`（本机没有真实供应商凭据，填占位值即可——验证的是安装流程，不是模型调用）。

Run: `cd /tmp/p15c-arm64 && ./install.sh; echo "exit=$?"`
Expected: 五步全部走完，退出码 0，末尾打印控制台地址

- [ ] **Step 8：验证装出来的东西真的能用**

```bash
cd /tmp/p15c-arm64
docker compose ps
curl -s http://localhost:8081/healthz
curl -s http://localhost:8080/healthz
docker compose exec -T airlock-control airlock version
curl -s -o /dev/null -w "控制台首页 HTTP %{http_code}\n" http://localhost:8081/
```
Expected: 六个服务健康；两个 `/healthz` 都返回 `{"status":"ok"}`；`airlock version` 打印与 `VERSION` 文件一致的版本号；首页返回 200（证明控制台静态站确实嵌进了镜像）

再确认试用模式生效（没配 license）：
```bash
docker compose logs airlock-control | grep -E "试用模式|SMTP"
```
Expected: 出现「未配置 AIRLOCK_LICENSE_FILE，以试用模式运行 seats=5」与「未配置 SMTP_ADDR，邮件通知未启用」

- [ ] **Step 9：验证重跑安装是安全的**

先记下当前 `.env` 的口令，再重跑：

```bash
cd /tmp/p15c-arm64
md5sum .env > /tmp/env-before.txt
./install.sh; echo "exit=$?"
md5sum .env > /tmp/env-after.txt
diff /tmp/env-before.txt /tmp/env-after.txt && echo ".env 未被改动"
```
Expected: 退出码 0；`.env` 的 md5 不变（`AIRLOCK_VERSION` 那行会被重写，但值相同所以 md5 一致）；schema 步骤无错

- [ ] **Step 10：清理并恢复开发栈**

```bash
cd /tmp/p15c-arm64 && docker compose down -v
cd -
rm -rf /tmp/p15c-arm64 /tmp/p15c-amd64
docker compose -p deploy start
docker ps --format '{{.Names}}' | sort
```
Expected: 验收栈连数据卷一起删干净；开发栈六个容器（deploy-*）重新跑起来

- [ ] **Step 11：验收后再跑一次全量测试**

Run: `make lint`
Expected: 全绿

Run:
```bash
POSTGRES_DSN="postgres://airlock:airlock_dev@localhost:5432/airlock?sslmode=disable" \
CLICKHOUSE_DSN="clickhouse://airlock:airlock_dev@localhost:9000/airlock" \
go test ./... -race -count=1
```
Expected: 全包绿

- [ ] **Step 12：写验收记录**

`docs/superpowers/findings/2026-09-06-airlock-p1.5c-acceptance.md`，逐条记录上面每一步的实际观测（命令、输出、退出码），并**必须**包含这一节：

> ## 验收局限
>
> 构建机是 arm64，**amd64 交付包无法在本机端到端运行**（容器起不来）。验收因此拆成两半：安装流程在 arm64 包上验证，架构正确性在 amd64 包上通过镜像属性检查验证。
>
> **没有任何一步在真实的 x86_64 无外网机器上跑过。** 真正的 x86 首装仍然是首个客户现场的风险点。

同时记录任何偏离与环境限制（例如供应商密钥为占位值，因此未验证真实模型调用）。

- [ ] **Step 13：提交**

```bash
git add docs/superpowers/findings/2026-09-06-airlock-p1.5c-acceptance.md
git commit -m "docs: P1.5c 端到端验收记录"
```

---

## 完成后

**REQUIRED SUB-SKILL:** 使用 `superpowers:finishing-a-development-branch` 收尾。按本项目惯例选**选项 2（Push and create a Pull Request）**——由用户在 GitHub 上审阅合并，不在本地 merge。
